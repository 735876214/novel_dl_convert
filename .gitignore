__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/

# 运行时目录（容器挂载点，不入库）
input/
output/
cookies/
cache/
config/cookies/
config/cache/

# 构建 / 产物
*.epub
*.mobi
*.azw3
build/
dist/
*.egg-info/

# 前端构建产物：由 frontend/ 构建生成（npm run deploy 或 Dockerfile 多阶段构建），
# 不入库，避免源码与产物两份真值源。
novelforge/static/v2/

# 本地冒烟测试脚手架（非项目产物）
_test/

# 系统
.DS_Store
.git
*.log
*.err

# AI 工作目录
#   记忆已于 2026-09-15 统一：.codebuddy/memory/ 是唯一真值源，
#   .workbuddy/memory/ 只剩一个指针文件（不再承载内容，勿往那里写）。
#   两个目录都只放行 memory/ 入库（跨会话项目知识），
#   settings.local.json、plans/ 等本机状态一律忽略。
#   写法要点：否定规则必须配不带尾斜杠的 /* 形式，否则无法重新包含子目录。
.workbuddy/*
!.workbuddy/memory/
.codebuddy/*
!.codebuddy/memory/
