# 2026-09-18

## compose 双版本整理
- 把三个 docker-compose 文件收敛为两个：真实版 `docker-compose.yml`（8992，ghcr 预构建，./data）；测试版 `docker-compose.test.yml`（8993，本地 build + 挂源码，./data-test 隔离）。删除了 dev/local 两个冗余文件。`.gitignore` 加了 `/data-test/`。
- 用户确认：测试版用「本地 build」方式、与真实版数据目录隔离。

## 设置页左侧列替换（前端）
- 需求：进 `/settings` 后，外壳主侧栏（AppSidebar 书籍轨道）整列替换为设置导航列，顶部加「返回主界面」按钮回仪表盘 `/`。
- 实现：新建 `frontend/src/components/SettingsSidebar.vue`（复用 AppSidebar 卡片样式 + 顶部返回键 + SETTINGS_GROUPS 分组导航）；`App.vue` 按 `route.path.startsWith('/settings')` 条件渲染 SettingsSidebar/AppSidebar；`SettingsLayout.vue` 删除内容区内重复的设置导航，仅留页头+面包屑+RouterView。
- `npm run build` 通过（Node 20.18 触发 Vite 版本提示但构建成功），无 TS/lint 错误。
- 用户取消了 AI 示意效果图的生成（未产出）。
- **排查记：用户反馈「点齿轮后左列没替换主侧栏」**——根因不是代码 bug（App.vue 条件渲染正确），而是**我只跑了 `npm run build`（只更新 `frontend/dist`），没跑 `npm run deploy`，所以 Docker 测试版 8993 服务的 `novelforge/static/v2` 仍是旧产物**（旧版设置导航画在内容区、主侧栏照旧）。带 `NODE_OPTIONS=""` 跑 `npm run deploy` 同步后，`static/v2` 已含新组件（grep 到 `返回主界面`）。**结论：验证 8993 前必须 build+deploy 两步都做**，否则看到的是旧界面。

## 环境/网络约束（重要）
- 本机**无外网**：Docker Hub（auth.docker.io）与 ghcr 拉取均超时（DeadlineExceeded）。`docker-compose.test.yml` 是本地 build（FROM node:22-bookworm-slim），基础镜像不在本地缓存，**断网环境无法构建**，勿用 `--build` 启动它。
- 本地仅缓存 `ghcr.io/735876214/novel_dl_convert:latest`（真实版镜像，464MB）。
- **「Failed to fetch」根因**：测试/真实容器都没在跑（docker ps -a 空），前端页面加载后所有 `/api` 请求连不上 → 报 Failed to fetch。非前端代码 bug。
- 离线启动真实版镜像的临时方案：新增 `docker-compose.override.yml`（`pull_policy: never` + 端口改 8993 + 挂载 `./novelforge:/app/novelforge`），用 `docker compose -f docker-compose.yml -f docker-compose.override.yml up -d` 启动。复用缓存镜像、挂上 host 的 `novelforge/static/v2`（含最新前端），后端同源可登录（admin/changeme）。**联网恢复后删除该 override 文件即可回归正常流程。**
- 真实版 compose 自带 `pull_policy: always`，离线直接 `docker compose up` 会拉取失败；必须用 override 关掉或改用 `docker run` 指定本地镜像。

## 上游缺口统计 + 第 6 期（前端快赢）实施
- 用户要求：统计"上游 BookOrbit v2.10.0 有、本项目缺失"的功能，列清单 + 分期计划，并写进跟踪基线。
- 产出基线文档 `docs/roadmap-gaps-remaining.md`：A 类（纯前端 9 项）/ B 类（轻后端 4 项）/ C 类（重 3 项）+ 明确不做的 9 项（国际化/在线抓取/作者传记/有声书/Pages/多书库/多用户/Kobo邮件/CBR）。
- **第 6 期（A1–A9）全部完成并 build+deploy 通过**：
  - A1 顶栏外观快捷浮层 `AppearanceMenu.vue`（复用 Segment/SwatchGrid/theme store，沿用 NotificationBell 浮层模式），替换 AppHeader 旧主题循环按钮。
  - A2 作者页排序(书量/姓名)+「2+本」筛选；A3 作者详情排序(书名/系列/最近添加)+「打开最近添加」；A4/A5 系列详情首册标记+顺序/倒序+诚实的 SYNOPSIS「未提供」说明。
  - A6 `/whats-new` + A7 `/docs` 两新页面（静态数据驱动，已注册路由，About 页链入）。
  - A8 BookDock 整页拖拽投递遮罩（复用后端 `/convert` 写 INPUT_DIR，新增 `api.convertDrop`）；A9 书架级控制条已在 ShelfView 工具栏实装，标记完成。
- 已知未做（需后端，留待第 7 期）：B1 上传多格式、B2 Metadata Score、B3 Book Dock 5态状态机、B4 用户菜单；A2 的「Added this week」需后端 author 级 added 字段。
- 验证纪律沿用：vue-tsc 类型检查 + `npm run build` + `npm run deploy`（同步 static/v2）。离线环境无法起容器验证运行态，但构建/部署产物已更新。

## 第 7 期（B 类轻后端）启动
- 本地测试实例（127.0.0.1:8791，uvicorn 直跑，已获授权重启/语法校验）可用来验证后端——本机 OS 是 macOS，无 Docker 离线问题（Docker 仍可能离线，但 uvicorn 直跑不依赖镜像）。
- **B1 上传多格式 ✅**：`/convert` 原硬校验仅 `.txt`（`server.py:3268`）。`pipeline.dispatch` 本就支持 EBOOK_EXT（EPUB/PDF/CBZ/FB2/MOBI/AZW3 直接入库），只是被该 guard 挡住。放开 guard 即可；前端 BookDock 拖拽提示同步改为多格式。已端到端验证（pdf/epub 200 入库、txt 200 转 EPUB、docx 400 拒绝）。
- **B4 用户菜单浮层 ✅**：顶栏头像原是静态 div。新增 `UserMenu.vue`（复用 NotificationBell 浮层模式：点外部/Esc 关闭），含用户名 + 个人资料入口（/settings/account/profile，ProfilePage 此前已有改密码+退出）+ 退出登录（清 `nf_token` 并 dispatch `nf-unauthorized` 弹回登录门禁）。另在 `lib/icons.ts` 加 `logout` 图标。已构建部署。
- **B3 收书目录五态状态机 ✅**：`db.book_dock_items`（id = 投递文件名，目录是平的）+ `core/bookdock.py`（pending/ready/needs_review/error + ignored 隐藏终态）；watcher 新增 `on_scan` 回调 + 运行时忽略集（`add_ignore`/`is_ignored`/`iter_files`），**由 server 注入回调避免 core 循环依赖**；`/api/book-dock`（GET 时懒对账 reconcile）+ `{id}/rescan|ignore|delete`。**delete 走回收目录（fileops.recycle_dir），不 unlink**。前端 BookDockPage 加 5 标签（全部/待复核/待处理/就绪/出错）+ 逐条操作。
- **B2 元数据完整度评分 ✅**：`core/metascore.py` 12 字段 × 5 组（core 40 / publishing 23 / classification 20 / provider_ids 10 / enrichment 7，加总 100）；**非 EPUB 的 cover/pages 不计入分母**（归一化，PDF 不被平白扣分）；P50/P90 线性插值；4 档 <50/50-69/70-89/90+。`GET /api/metadata-score?force=` + `stats.overview.metadata_score`。前端新建 `MetadataScoreCard.vue` 接进 MetadataPage 的 `score` 段（该页原有「抓取候选阈值」已改名，避免与完整度混淆）。
- 第 7 期 B1/B2/B3/B4 **全部完成并验证**：后端 py_compile + 重启 8791 + 带 token 实调 `/api/book-dock`、`/api/metadata-score`；前端 vue-tsc + build + deploy 全绿。
- 验证纪律补充：临时脚本放 `/tmp`（跑完删），因 workspace 外 delete_file 无效需 `rm`；`pkill -f "port 8791"` 重启前记得 `mkdir -p /tmp/nf-test/{input,output,config,cache,logs,data}`。
- **已提交并推送**：`ad83318..c7c7a0e`，4 个中文 commit —— `feat(backend)` 第 7 期 B 类（bookdock/metascore/db/watcher/stats/server）、`feat(frontend)` 第 6–7 期（含 SettingsSidebar/UserMenu/MetadataScoreCard）、`chore(build)` compose 精简为真实版+测试版、`docs` 缺口基线+工作记忆。工作区已干净，与 origin/main 同步。
- **「不做」清单复核（以代码实况为准）+ 重新排期**：原基线 9 项里有 6 项判定过期/过粗。
  - 纠正：①**在线元数据抓取已实现**（第 5 期 `metasources`+`metafetch`，不是"不做"）；②**pages 已实现**（EPUB 估算 `pages_source='estimate'`、CBZ 真实 `='archive'`，且 B2 已计入 Publishing 4 分）。
  - 移入排期：作者传记/头像 → 第 8 期；CBR（需 RAR 依赖）、有声书（需音频格式+播放器）→ 第 9 期（有前置才开工）；多书库（动摇单一 `OUTPUT_DIR`）→ 第 10 期。
  - 仍不做：国际化、多用户能力、Kobo/邮件投递、元数据「源插件市场」。
  - 顺带发现：`settingsNav` 的 `metadata/authors` 页 status 标 `ready` 但实际只有通用抓取开关（名实不符），列为 D3 待做实。
  - 落地：`docs/roadmap-gaps-remaining.md` 新增 D 类（D1–D8）待实施清单 + 第 8/9/10 期排期，并加「已修正的过期记载」小节。**（该文档改动尚未提交）**

## 第 8 期（元数据：在线优先 / 本地兜底 / 可编辑）完成
- 用户原则：「元数据优先使用在线数据；在线数据错误则用本地数据；且具备编辑修改功能」。范围经澄清取**最大档**：作者 + 书籍一起改（含把书籍抓取默认从 `fill_only` 翻转为 `overwrite`）。
- **书籍侧**
  - `core/db.py` 新增 `meta_override(book_id, field, value, orig, updated_at)` 与 `meta_online(book_id, field, value, source, fetched_at)`。`orig` = **首次覆盖前的 OPF 原值**（老库有 `ALTER TABLE ... ADD COLUMN orig` 迁移）。
  - 新建 `core/metastore.py`：生效值分层 **override > online > opf**（`effective()` / `state()` 逐字段 value/online/opf/overridden）。**只服务详情/编辑接口，`library.books()` 热路径不动**（控制爆炸半径）。
  - `core/metafetch.py`：`DEFAULT_POLICY` 由 `fill_only` → **`overwrite`**（`config.py` 默认字段策略同步翻转）；`plan()` 一次性 `db.all_overrides()` 并**跳过用户改过的字段**；`apply()` 把写回的在线值记入 `meta_online`；新增纯查询 `online_candidate()`。`auto_fetch` 语义同步。
  - `server.py`：`GET/POST /api/books/{bid}/metadata` 返回 `fields`(生效值) + `meta`(明细)；POST 只对**与 OPF 不同**的字段记 override（带 `orig`），一致则撤覆盖；新增 `GET .../metadata/online`（实时在线建议）与 `POST .../metadata/revert`（撤覆盖；有在线值写回在线值，无则还原 `orig`）。
  - 前端 `MetadataEditor.vue`：「已本地修改」徽标 + 逐字段「恢复在线」+「恢复全部为在线」+ 在线值提示；`api.ts` 加 `MetaFieldState/MetaStateMap/MetadataOnlineResult/MetadataRevertResult`。
- **作者侧（D1/D2/D3/D5）**
  - `core/db.py` 新增 `authors(name PK, bio, bio_local, photo_path, photo_source, photo_local_path, fetched_at)`；写覆盖用 **upsert**（作者从没抓取过也要能落库 —— 踩过 `UPDATE` 静默失败）。
  - 新建 `core/authors.py`：OpenLibrary 作者检索（`/search/authors.json` → `/authors/{key}.json`）取传记 + 头像 `photos[0]`；头像**下载到 `CACHE_DIR/authors/`**（零外链）；归一化名相似度 <0.5 视为不同人（宁可放弃不给错配）；全程容错不抛。
  - `server.py`：`GET /api/authors` 加 `has_photo/added_ts`（added = 名下最早一本书 mtime）；`GET /api/authors/{name}` 加 `bio/bio_overridden/has_photo/photo_overridden/photo_source/fetched_at/added_ts`；新增 `GET /{name}/photo`（分发，无图 404）、`POST /{name}/bio`、`POST|DELETE /{name}/photo`、`POST /{name}/fetch`、`POST /fetch-all`。**头像端点已加入 `_MEDIA_TOKEN_PATHS`**（`<img src>` 只能靠 `?token=`）。
  - 前端：`AuthorsView` 有头像显示头像（否则书封占位）+「本周新增」筛选 +「新」徽标；`AuthorDetailView` 资料卡（头像/传记 + 抓取在线资料/编辑传记/上传头像/恢复在线）；`MetadataPage` 作者区块做实（启用/抓传记/抓头像 + 立即抓取全部作者）。
- **D4**：`metasources.py` 抽出 `_ol_entry/_gb_entry`，新增 **ISBN 精确匹配** `search_by_isbn()`（命中即 `score=1.0`、`exact_isbn=True`）；`metafetch.plan` 与 `online_candidate` **优先用 ISBN 命中**，否则回退书名 + 作者检索。
- **验证**：后端 `py_compile` 全绿；前端 `vue-tsc`（type-check）0 错误 + `npm run build` + `npm run deploy`；重启 8791 带 token 冒烟 —— 作者列表新字段、书籍 set→override(记录 orig)→revert 还原、作者 bio 覆盖/清除、头像 404 容错，全部符合预期。

## 第 9 期（媒体形态扩展：CBR + 有声书）完成
- 用户口径（全取最大档）：**D6 + D7 都做**；CBR **引入系统依赖完整支持**；有声书按**文件夹多轨 = 一本书**；播放器**完整**（倍速/跳转间隔/睡眠定时 + 设置页做实）。多书库（库类型/来源目录/自动归类）**单列第 10 期**，规格已写进 `docs/roadmap-gaps-remaining.md`。
- **D6 CBR**
  - `core/comics.py` 重构为 **zip/rar 双后端**：`_Archive` 统一 `entries()/read()`；**魔数嗅探**（`PK` → zipfile，`Rar!` → rarfile）不信任扩展名；`probe/pages/page_bytes/cover_entry` **签名不变**（上层 zero-branch）；新增 `is_comic/is_cbr/rar_available/cover_bytes`；RAR 目录项按「名字以 `/` 结尾」判定。
  - `requirements.txt` 加 `rarfile>=4.0`；`Dockerfile` runtime 加 **`libarchive-tools`**（提供 bsdtar；macOS 自带）。缺解压器时 comic 路由返回 **503** 而非 500。
  - `library.BOOK_EXTS` 收 `.cbr`；`_scan_once` 分派改用 `comics.is_comic`；`server.api_book_cover` 的 CBZ 分支改为 `fmt in ("CBZ","CBR")` 并改用 `comics.cover_bytes`（**修掉它原先自己 `zipfile` 读一遍封面、会绕过 comics 抽象的问题**）；两个 comic 路由由写死 `!= "CBZ"` 放开；`komga_api` 页面流、`opds._MIME`（`application/vnd.comicbook-rar`）、`koreader.from_nf` 的 `has_pages` 同步。
- **D7 有声书**
  - 新增 `core/audio.py`：`AUDIO_EXTS`（m4b/mp3/m4a/opus/ogg/flac/aac/wav）、`is_audio`、`is_audio_dir`（只看直接子文件，不递归）、`tracks`（单文件 1 轨 / 目录自然序多轨）、`track_path`、`cover_in_dir`（cover/folder/poster…）、`dir_size_and_mtime`。
  - `core/library.py`：`_iter_book_files` → **`_iter_book_entries`（文件 或 含音频的目录）**；书目 `format` 统一 `"AUDIO"`、新增 `tracks` 字段（音频目录 size=音频体积和、mtime=最新音频 mtime）；**`_dir_signature` 与枚举同源**且把目录内部文件数与最新 mtime 计入（否则「加了音频但列表不刷新」，且「重建索引」也修不回来）；`book_detail` 对音频下发 `audio_tracks` 并跳过目录的兄弟文件枚举。
  - 摄入：`pipeline.EBOOK_EXT` 加 `.cbz/.cbr/*audio`；`dispatch` 新增**目录分支**（`audio.is_audio_dir` 才复制，`_copy_tree` 整树复制，落盘名用新增的 `komga.relpath_for_dir`，**不带伪扩展名**）；`watcher` 新增 `_iter_entries`、目录感知的 `_sig`/`_wait_stable`、`handle_file` 目录分支（指纹按内部总量，递归模式下不会把音频目录与其内部文件重复入库）。
  - `server`：`GET /api/books/{bid}/audio`（轨清单）与 `/audio/{index}`（**`FileResponse` → 自带 Range 206**，拖拽跳转必需）；`_MEDIA_TOKEN_PATHS` 加 `^/api/books/[^/]+/audio/\d+$`（`<audio src>` 带不了 Bearer）；封面对 AUDIO 走目录封面文件；`PREFS_BLOCKS` 加 `audio`。
  - 前端：`lib/audioPrefs.ts`（`audio-prefs` 键 + 档位常量）、`components/reader/AudioPlayer.vue`（播放/进度/上下轨/倍速/快退快进/睡眠定时/轨道列表；进度语义 `locator=当前轨秒数`、`percent=按轨加权`，恢复时由 percent 反推轨号）、`views/AudioPlayerView.vue`（路由 **`/listen/:id`**）；`BookDetailView` 按格式分流（`canRead` 顺带修好了原先只认 EPUB、却与 ReaderView 支持 PDF/CBZ 不一致的问题；AUDIO → 播放器，按钮文案变「开始/继续播放」）；`ReaderView.isComic` 放开 CBZ/CBR + AUDIO 深链兜底跳播放器；`bookInfo.pagesLabel` 对 AUDIO 显示「N 轨」而非「0 页」；`icons.ts` 新增 `pause/skipBack/skipForward/volume/moon`。
  - 设置页：新增 `views/settings/pages/ReaderAudioPage.vue` 并在 `router` 的 `SETTINGS_PAGE_COMPONENTS` 注册；`settingsNav` 的 `reader/audio` 由 **placeholder → ready**（注意 router 对「ready 但未注册组件」会 `console.error`，两处必须同时改）；偏好同步**三处同步**加 `audio` 块（`prefsPayload` 的 `Payload`/`PAYLOAD_BLOCKS`/`normalizePayload` + `prefSync` 的 `collect`/`applyPayload` + 后端 `PREFS_BLOCKS`），否则推送会 400 或静默丢块。
- **验证**：后端 `py_compile` 全绿；前端 `vue-tsc` 0 错误 + build + deploy；重启 8791 冒烟 —— 音频目录扫描（2 轨 / 封面 / size 汇总）、`/audio` 轨清单、**Range 请求 206 且 Content-Range 正确**、音频封面 200、CBZ 页清单与封面 200、坏 CBR 优雅降级（pages 0 / 封面 404 / `unparsable`）、`/api/books` 出现 `AUDIO`/`CBR` 两种 format、SPA 200 且已服务新 bundle。
- **补充复验（偏好多块同步）**：`PUT /api/prefs/devices/{id}` 带 `audio` 块 → **200 且值原样保留**；同时带一个非法块（`bogus`）→ **400「未知的偏好块」**。证明 `PREFS_BLOCKS`/`PAYLOAD_BLOCKS`/`normalizePayload` + `prefSync.collect/applyPayload` 四处已一致，跨设备同步不会丢 audio 块、也没放松校验。
