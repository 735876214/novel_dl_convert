# 2026-09-18

## compose 双版本整理
- 把三个 docker-compose 文件收敛为两个：真实版 `docker-compose.yml`（8992，ghcr 预构建，./data）；测试版 `docker-compose.test.yml`（8993，本地 build + 挂源码，./data-test 隔离）。删除了 dev/local 两个冗余文件。`.gitignore` 加了 `/data-test/`。
- 用户确认：测试版用「本地 build」方式、与真实版数据目录隔离。

## 设置页左侧列替换（前端）
- 需求：进 `/settings` 后，外壳主侧栏（AppSidebar 书籍轨道）整列替换为设置导航列，顶部加「返回主界面」按钮回仪表盘 `/`。
- 实现：新建 `frontend/src/components/SettingsSidebar.vue`（复用 AppSidebar 卡片样式 + 顶部返回键 + SETTINGS_GROUPS 分组导航）；`App.vue` 按 `route.path.startsWith('/settings')` 条件渲染 SettingsSidebar/AppSidebar；`SettingsLayout.vue` 删除内容区内重复的设置导航，仅留页头+面包屑+RouterView。
- `npm run build` 通过（Node 20.18 触发 Vite 版本提示但构建成功），无 TS/lint 错误。
- 用户取消了 AI 示意效果图的生成（未产出）。
- **排查记：用户反馈「点齿轮后左列没替换主侧栏」**——根因不是代码 bug（App.vue 条件渲染正确），而是**我只跑了 `npm run build`（只更新 `frontend/dist`），没跑 `npm run deploy`，所以 Docker 测试版 8993 服务的 `novelforge/static/v2` 仍是旧产物**（旧版设置导航画在内容区、主侧栏照旧）。带 `NODE_OPTIONS=""` 跑 `npm run deploy` 同步后，`static/v2` 已含新组件（grep 到 `返回主界面`）。**结论：验证 8993 前必须 build+deploy 两步都做**，否则看到的是旧界面。

## 环境/网络约束（重要）
- 本机**无外网**：Docker Hub（auth.docker.io）与 ghcr 拉取均超时（DeadlineExceeded）。`docker-compose.test.yml` 是本地 build（FROM node:22-bookworm-slim），基础镜像不在本地缓存，**断网环境无法构建**，勿用 `--build` 启动它。
- 本地仅缓存 `ghcr.io/735876214/novel_dl_convert:latest`（真实版镜像，464MB）。
- **「Failed to fetch」根因**：测试/真实容器都没在跑（docker ps -a 空），前端页面加载后所有 `/api` 请求连不上 → 报 Failed to fetch。非前端代码 bug。
- 离线启动真实版镜像的临时方案：新增 `docker-compose.override.yml`（`pull_policy: never` + 端口改 8993 + 挂载 `./novelforge:/app/novelforge`），用 `docker compose -f docker-compose.yml -f docker-compose.override.yml up -d` 启动。复用缓存镜像、挂上 host 的 `novelforge/static/v2`（含最新前端），后端同源可登录（admin/changeme）。**联网恢复后删除该 override 文件即可回归正常流程。**
- 真实版 compose 自带 `pull_policy: always`，离线直接 `docker compose up` 会拉取失败；必须用 override 关掉或改用 `docker run` 指定本地镜像。

## 上游缺口统计 + 第 6 期（前端快赢）实施
- 用户要求：统计"上游 BookOrbit v2.10.0 有、本项目缺失"的功能，列清单 + 分期计划，并写进跟踪基线。
- 产出基线文档 `docs/roadmap-gaps-remaining.md`：A 类（纯前端 9 项）/ B 类（轻后端 4 项）/ C 类（重 3 项）+ 明确不做的 9 项（国际化/在线抓取/作者传记/有声书/Pages/多书库/多用户/Kobo邮件/CBR）。
- **第 6 期（A1–A9）全部完成并 build+deploy 通过**：
  - A1 顶栏外观快捷浮层 `AppearanceMenu.vue`（复用 Segment/SwatchGrid/theme store，沿用 NotificationBell 浮层模式），替换 AppHeader 旧主题循环按钮。
  - A2 作者页排序(书量/姓名)+「2+本」筛选；A3 作者详情排序(书名/系列/最近添加)+「打开最近添加」；A4/A5 系列详情首册标记+顺序/倒序+诚实的 SYNOPSIS「未提供」说明。
  - A6 `/whats-new` + A7 `/docs` 两新页面（静态数据驱动，已注册路由，About 页链入）。
  - A8 BookDock 整页拖拽投递遮罩（复用后端 `/convert` 写 INPUT_DIR，新增 `api.convertDrop`）；A9 书架级控制条已在 ShelfView 工具栏实装，标记完成。
- 已知未做（需后端，留待第 7 期）：B1 上传多格式、B2 Metadata Score、B3 Book Dock 5态状态机、B4 用户菜单；A2 的「Added this week」需后端 author 级 added 字段。
- 验证纪律沿用：vue-tsc 类型检查 + `npm run build` + `npm run deploy`（同步 static/v2）。离线环境无法起容器验证运行态，但构建/部署产物已更新。

## 第 7 期（B 类轻后端）启动
- 本地测试实例（127.0.0.1:8791，uvicorn 直跑，已获授权重启/语法校验）可用来验证后端——本机 OS 是 macOS，无 Docker 离线问题（Docker 仍可能离线，但 uvicorn 直跑不依赖镜像）。
- **B1 上传多格式 ✅**：`/convert` 原硬校验仅 `.txt`（`server.py:3268`）。`pipeline.dispatch` 本就支持 EBOOK_EXT（EPUB/PDF/CBZ/FB2/MOBI/AZW3 直接入库），只是被该 guard 挡住。放开 guard 即可；前端 BookDock 拖拽提示同步改为多格式。已端到端验证（pdf/epub 200 入库、txt 200 转 EPUB、docx 400 拒绝）。
- **B4 用户菜单浮层 ✅**：顶栏头像原是静态 div。新增 `UserMenu.vue`（复用 NotificationBell 浮层模式：点外部/Esc 关闭），含用户名 + 个人资料入口（/settings/account/profile，ProfilePage 此前已有改密码+退出）+ 退出登录（清 `nf_token` 并 dispatch `nf-unauthorized` 弹回登录门禁）。另在 `lib/icons.ts` 加 `logout` 图标。已构建部署。
- **B3 收书目录五态状态机 ✅**：`db.book_dock_items`（id = 投递文件名，目录是平的）+ `core/bookdock.py`（pending/ready/needs_review/error + ignored 隐藏终态）；watcher 新增 `on_scan` 回调 + 运行时忽略集（`add_ignore`/`is_ignored`/`iter_files`），**由 server 注入回调避免 core 循环依赖**；`/api/book-dock`（GET 时懒对账 reconcile）+ `{id}/rescan|ignore|delete`。**delete 走回收目录（fileops.recycle_dir），不 unlink**。前端 BookDockPage 加 5 标签（全部/待复核/待处理/就绪/出错）+ 逐条操作。
- **B2 元数据完整度评分 ✅**：`core/metascore.py` 12 字段 × 5 组（core 40 / publishing 23 / classification 20 / provider_ids 10 / enrichment 7，加总 100）；**非 EPUB 的 cover/pages 不计入分母**（归一化，PDF 不被平白扣分）；P50/P90 线性插值；4 档 <50/50-69/70-89/90+。`GET /api/metadata-score?force=` + `stats.overview.metadata_score`。前端新建 `MetadataScoreCard.vue` 接进 MetadataPage 的 `score` 段（该页原有「抓取候选阈值」已改名，避免与完整度混淆）。
- 第 7 期 B1/B2/B3/B4 **全部完成并验证**：后端 py_compile + 重启 8791 + 带 token 实调 `/api/book-dock`、`/api/metadata-score`；前端 vue-tsc + build + deploy 全绿。
- 验证纪律补充：临时脚本放 `/tmp`（跑完删），因 workspace 外 delete_file 无效需 `rm`；`pkill -f "port 8791"` 重启前记得 `mkdir -p /tmp/nf-test/{input,output,config,cache,logs,data}`。
- **已提交并推送**：`ad83318..c7c7a0e`，4 个中文 commit —— `feat(backend)` 第 7 期 B 类（bookdock/metascore/db/watcher/stats/server）、`feat(frontend)` 第 6–7 期（含 SettingsSidebar/UserMenu/MetadataScoreCard）、`chore(build)` compose 精简为真实版+测试版、`docs` 缺口基线+工作记忆。工作区已干净，与 origin/main 同步。
- **「不做」清单复核（以代码实况为准）+ 重新排期**：原基线 9 项里有 6 项判定过期/过粗。
  - 纠正：①**在线元数据抓取已实现**（第 5 期 `metasources`+`metafetch`，不是"不做"）；②**pages 已实现**（EPUB 估算 `pages_source='estimate'`、CBZ 真实 `='archive'`，且 B2 已计入 Publishing 4 分）。
  - 移入排期：作者传记/头像 → 第 8 期；CBR（需 RAR 依赖）、有声书（需音频格式+播放器）→ 第 9 期（有前置才开工）；多书库（动摇单一 `OUTPUT_DIR`）→ 第 10 期。
  - 仍不做：国际化、多用户能力、Kobo/邮件投递、元数据「源插件市场」。
  - 顺带发现：`settingsNav` 的 `metadata/authors` 页 status 标 `ready` 但实际只有通用抓取开关（名实不符），列为 D3 待做实。
  - 落地：`docs/roadmap-gaps-remaining.md` 新增 D 类（D1–D8）待实施清单 + 第 8/9/10 期排期，并加「已修正的过期记载」小节。**（该文档改动尚未提交）**

## 第 8 期（元数据：在线优先 / 本地兜底 / 可编辑）完成
- 用户原则：「元数据优先使用在线数据；在线数据错误则用本地数据；且具备编辑修改功能」。范围经澄清取**最大档**：作者 + 书籍一起改（含把书籍抓取默认从 `fill_only` 翻转为 `overwrite`）。
- **书籍侧**
  - `core/db.py` 新增 `meta_override(book_id, field, value, orig, updated_at)` 与 `meta_online(book_id, field, value, source, fetched_at)`。`orig` = **首次覆盖前的 OPF 原值**（老库有 `ALTER TABLE ... ADD COLUMN orig` 迁移）。
  - 新建 `core/metastore.py`：生效值分层 **override > online > opf**（`effective()` / `state()` 逐字段 value/online/opf/overridden）。**只服务详情/编辑接口，`library.books()` 热路径不动**（控制爆炸半径）。
  - `core/metafetch.py`：`DEFAULT_POLICY` 由 `fill_only` → **`overwrite`**（`config.py` 默认字段策略同步翻转）；`plan()` 一次性 `db.all_overrides()` 并**跳过用户改过的字段**；`apply()` 把写回的在线值记入 `meta_online`；新增纯查询 `online_candidate()`。`auto_fetch` 语义同步。
  - `server.py`：`GET/POST /api/books/{bid}/metadata` 返回 `fields`(生效值) + `meta`(明细)；POST 只对**与 OPF 不同**的字段记 override（带 `orig`），一致则撤覆盖；新增 `GET .../metadata/online`（实时在线建议）与 `POST .../metadata/revert`（撤覆盖；有在线值写回在线值，无则还原 `orig`）。
  - 前端 `MetadataEditor.vue`：「已本地修改」徽标 + 逐字段「恢复在线」+「恢复全部为在线」+ 在线值提示；`api.ts` 加 `MetaFieldState/MetaStateMap/MetadataOnlineResult/MetadataRevertResult`。
- **作者侧（D1/D2/D3/D5）**
  - `core/db.py` 新增 `authors(name PK, bio, bio_local, photo_path, photo_source, photo_local_path, fetched_at)`；写覆盖用 **upsert**（作者从没抓取过也要能落库 —— 踩过 `UPDATE` 静默失败）。
  - 新建 `core/authors.py`：OpenLibrary 作者检索（`/search/authors.json` → `/authors/{key}.json`）取传记 + 头像 `photos[0]`；头像**下载到 `CACHE_DIR/authors/`**（零外链）；归一化名相似度 <0.5 视为不同人（宁可放弃不给错配）；全程容错不抛。
  - `server.py`：`GET /api/authors` 加 `has_photo/added_ts`（added = 名下最早一本书 mtime）；`GET /api/authors/{name}` 加 `bio/bio_overridden/has_photo/photo_overridden/photo_source/fetched_at/added_ts`；新增 `GET /{name}/photo`（分发，无图 404）、`POST /{name}/bio`、`POST|DELETE /{name}/photo`、`POST /{name}/fetch`、`POST /fetch-all`。**头像端点已加入 `_MEDIA_TOKEN_PATHS`**（`<img src>` 只能靠 `?token=`）。
  - 前端：`AuthorsView` 有头像显示头像（否则书封占位）+「本周新增」筛选 +「新」徽标；`AuthorDetailView` 资料卡（头像/传记 + 抓取在线资料/编辑传记/上传头像/恢复在线）；`MetadataPage` 作者区块做实（启用/抓传记/抓头像 + 立即抓取全部作者）。
- **D4**：`metasources.py` 抽出 `_ol_entry/_gb_entry`，新增 **ISBN 精确匹配** `search_by_isbn()`（命中即 `score=1.0`、`exact_isbn=True`）；`metafetch.plan` 与 `online_candidate` **优先用 ISBN 命中**，否则回退书名 + 作者检索。
- **验证**：后端 `py_compile` 全绿；前端 `vue-tsc`（type-check）0 错误 + `npm run build` + `npm run deploy`；重启 8791 带 token 冒烟 —— 作者列表新字段、书籍 set→override(记录 orig)→revert 还原、作者 bio 覆盖/清除、头像 404 容错，全部符合预期。

## 第 9 期（媒体形态扩展：CBR + 有声书）完成
- 用户口径（全取最大档）：**D6 + D7 都做**；CBR **引入系统依赖完整支持**；有声书按**文件夹多轨 = 一本书**；播放器**完整**（倍速/跳转间隔/睡眠定时 + 设置页做实）。多书库（库类型/来源目录/自动归类）**单列第 10 期**，规格已写进 `docs/roadmap-gaps-remaining.md`。
- **D6 CBR**
  - `core/comics.py` 重构为 **zip/rar 双后端**：`_Archive` 统一 `entries()/read()`；**魔数嗅探**（`PK` → zipfile，`Rar!` → rarfile）不信任扩展名；`probe/pages/page_bytes/cover_entry` **签名不变**（上层 zero-branch）；新增 `is_comic/is_cbr/rar_available/cover_bytes`；RAR 目录项按「名字以 `/` 结尾」判定。
  - `requirements.txt` 加 `rarfile>=4.0`；`Dockerfile` runtime 加 **`libarchive-tools`**（提供 bsdtar；macOS 自带）。缺解压器时 comic 路由返回 **503** 而非 500。
  - `library.BOOK_EXTS` 收 `.cbr`；`_scan_once` 分派改用 `comics.is_comic`；`server.api_book_cover` 的 CBZ 分支改为 `fmt in ("CBZ","CBR")` 并改用 `comics.cover_bytes`（**修掉它原先自己 `zipfile` 读一遍封面、会绕过 comics 抽象的问题**）；两个 comic 路由由写死 `!= "CBZ"` 放开；`komga_api` 页面流、`opds._MIME`（`application/vnd.comicbook-rar`）、`koreader.from_nf` 的 `has_pages` 同步。
- **D7 有声书**
  - 新增 `core/audio.py`：`AUDIO_EXTS`（m4b/mp3/m4a/opus/ogg/flac/aac/wav）、`is_audio`、`is_audio_dir`（只看直接子文件，不递归）、`tracks`（单文件 1 轨 / 目录自然序多轨）、`track_path`、`cover_in_dir`（cover/folder/poster…）、`dir_size_and_mtime`。
  - `core/library.py`：`_iter_book_files` → **`_iter_book_entries`（文件 或 含音频的目录）**；书目 `format` 统一 `"AUDIO"`、新增 `tracks` 字段（音频目录 size=音频体积和、mtime=最新音频 mtime）；**`_dir_signature` 与枚举同源**且把目录内部文件数与最新 mtime 计入（否则「加了音频但列表不刷新」，且「重建索引」也修不回来）；`book_detail` 对音频下发 `audio_tracks` 并跳过目录的兄弟文件枚举。
  - 摄入：`pipeline.EBOOK_EXT` 加 `.cbz/.cbr/*audio`；`dispatch` 新增**目录分支**（`audio.is_audio_dir` 才复制，`_copy_tree` 整树复制，落盘名用新增的 `komga.relpath_for_dir`，**不带伪扩展名**）；`watcher` 新增 `_iter_entries`、目录感知的 `_sig`/`_wait_stable`、`handle_file` 目录分支（指纹按内部总量，递归模式下不会把音频目录与其内部文件重复入库）。
  - `server`：`GET /api/books/{bid}/audio`（轨清单）与 `/audio/{index}`（**`FileResponse` → 自带 Range 206**，拖拽跳转必需）；`_MEDIA_TOKEN_PATHS` 加 `^/api/books/[^/]+/audio/\d+$`（`<audio src>` 带不了 Bearer）；封面对 AUDIO 走目录封面文件；`PREFS_BLOCKS` 加 `audio`。
  - 前端：`lib/audioPrefs.ts`（`audio-prefs` 键 + 档位常量）、`components/reader/AudioPlayer.vue`（播放/进度/上下轨/倍速/快退快进/睡眠定时/轨道列表；进度语义 `locator=当前轨秒数`、`percent=按轨加权`，恢复时由 percent 反推轨号）、`views/AudioPlayerView.vue`（路由 **`/listen/:id`**）；`BookDetailView` 按格式分流（`canRead` 顺带修好了原先只认 EPUB、却与 ReaderView 支持 PDF/CBZ 不一致的问题；AUDIO → 播放器，按钮文案变「开始/继续播放」）；`ReaderView.isComic` 放开 CBZ/CBR + AUDIO 深链兜底跳播放器；`bookInfo.pagesLabel` 对 AUDIO 显示「N 轨」而非「0 页」；`icons.ts` 新增 `pause/skipBack/skipForward/volume/moon`。
  - 设置页：新增 `views/settings/pages/ReaderAudioPage.vue` 并在 `router` 的 `SETTINGS_PAGE_COMPONENTS` 注册；`settingsNav` 的 `reader/audio` 由 **placeholder → ready**（注意 router 对「ready 但未注册组件」会 `console.error`，两处必须同时改）；偏好同步**三处同步**加 `audio` 块（`prefsPayload` 的 `Payload`/`PAYLOAD_BLOCKS`/`normalizePayload` + `prefSync` 的 `collect`/`applyPayload` + 后端 `PREFS_BLOCKS`），否则推送会 400 或静默丢块。
- **验证**：后端 `py_compile` 全绿；前端 `vue-tsc` 0 错误 + build + deploy；重启 8791 冒烟 —— 音频目录扫描（2 轨 / 封面 / size 汇总）、`/audio` 轨清单、**Range 请求 206 且 Content-Range 正确**、音频封面 200、CBZ 页清单与封面 200、坏 CBR 优雅降级（pages 0 / 封面 404 / `unparsable`）、`/api/books` 出现 `AUDIO`/`CBR` 两种 format、SPA 200 且已服务新 bundle。
- **补充复验（偏好多块同步）**：`PUT /api/prefs/devices/{id}` 带 `audio` 块 → **200 且值原样保留**；同时带一个非法块（`bogus`）→ **400「未知的偏好块」**。证明 `PREFS_BLOCKS`/`PAYLOAD_BLOCKS`/`normalizePayload` + `prefSync.collect/applyPayload` 四处已一致，跨设备同步不会丢 audio 块、也没放松校验。

## 第 10 期（多书库 D8 + C2）· 进行中
- 用户口径：**范围 = D8 + C2**（C1/C3 顺延）；**现有书启动时按格式自动迁移**（真移文件，需 manifest + 回滚）；**book_id 保持 basename 派生、暂不支持跨库同名**（冲突即拒绝/建议改名，**不做数据迁移**）；**功能显隐取全量**（导航 / 工具页标签 / 设置分组 / 仪表盘部件）。
- **关键简化**：既然不做跨库同名，`book_id` **无需带库维度** —— 同时规避了 `_MEDIA_TOKEN_PATHS`（`[^/]+`）被 `/` 击穿的风险（否则封面/漫画页/音频轨会静默 401）。
- **影响面探查已完成**（code-explorer），结论要点：
  - `config.OUTPUT_DIR` 直接引用约 31 处（server 16 / komga_api 7 / library 4 / cli 2 / fileops 1 / watcher 1）+ `server.py` 模块常量 `OUTPUT_DIR`。
  - **唯一会真删数据的路径**：`_orphans()` 的有效 id 集来自 `books()`；若只扫单根，别的库的书会被判孤儿 → `delete_orphans()` 对 `progress/annotations/collection_items/reading_sessions` 执行**不可恢复 DELETE**。**必须先堵**（现已因为 `books()` 合并全部库而天然安全）。
  - `fileops.safe_path` 的基根若换成书库根会允许跨库穿越；若仍用 OUTPUT_DIR 则库内路径全被判非法 → 已改为 `safe_path(name, library_id)`。
  - 前端：`LIBRARIES = []`（`data/collections.ts`）是侧栏「库」组占位；`GET /api/libraries` 当前语义是**格式分面**（`fmt:EPUB`/`issues:1`/`nocover:1`），与「库实体」**命名冲突**，需拆分（建议库实体留 `/api/libraries`，分面挪 `/api/library-facets`）。
- **本期已完成（稳定检查点，已回归验证）**：
  1. `config.py` 新增 `LIBRARY_SOURCE_DIR`（默认 `/app/libraries`）。
  2. `db.py` 新增 **`libraries`**（id/name/type/mode/root_path/storage_path/source_subdir/rules/sort_order/last_scan_*）与 **`library_migrations`**（batch_id/direction/library_id/src/dst/status/error，幂等 + 回滚依据）两表与全部存取（`list/get/create/update/delete_library`、`set_library_scan`、`migration_add/mark/batch/batches/last_batch`）。
  3. `library.py`：新增库注册表（`DEFAULT_LIBRARY_ID='default'`、`_exts_for_type` 按库类型收窄白名单、`default_library()`、`libraries()`、`get_library()`、`ensure_default_library()`、`library_of()`、**`root_of()`**、**`resolve()`**）；`_iter_book_entries(d, exts)` 与 `_dir_signature(d, exts)` 支持按类型收窄；`_scan_once(lib)` 逐库扫描并给每条书目打 **`library_id`/`library_type`**；**缓存改为按库分桶** `_cache = {lib_id: {...}}`，`books(library_id=None)` 合并全库、`invalidate(library_id=None)` 可按库失效；**`by_id` 命中多库时显式报错**（防进度写错书）；`find/book_detail` 支持库参数并用 `root_of(b)` 解析路径。**`name` 语义不变**（相对所属库根）→ BookCard/前端/OPDS/Komga 无需连锁改。
  4. `server.py`：lifespan 加 `library.ensure_default_library()`（**漏掉会让书目为空 → 孤儿判定真删**）；12 处 `config.OUTPUT_DIR / b["name"]` → `library.root_of(b) / b["name"]`；`OUTPUT_DIR` 模块常量加注释降级为「默认库根」；KOReader 索引改为**逐库**（partialMD5 与路径相关，错根会把进度同步到错书）；`ko_libraries` 返回**多库 DTO**、`ko_me.sharedLibraries` 列全库。
  5. `komga_api.py`：6 处取路径改 `library.root_of(b)`；`library_dto(lib=None)` 变为**每库一份**（id/name/root 来自库实体）。
  6. `fileops.py`：`output_dir(library_id=None)` 与 **`safe_path(name, library_id=None)`** 库感知；新增 `_lib_of()`（优先前端回传的 `library_id`，否则 `library.find` 反查）；`apply_rename` / `apply_komga_layout` / `plan_komga_layout` / `recycle_items` 全部按条目所属库解析；`_prune_empty_dirs(library_id=None)`。
  7. `watcher.py`：新增 **`target_root(src)`**（当前返回默认库根，行为不变；预留 `library_rules.decide_for_path` 接缝）；目录分支与复制分支改用 `target_root(p)`。
  8. `cli.py`：新增 `_resolve_out()`（`-o` → `--library <id>` → 默认库根）与 `--library` 参数。
- **本期未完成（待续）**：`core/library_rules.py` 归类规则 + 接入 pipeline/watcher；`core/migrate.py`（按格式迁移 / 冲突拒绝 / manifest 幂等 / 一键回滚）；server 的库 CRUD、来源目录列举、扫描、规则、迁移预览/执行/回滚接口，以及 `/api/libraries` 与分面的**命名拆分**；前端 `LibrariesPage` + 分步新建向导 + 侧栏「库」组接真实数据 + **全量显隐矩阵**（`data/libraryTypes.ts` 为单一真值源）；C2 系列按媒体分组；compose/Dockerfile 的 `LIBRARY_SOURCE_DIR` 挂载与 `.gitignore`。
- **验证**：后端 `py_compile` 全绿；重启 8791 实测 —— `/api/books` 25 本且全部带 `library_id=default`/`library_type=mixed`、各格式（AUDIO/CBR/CBZ/EPUB/PDF）完好、AUDIO 封面与 2 轨音频、CBZ 封面与 2 页、`/api/entities`/`/api/duplicates`/`/api/missing`/`/api/libraries`/`/api/rename/preview` 全部 200（**零回归**）。

## 第 10 期（多书库 D8 + C2）完成
- **用户拍板的四项结构决策**（偏离前需重新确认，已写入 `docs/roadmap-gaps-remaining.md`）：
  1. `/api/libraries` **升级为库实体**；格式分面改址 `/api/library-facets`；侧栏「库」组只列真实书库，**分面不再占侧栏**（能力由书库页的格式筛选承接）。
  2. **库根不设默认、逐库选**：向导里二选一「就地引用 / 独立存储」；库里**只存相对的来源子目录名**，不存来源绝对路径（挂载点换了绝对路径会失效）。
  3. **迁移首次需一次确认**：启动只出预览 + 落 manifest，**阻塞**等点一次「执行迁移」；「暂不迁移」记入 `app_state` 不再打扰；设置可勾「以后自动执行」。
  4. **保留「全部书库」为默认**（不裁剪）；只有选中某库时才按该库类型裁剪导航 / 工具标签 / 设置分组 / 仪表盘。
- **新增后端模块**
  - `core/migrate.py`：只看格式判目标（ebook/comic/audiobook）；**只挪库不改名**（`name` 是库内相对路径 → `book_id` 不变 → 进度/批注不断链）；**manifest 先行**（执行前落 pending、执行后 done/failed）→ 幂等 + 可回滚；同名**拒绝覆盖**并给建议名 `X (2).ext`（只建议不自动改）；`batch_id` 由条目集合**确定性 sha1 派生** → 重复 plan 复用同批次；`preview/plan/execute/rollback/pending_batches/gate_state/dismiss/reset_gate/suggest_specs`。门禁状态存 `app_state`（KV），不放 `config.yaml`（那是用户配置会被设置页覆盖）。
  - `core/library_rules.py`：入库归库优先级 **来源子目录名 > 格式 > 元数据关键词**；`rules` 字段兼容 JSON 与纯文本逗号分隔；**都不可靠时返回 None → 回退默认库**（宁可落默认库也不乱归）；`target_root()` 是摄入侧取目标目录的唯一入口（watcher/上传/convert-path/书源下载/OPDS 下载五处共用）。
  - `core/features.py`：库类型 → 能力矩阵（真值源），只登记**有区分度**的能力（通用项前端不声明需求）；`library_id` 为空 = 「全部书库」= 全部能力（不裁剪）；判据是**现有实现真实支持的范围**（元数据抓取只写 EPUB → 仅 ebook 等）。
- **后端改动**：`db.py` 加 `app_state` KV（`state_get/set/delete`）；`server.py` 新增库实体 CRUD / 扫描 / 来源目录列举、`/api/features`、`/api/library-migrations/*`（preview/plan/apply/rollback/batches/dismiss/reset-gate）；`series/{name}` 增 `groups`（按媒体，C2）；`_SANITIZE`+`GET /api/config` 加 `libraries` 段；5 处摄入落盘点改走 `library_rules.target_root`；Komga `book_dto/series_dto` 的 `libraryId` 改为**真实库 id**（原硬编码 `"novelforge"` 与 `/api/v1/libraries` 对不上）并让 `_ko_read_filter` 按真实库过滤；`config.ensure_dirs` 加 `LIBRARY_SOURCE_DIR`。
- **安全边界（重要）**：库根**只允许落在**「`LIBRARY_SOURCE_DIR` / `OUTPUT_DIR` / `DATA_DIR`」之内 —— 库根就是 `safe_path` 的边界，放任任意路径等于放任改名/回收作用到系统目录。已实测 `/etc` 被 400 拒绝。
- **前端改动**：`api.ts` 新增 `LibraryEntity/LibrariesResult/FeaturesResult/Migration*/LibraryFacet` 等类型与全部方法；`stores/library.ts` 加 `libraryEntities/sourceDir/currentLibraryId(localStorage)/currentLibrary(Name)/scopedBooks/features/hasFeature/loadFeatures/setCurrentLibrary/openLibraryById`，并把**书库页相关筛选（smart/facet/tag/continueReading/scopeCounts/allTags）改为按当前库**（未选库 = 全部）；`AppSidebar` 库组接真实书库（置顶「全部书库」）+ 按能力裁剪条目；`ToolsLayout` 标签按能力裁剪 + 新增「书库管理」；`SettingsSidebar` 用 `PAGE_FEATURE` 表裁剪设置页；`DashboardWidgetRow` 用 `WIDGET_FEATURE` 裁剪部件；新增 `views/tools/LibrariesView.vue`（迁移卡片 + 库列表 + 新建/编辑弹窗 + 当前库能力徽标 + 台账）与 `components/MigrationGateDialog.vue`（启动阻塞确认，挂在 `App.vue`，读不到预览**则不打扰**）；`settingsNav` 的 `libraries` 页 **placeholder → ready** 并链到 `/tools/libraries`；`SeriesDetailView` 按媒体分段（仅多于一组才加组标题）。
- **验证**：后端 `py_compile` 全绿；前端 `vue-tsc` 0 错误 + build + deploy；迁移端到端冒烟（25 本：预览 25 `no_library` → 建 3 库后 24 ready + 1 冲突并给出 `测试漫画 (2).cbz` → plan 幂等复用同批 → 执行 25 移成功 0 失败 → 迁移后再预览归零 → 回滚 25 本原位还原，台账 25 `rolled_back`）；门禁 dismiss/reset 联动 `needs_confirm`；库 CRUD 边界（越界库根 400、非法类型 400、默认库不可删、非空库需 force）；`/api/features` 全部 17 项 vs comic 9 项；`series/{name}.groups` 返回 `[('ebook','电子书库',2)]`。
- **未做**：C1 Requests、C3 SYNOPSIS（顺延）；跨库同名不做数据迁移（冲突即拒绝，`book_id` 仍 basename 派生）；每库独立的 scheduled scan / write-to-file 开关仍为全局策略。

## 第 11 期（工程护栏：自动化测试 + 删除 C1）完成
- **用户决策**：第 11 期主题 =「工程护栏：自动化测试」；范围 =「核心 + TestClient 接口冒烟」（**未选**自检脚本 / CI 选项，故不加 CI）；追加指令原话：**「C1相关功能删除，不再做这个了」**（= 不只是不排期，而是把代码删掉并归档为已决策不做）。
- **新增 `tests/`：107 个用例，完全离线，约 2 秒跑完**。两层：
  - **核心纯逻辑**（`test_migrate.py` / `test_library_rules.py` / `test_features.py` / `test_metastore.py` / `test_fileops_guard.py`）：迁移批次幂等与回滚、同名冲突拒绝 + 建议名递增、归库优先级（来源子目录名 > 格式 > 关键词 > 回退默认库）、能力矩阵边界、override > online > opf 与 `orig` 只写一次、`safe_path` 越界/绝对路径/层级/非法字符/**跨库穿越**负例。
  - **接口冒烟**（`test_api_smoke.py`）：鉴权 401、库根白名单 400（`/etc`、`/tmp`、相对路径）、默认库不可删、非空库需 `force` 且**只移除登记不删文件**、迁移 preview→plan→apply→rollback 全链路、元数据编辑产生覆盖（含 `orig`）并可恢复、非 EPUB 编辑被拒、能力清单按库、系列按媒体分组。
- **两个必须记住的测试前置（否则测试会写真实目录）**：① **环境变量必须在 import 业务模块之前设置** —— `config` 在导入时就用 `os.getenv` 固化各目录，`server.py` 导入阶段即执行 `ensure_dirs()`；② `db` 的 `_conn` / `_db_path` 是**模块级缓存**，切换数据目录必须重置。为此**本期唯一一处业务代码改动**是给 `core/db.py` 加 `close()`（清两个缓存，docstring 注明供测试与切换 DATA_DIR，**不改运行时行为**）。
- **测试隔离设计**：`conftest.py` 顶部建会话级临时根 + 写 env + 补 `sys.path`，**之后**才 import 业务模块；`isolated` 夹具按用例 `monkeypatch` `DATA_DIR/OUTPUT_DIR/LIBRARY_SOURCE_DIR` → `db.close()` → `db.init()` → `ensure_default_library()` → `invalidate()`；`make_library` **强制依赖 `isolated`**（消除「用了它却没隔离」的写法）。`pytest.ini` 用 `pythonpath = .`；`requirements-dev.txt` 只放 dev 依赖（**不进生产镜像**）；`.gitignore` 加 `.pytest_cache/`。
- **踩坑记录（值得记）**：① `pytest` 本机**未装**，但 **pip 可联网**（只有 Docker registry 被墙）→ 可用 `pip install`；② 我最初把测试库根建在 `tmp_path/libs` → 被**库根白名单正确拒绝**（400），说明安全边界真的在工作，测试改用 `LIBRARY_SOURCE_DIR` 之下；③ 库 id 由名称派生：**中文名 slug 为空 → 回退 `lib-<sha1[:8]>`**，断言里不能硬编码 id（API 支持显式传 `id`）。
- **删除 C1**：后端 `REQUEST_SECTIONS` + `GET /api/requests/config`（`server.py` 2693–2766）；前端 `RequestsPage.vue`（整文件）+ `router` import 与注册项 + `settingsNav` 的 `admin/requests` 条目 + `api.ts` 的 `RequestSection`/`RequestsConfig` + `requestsConfig()` + `NetworkPage` 死链文案。**加了一条 404 断言防止半删状态回归**。**保留**（属上游采集/对照记录，不是本项目功能）：`NotificationsPage` 里 `Book requests` 事件列举、`docs/review/*`、`docs/bookorbit-settings-inventory.md`。
- **删除前的穷尽复核（code-explorer）结论**：唯一真实死链是 `NetworkPage.vue:67`（删路由后不会 404 而是落通配占位页）；**最容易漏的是已部署产物** `novelforge/static/v2`（不 rebuild+deploy 就会「页面能打开、接口 404」）；`router/index.ts` 有「`ready` 但未注册组件」的 `console.error` 检查 → **注册表与组件必须同批删**；删后 `store` 仍有 10 处调用、无孤儿 import。
- **验证证据**：`py_compile` 全绿；`pytest` **107 passed** 且**连跑两次一致**、**反向文件顺序也通过**（证明无顺序依赖）；`vue-tsc` 0 错误 + `build` + `deploy`；重启 8791 实测 `health=200 / spa=200 / /api/requests/config=404`；新产物 `admin/requests` **0 处**；`git status` 只有预期文件（`.pytest_cache/` 已被忽略）。
- **未验证项（如实披露）**：**未做浏览器真实驱动验证** —— 本机**未安装 playwright-cli 与浏览器**（需 ~200MB 下载）。替代证据：前端源码里 `admin/requests|RequestsPage|requestsConfig|RequestsConfig|RequestSection|/api/requests` **零残留**、已部署产物 `admin/requests` 零命中、`vue-tsc`（会编译 SFC 模板）0 错误、侧栏「库/设置」条目完全由注册表驱动（删条目即不可渲染）。待装好 Playwright 后应补跑一次界面冒烟。
- **文档与实况对齐**：路线图 C1 归档「已决策不做」并指出**旧理由「依赖多库」在多库落地后已失效**；补勾第 8 期遗留未勾的 **D1–D5**（**D4 的「系列级元数据」明确标注仍缺、未随本期关闭**）；`capability-gap.md` 修正 **6 处**过期记载（Requests 三处 + 多库两处 + 有声书/Pages）；`roadmap-verification.md` 的历史数字**不改写**（那是当时快照）而是加注说明；README 48 → 47 并新增「自动化测试」小节；`router/index.ts` 与 `settingsNav.ts` 的页面计数统一为 **47**（后端路由现值 **199**）。

## 第 12 期（系列级元数据 = C3 SYNOPSIS + D4 遗留）计划已定
- 计划名 `phase12-series-metadata`（状态 ready，待用户确认执行）。**这是路线图最后一项**。
- **用户逐条拍板（实施不得偏离）**：
  1. 主题 = 系列级元数据；深度取最全档（系列简介 + 系列级字段 + 本地覆盖 + **系列页可编辑/覆盖保护/恢复在线**）。
  2. **存储取「方案 A：纯服务器侧、绝不写回 EPUB」** —— 用户先追问「存 NAS 里其他设备读是否受影响」，得到分析后选定。**原「写回各册 OPF（题材合并/出版社仅补空）」方案已作废，不得实施**。生效点 = **三处注入**：`GET /api/series/{name}`、`komga_api.series_dto`（其 `metadata.summary` 现在是空串）、OPDS 系列入口。
  3. 「重排册号」**只改 OPF `calibre:series_index`、不动文件名** → `book_id` 不变 → 进度/批注/评分/收藏不断链。
  4. 另加「抓取全部系列」（逐个抓、失败不中断、带回统计）。
  5. **Komga 侧不做调整**（用户先答「一并支持写回 PATCH `/api/v1/series/{id}/metadata`」，随后**补充指令推翻**）→ 不新增任何 Komga 写接口；读侧 `series_dto` 注入保留（是方案 A 生效点之一）。
- **方案 A 的语义边界（用户已知并接受，勿偷偷加写回）**：其他设备**连服务读**（Komga 客户端 / OPDS / 浏览器）→ 全部字段可见，**含系列简介**；其他设备**直读 NAS 文件**（SMB→Calibre、Kobo 原生浏览）→ 系列名与册号不受影响（`calibre:series`/`series_index` 本就在 OPF 里），但系列简介/出版社/首发年/总册数**看不到**；其中只有「出版社」是写回能救回的、「**系列简介任何方案都救不回**」（OPF 无此字段，写 `dc:description` = 覆盖单册简介）。
- **在线来源的诚实性约束**：OpenLibrary **没有系列实体**（不像 `authors.py` 有 `/search/authors.json`）→ 只能「用系列名检索 + 用成员书书名/作者做一致性打分」；**聚合字段（总册数/首发年/出版社/题材）优先本地聚合（零猜测），在线值仅补空**；低于阈值如实返回「未找到」+ 来源 + 置信度。
- **两条必须带去实现的红线**：① 第 8 期红线仍成立 —— `library.books()` 热路径**不查 DB**，系列元数据合并**只能在接口层**；② `server.py:3427` 的既有警告 —— 外呼「全库一次跑必然超时」，故「抓取全部系列」必须逐项/限流/进度反馈。
- **测试注意**：元数据写回 / **系列解析必须用真 EPUB**（`epub_builder.build_epub`），假 `b"EPUB"` 只够扫描类用例（否则接口 500）；本机无外网，系列抓取接口用例**只断言「优雅失败 / 不抛」**。

## 第 12 期（系列级元数据 = C3 SYNOPSIS + D4 遗留）完成
- **交付**：`core/series_meta.py`（新）+ `db.series_meta` 表 + `metasources.search_series/score_against_members`（新）+ 6 个系列接口 + 三处注入（`GET /api/series/{name}`、`komga_api.series_dto`、OPDS 列表 `<summary>` / 系列内 `<subtitle>`）+ 前端 `SeriesMetaPanel.vue`、`SeriesRenumberDialog.vue`。**路线图最后一项已关**：C3 勾掉、D4「系列级元数据仍缺」标记为已完结；`capability-gap.md` 的 `/series/:id` 行更新为「已完整」；README 增「系列级元数据」「重排册号」两条并**修正一处过期表述**（「数据全部来自 output/、没有额外的库实体」→ 第 10 期起书库已是数据记录）。
- **字段分层（用户拍板的顺序，勿改）**：**本地覆盖 > 本地聚合 > 在线补空**。册数 / 首发年 / 出版社 / 题材优先取**成员书 OPF 聚合的事实**（出版社取出现最多的那个、首发年取最早、题材取保序并集），在线值只在算不出时补位；**系列简介只能来自在线**（本地无从推断）。
- **两个「册数」不可混用**：`owned_count` = 库里实际拥有（必来自聚合）；`declared_count` = 外部声明的总数（当前恒 0 = 未知，两个源都不提供）。界面分开显示。
- **在线可靠性的诚实降级**：外部源**没有系列实体**（`metasources._OL_FIELDS` 里没有系列字段、也无系列详情接口），只能「用系列名检索 + 用**成员书书名/作者**打分（复用 `score_candidate` 的书名 0.7 / 作者 0.3 口径）」；一致性分 < `MIN_MATCH = 0.6` 即**如实回「未找到」且不写库**，并把 `source` / `score` 交给界面展示置信度。
- **`series_dto` 的性能坑（已按 code-explorer 复核避开）**：列表端点会对**每个**系列调一次 `series_dto`，若在函数内调 `series_meta.effective()` 就会 N 次全库聚合 → 故新增 **`effective_light()`**（只做「本地覆盖 > 在线」、**不做聚合**、**不提供 owned_count**，因为它必须来自聚合不能瞎猜），列表端点配 `db.all_series_meta()` 一次取全。
- **顺带修掉一处既有不确定性**：`series_dto` 的 `genres` 原来是 `set` 去重 → 同一份数据每次运行顺序都可能不同（字符串哈希随机化）；改成**保序去重**。
- **OPDS 改动的安全边界**：`_feed` 加 **keyword-only** `subtitle`（既有 4 个调用点全用位置参数，keyword-only 不可能被顶掉）、`group_navigation` 加 keyword-only `descriptions`、`acquisition_feed` 的 keyword-only 组末尾加 `subtitle`；**不传时输出逐字节不变**。`group_navigation` 被作者 / 系列 / 标签三页共用，故简介**只能由调用方显式传入** —— 内部自己查库会顺带改变作者页与标签页的输出（已实测作者页无 `<summary>`）。
- **重排册号的不变量（有测试钉住）**：只改 OPF 的 `calibre:series_index`、**不动文件名** → `book_id` 不变 → 进度/批注/评分/收藏不断链；默认按当前序号升序、缺序号的排最后；**幂等**（已排好再排 changing=0）；**可完整回滚**（返回每条 `old_index`，含「原本没序号 → 赋空串清除」）；会员校验（非本系列成员一律跳过）+ 走 `safe_path(name, library_id)`；`RLock` 串行化；写后 `library.invalidate(lid)` 再**回读 OPF 确认**（`verified` / `mismatched`）。
- **踩坑记录**：① `ebooklib.write_epub` 在**父目录不存在时只 warn 不抛**（新版本才改）→ 文件静默没生成，后续 `patch_epub_meta` 报「EPUB 元数据写入失败」；**造真 EPUB 前必须先 mkdir 输出目录**（本机 smoke 与测试都要注意，`conftest` 的 `default_root` 已由夹具建好）。② 用 playwright 直连时 **`localStorage` 注入必须在页面脚本之前**（`add_init_script`）：首屏请求若先拿到 401，api 客户端会清 `nf_token` 并弹登录门禁，注入就白做。③ 本机 playwright 的 **1.63 自带内核未下载**，但缓存里有 `chromium_headless_shell-1223` → `launch(executable_path=...)` 复用即可（`pip install playwright` 装在项目 `.venv`，不进生产依赖）。
- **验证证据**：`py_compile` 全绿；`pytest` **136 passed**（原 107 → 新增 29：`tests/test_series_meta.py` 19 个 + `test_api_smoke.py` 10 个），**连跑两遍一致**、仍约 2.4 秒且完全离线；真实 HTTP 端到端（列表/详情/编辑/恢复/抓取优雅失败/批量/Komga `metadata.summary` 与 `booksMetadata.summary` 注入/OPDS `<summary>`+`<subtitle>`/作者页未被注入/重排+回滚后磁盘文件名不变）；**playwright 真实驱动界面 6 项全过**（截图在 `/tmp/nf-ui-*.png`：列表摘要、详情简介与字段行、就地编辑出「已本地修改」+「恢复在线」、恢复后回落空态诚实文案、重排对话框预览 3 行且未改动时确认按钮禁用、390px 窄屏不破版）；前端 `type-check` 0 错误 + `build` + `deploy`。
- **已知限制 / 未修复（如实披露）**：① 系列简介 / 出版社 / 首发年 **只存 DB** —— 直读文件（SMB→Calibre）看不到，这是用户拍板的取舍；② `declared_count` 当前恒为 0（两个源都不提供系列总册数），界面按「有值才显示」处理；③ 控制台有一条**既有**告警 `设置页 "libraries" 标记为 ready 但未注册组件`（`settingsNav.ts:232` 的 `p('libraries', ...)` 只指向 `/tools/libraries`，无独立组件；非本期引入，未改）；④ 代码审查脚本报的 15 条「空指针风险」经核对**全为误报**（`pubs`/`local`/`tags_local` 等均为带默认值的本地变量），唯一有效建议是 `renumber_apply` 偏长（65 行）—— 因其为「单循环 + 失效缓存 + 回读校验 + 记日志」的高内聚流程，拆分会增加间接层，故保留并在 docstring 里写明约束。

## 第 13 期（多书库收尾：每库覆盖 + 跨库同名防护 + 工具页库维度）完成
- **交付（三块）**：① **每库覆盖** —— `core/lib_settings.py`（新）+ `libraries.settings` 稀疏 JSON + `features.SETTING_CAPS`（能力矩阵联动：库类型没这能力 → 覆写项不返回也不生效），生效值 = 每库覆写 ?? 全局值；`pipeline`/`watcher`/`metafetch`/`series_meta` 全改成「按目标库取」；② **跨库同名防护** —— `library_rules.guard_conflict`（**同库同名放行**、跨库抛 `IngestConflict` 带建议名与撞上的那本）+ `library.id_conflicts`/`id_conflict_with`/`suggest_name` + `fileops.apply_conflict_rename`（**改名必换 `book_id` → 必须 `db.remap_book_id` 搬关联数据，否则进度/批注清零**，这正是用户点这个按钮的反面）；③ **工具页库维度** —— 实体管理/批量重命名/重复书籍/缺失资源加「范围：当前库 / 全部书库」（空串 = 全部 = 零行为变化），全部书库时按库分组并标「跨库重复」。
- **前端新增 3 个组件**：`components/tools/LibrarySettingsPanel.vue`（每项同时显示生效值与「继承 / 已覆盖」）、`LibraryConflictPanel.vue`（冲突清单 + 一键改名，建议名由后端给 → 口径只有一处）、`LibraryScopeSwitch.vue` + `composables/useLibraryNames.ts`。
- **本次收尾（补做）**：`tests/test_library_conflicts.py`（8 个用例）；前端 `type-check` 0 错误 + `build` + `deploy`（**第 13 期组件此前没部署过**，产物时间戳 18:29 早于源码 19:18）；`LibraryConflictPanel.vue` 模板里一处 markdown `**` 残留（界面上会显示字面星号）删掉。
- **验证证据**：`pytest` **171 passed**（原 163 + 新增 8；完全离线、约 2.4s；`pytest.ini` 的 `-q` 不出汇总行，靠点数 72+72+27 核）；重启 8791 实测 `GET /api/libraries/{lid}/settings` 结构正确（`values`/`global`/`overridden`/`overrides`）、`PUT` 覆盖后**全局值不变**、坏值 400、`DELETE ?keys=output.layout` 只清该键（`metadata_fetch.fields` 覆写留着）、`DELETE` 整体恢复继承后 `values == global`、`library_id` 过滤生效（同一实体 `total` 2 → 1）、不存在的库 → 404、`/api/library-conflicts` 结构完整（真实数据 4 库 18 书，total=0）；`git log` 4 个 commit：`feat(core)` / `feat(server)` / `feat(frontend)` / `test(library)`。
- **写测试时的两处设计纠正（值得记）**：① 「同库同名放行」**不能**用「两库都有同名时站任一库看」来表达 —— 两库都有同名本身就是真冲突；正确构造是「该 basename **只在目标库**存在」；② `id_conflict_with(name, lib_id)` 的判据是「**别的**库」，所以「跨库拦截」用例要让同名书**只存在于另一个库**（mixed 库收 `.cbz` 可用来把同名塞进「非目标库」）。
- **未做（如实披露）**：本期**未做浏览器真实驱动验证**（playwright 装在项目 `.venv`、可复用缓存内核，脚本写法见第 12 期记录），只做到「type-check + build + deploy + 接口级冒烟」。

## 第 13 期收尾（补做 UI 冒烟 + 文案修复 + 提交）
- **补做浏览器真实驱动冒烟（playwright，8 项全过）**：四个工具页的「范围：当前库 / 全部书库」控件、每库设置面板（含「继承全局」提示）、设置页书库页、范围下拉真实选库，全部可渲染可操作；`未注册组件` 告警消失。视图截图见 `/tmp/nf-ui13-*.png`。
- **修 22 处 markdown `**` 残留**：Vue 模板不渲染 markdown，`**xxx**` 会原样显示星号。静态模板文本 → `<strong>` 保留强调；属性值 / 数组元素里的字符串经插值输出 → 直接去掉星号。涉及 `MigrationGateDialog.vue`、`LibrarySettingsPanel.vue`、`settingsNav.ts`、`settings/pages/*`（Integration/Komga/Metadata/Pdf/PreferenceSync/ReaderAudio）、`LibrariesView.vue`、`OpdsSourcesView.vue`、`LibraryConflictPanel.vue`。
  - 踩坑：`replace_in_file` 逐条替换**全部失败**（行内容/行号与预期不符、部分是 JS 字符串）→ 改用 Python 脚本按行号 `re.sub` 精确替换，并带「命中数为 0 即中止」保护。
- **`settingsNav.ts` 的 `libraries` 页 status 由 `ready` 改回 `placeholder`**：该页本就是「上游结构对照 + 跳 `/tools/libraries`」，无独立组件 → 路由初始化会 `console.error`。note 同步到第 13 期现状。
- **提交**：`f07012e fix(frontend): 修界面文案的 markdown 星号残留 + 设置页书库状态误标`，已 `git push origin main`，工作区干净。
- **playwright 复用内核的坑**：`chrome-headless-shell` 的 glob 目录名是 `chrome-headless-shell-mac-arm64/`（不是 `chrome-mac/`）；第 10 期迁移确认弹窗（`div.fixed.inset-0 z-[70]`）会挡住点击，脚本需先 `force=True` 点「暂不迁移」。
- **残留告警（既有、非本期引入、可接受）**：`GET /api/prefs/devices/<id>` → 404「设备未登记」。

## 第 14 期（OPDS 按库暴露）
- **主题来源**：梳理「未完成功能」后，用户在四个候选（OPDS 按库暴露 / 元数据抓取扩到非 EPUB / 外部服务同步 / Komga 接入侧）里选了成本最低、且能把第 10 期多库闭环到对外接口的这一个；并确认把四处小尾巴并入本期。
- **实现**：`features` 加能力键 `opds`（入 `_COMMON`）+ `SETTING_CAPS["opds.expose"]`；`lib_settings.ITEMS` 加「对 OPDS 暴露」（bool）；`config.DEFAULTS["opds"]["expose"]=True`。`opds.py` 给 `navigation_feed` / `group_navigation` / `acquisition_feed` / `book_entry` / `book_feed` 加 keyword-only `prefix`（默认 `/opds`，输出逐字节不变）并新增 `library_navigation`。`server.py` 新增 14 条 `/opds/lib/{lid}/*` 路由 + `_opds_visible_libraries` / `_opds_lib` / `_opds_prefix` / `_opds_fid` / `_opds_book_in` 五个辅助；`EDITABLE["opds"]` 加 `expose`（可读侧 `/api/config` 是整块回传，自动带上）。
- **三处易错（都踩过或差点踩）**：① `acquisition_feed` 的 self/next/prev/up 必须全跟 prefix，漏一处就跳回全局；② 单库取书不能用 `library.by_id`（跨库同名抛 `BookIdConflict`）；③ 可见性「不可见与不存在一律 404」。
- **测试**：新增 `tests/test_opds_library.py`（10 例）。能力集 +1 `opds` → 同步改 `test_features.COMMON` 与 test_api_smoke「能力清单 17 → 18」。全量 **181 passed**（171 + 10），无回归。
- **验证**：`py_compile` 全绿；`type-check` 0 错误 → `build` → `deploy`（本工作区无 node_modules，先 `npm install`，8s 装完）；8791 实例端到端 —— 全局 6 个地址全部 200 且**不含 `/opds/lib/`**、根 feed「按书库」仅在两库时出现、库导航 2 条、单库整套 200、越库 book/download 404、本库 book/download 200、关掉暴露后直连 404 且不进导航、恢复后 200、无凭据 / 错密码均 401。
- **本工作区环境差异（与 macOS 不同）**：无 `.venv`，用 `python3`（3.14，依赖已装在用户 site-packages）；`pytest` 需先 `python3 -m pip install pytest`；**`novelforge/static/` 目录必须存在**（`server.py` import 时 `StaticFiles` 挂载），缺失会让 conftest 直接 ImportError。
- **四处小尾巴**：Watcher note（auto_fetch 已实现且按阈值自动定稿）、MEMORY 部件数（12/12）、`koreader` 双 id（上游对照改 path `koreader-upstream`）、`/placeholder/_authors` → `/authors`。
- **协作约定（用户 2026-09-18 明确）**：写计划**只写**需求来源 / 功能范围 / 防回归要点 / 任务清单，**不要**输出架构设计、目录结构、关键代码结构。

## 第 15 期（Komga 兼容服务端补齐）
- **主题纠正**：用户要的「为其他设备提供 Komga 订阅源」= 第 5 期的 `/api/v1/*` 兼容服务端（**客户端直连**），不是 OPDS。第 14 期 OPDS 按库暴露已完成但**不单独提交**，与本期一起提交（用户选择）。
- **五处补齐**：① `POST /series/list` 的 `libraryId` 真生效（此前 payload 整个被丢掉）+ `GET /series`、`GET /books` 加 `library_id` 查询参数（不传 = 全部可见库 = 与之前一致）；② 有声书库不进 Komga（复用 `features` 的 `komga` 能力，**不新增配置项**）；③ CBR 补 `MEDIA_TYPES` / `_PROFILES`（页面流早支持，缺的只是元数据）；④ 系列级已读（官方 `POST` / `DELETE /series/{id}/read-progress` → 204，无 body）；⑤ 书级补官方新口径 `PATCH`（老客户端 `PUT` 保留）。
- **两个必须记住的协议细节**：① 官方书级进度是 **PATCH**（老口径 PUT 也要留）；系列级是 **POST（已读）/ DELETE（未读）**，**无 body** —— 仓库里此前没有任何记载，是查官方 OpenAPI 页面确认的，不要凭印象写；② **标已读必须保留原 locator、只顶 percent**（与书级 `completed:true` 同口径），否则「标已读」等于「回到第一页」。
- **已知取舍**：系列 id 由**名字**派生（客户端已存进度 / 收藏），同名系列跨库时按库过滤只出现在第一本所在库 —— 写进 `grouped()` 注释与文档，不为此改派生方式。
- **测试**：新增 `tests/test_komga_library.py`（7 例），全量 **188 passed**（181 + 7），无回归。
- **验证**：`py_compile` 全绿；`type-check` 0 错误 → `build` → `deploy`；8791 端到端 —— `/api/v1/libraries` 只有 default 与 comics（有声书库 `lib-02772f55` 被排除）、默认 3 系列 / 3 书、按库过滤 comics=2 / default=1、CBR 拿到 `application/vnd.comicbook-rar` + `DIVINA`、系列级 POST/DELETE 均 204 且 booksReadCount 正确翻转、PATCH 与 PUT 均 204、无凭据 401 / X-API-Key 200。
- **MEMORY 的坑（第二次遇到）**：`replace_in_file` 对 `MEMORY.md` 匹配失败**不是因为 CRLF**（实际是 LF 行尾），而是我对文本记忆不准 —— 交付索引是**单行汇总**（`**13 期（已完结）** …`），不是我以为的列表项。教训：改记忆前先 `grep -n` 确认真实文本，批量改用 Python 脚本 +「命中数必须为 1」保护。

## 第 16 期（Komga 客户端兼容收尾 + 删除 OPDS 订阅）
- **方向拍板**：用户明确「本项目就是 Komga 服务端，无需从其他 Komga 项目获取数据」→ **接入侧永久不做**；并据此把**已存在的「工具 → OPDS 订阅」（订阅外部 OPDS 源）也删除**（后端 6 路由 + 注释段共 107 行、`core/opds_client.py` 240 行、db 5 个 CRUD、`features.opds_sources` 能力键、前端 `OpdsSourcesView.vue` + ToolsLayout 标签 + router + `lib/api.ts`、README 四处、两个「订阅源」文案）。对外两条通道（`/opds` 目录 与 `/api/v1/*` 冒充 Komga）**都保留** —— 协议与客户端群体不同，不重复。
- **Collections 映射收藏夹（可写）**：Komga 的 Collection 装**系列**、本项目收藏夹装 **book_id** → 按书归到系列再给出。规格查自 komga.org OpenAPI 与 deepwiki：`GET/POST /collections`、`GET/PATCH/DELETE /collections/{id}`、`GET/PUT /collections/{id}/series`、`DELETE .../series/{sid}`、`GET thumbnail`；`POST|PUT|DELETE thumbnail` 一律 **403**（夹封面取成员书封面）。db 新增 `update_collection` / `clear_collection`（**此前没有重命名入口**）。
- **两个易错点**：① 收藏夹是**全局**的（能装有声书库的书）→ Komga 侧必须再过 `_ko_books()` 可见性过滤，否则客户端拿到打不开的坏条目；② `collections.name` 是 UNIQUE，重名靠撞库判 **409**，不预查（`collection_items` 的 UNIQUE + `INSERT OR IGNORE` 负责幂等）。
- **没有的概念诚实为空**：Readlist 返回空分页 + 写操作 403；`POST /series/{id}/analyze` 是 **204 空实现**（书目实时扫描，没有「重新分析」这一步，但不假装做了）。
- **OSDD**：新增 `/opds/search/description` 与单库版，根 feed 的 `rel="search"` 改指向它、type 改 `application/opensearchdescription+xml`（此前放的是 acquisition feed 类型，客户端多半识别不出搜索框）。
- **测试**：新增 `tests/test_komga_client.py`（12 例）；能力集删掉 `opds_sources` → `test_features` 两处 + 「能力清单 18 → 17」同步。全量 **200 passed**（188 + 12），无回归。
- **验证**：`type-check` 0 错误 → `build` → `deploy`；8791 端到端 —— Collections 全流程 200/204/403/404 正确、应用内 `/api/collections` 同步可见（count=1，证明写的是真实收藏夹）、Readlists 0 与 403、Referential 九个字段齐全、单库详情与不存在 404、previous/next 单本系列 404、analyze 204/404、OSDD 200 且根 feed 含 OSDD、`/api/opds/sources` 404、前端产物里已无 `tools-opds-sources`。
- **删除用的办法（值得复用）**：大段删除用 Python 脚本按行号 + **首尾断言**（首行文本、尾行之后一行含下一区块标记、段内含关键字），断言不过就不删。`api.ts` 删了两次：第一次只删掉 `OpdsSource`（按「第一个顶层 `}`」定位），第二次按「第二个顶层 `}`」才删净 `OpdsEntry` + `OpdsFeed` —— 删除后必须 `grep` 复查残留。

## 按 `docker-compose.test.yml` 运行（Linux codespace）
- 环境：**Linux + Docker 29.7.2（daemon 可用）**，非 macOS（之前日记里「本机无外网 / 无法 build」的约束在**本环境不成立**）。容器内 `/app/novelforge` 挂载的是 `/workspaces/novel_dl_convert/novelforge`。
- 现状：容器 `novelforge-test`（镜像 `novelforge:test`，compose 文件 = `docker-compose.test.yml`）**已在运行且 healthy**，端口 `8993:8000`。无需重复 `up --build`。
- 判断「是否已用最新代码」的做法：`stat` 对比 容器 `State.StartedAt` vs 各源码 mtime vs `novelforge/static/v2/index.html` mtime。本次容器启动 16:36:17 > Python 最晚改动 16:28:13（后端已加载最新），产物 16:39:48 > 前端最晚改动 16:38:46（**结论：无需 restart / 无需 rebuild**）。这套时间戳比对很好用，以后先比再动。
- 冒烟全部通过：`/health` 200、`/` 200（SPA）、`index-*.js/.css` 200 + woff2 200；`POST /api/auth/login`（admin/changeme）拿 token → `Bearer` 头部可用（不必非走 cookie）→ `/api/libraries` 返回默认库（root `/app/output`，mixed/inplace）、`/api/auth/me`、`/api/library-facets` 200。日志显示 `/api/books`、`/api/features`、`/api/config`、`/api/stats`、`/api/annotations`、`/api/prefs/profiles` 全部 200。
- ⚠️ `/opds` 返回 **404 属预期**（`_opds_guard`：`config.opds.enabled` 未开 → 404），不是接口坏了。
- ⚠️ 测试版没挂 `LIBRARY_SOURCE_DIR`（`/app/libraries` 在容器层），真实版 compose 才挂 `./libraries`；新建书库若要就地引用，测试版需补挂 `./libraries:/app/libraries` + 环境变量 `LIBRARY_SOURCE_DIR=/app/libraries`。
