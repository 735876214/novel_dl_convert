# 2026-09-19

## 第 17 期 T2：逐库扫描调度（已完成）
- **落地**：`db.py` 给 `libraries` 表新增 `watch`(默认1)/`scan_interval`(默认0)/`scan_cron`(默认'') 三列（CREATE + 老库 `PRAGMA` 检查后 `ALTER` 幂等），并同步 `_LIBRARY_COLS`、`create_library`、`update_library`、`server` 的建/改库接口与 `_library_dto`。`requirements.txt` 加 `croniter>=2.0`。
- **watcher 重构为多目标调度器**（`core/watcher.py`）：全局 `INPUT_DIR` 目标（`library_id=None`，沿用全局 enabled+interval）行为不变；每个库的 `LIBRARY_SOURCE_DIR/<source_subdir>` 作独立目标，带该库 watch/interval/cron。`_loop` 短间隔 tick，按「watch 开关 + interval 到期 + cron 窗口」判定是否扫；坏 cron 退化为 interval（不崩）。state 键按根分桶；每库目标扫完 `db.set_library_scan` 回写 LAST SCAN。`scan_once()` 仍只扫全局 INPUT_DIR（向后兼容 `/api/scan`）。新增 `scan_library_now(lid)` 供单库扫描即时触发。`status()` 返回 `targets`。
- **关键约束**：`watch`/`scan_interval`/`scan_cron` 是库**实体列**（非 `settings` 覆盖项，不进 `SETTING_CAPS`）。`handle_file` 增加 `owner_lib` 参数——来源子目录扫描直归该库（跳过子目录名路由），就地库（来源即存储）跳过自复制。
- **验证**：新增 `tests/test_watcher_perlibrary.py`（6 用例：关库不扫/开库被扫+LAST SCAN/全局不变/cron窗口+坏cron退化/派生目标watch标志/派生interval+cron）。完整 pytest 全绿（~171）。`py_compile` + lint 干净。
- **待续**：T3 每库元数据写回 EPUB（接线 `metadata_fetch.enabled` 覆盖）；T4 前端新增/编辑书库流程对齐上游（CONTENTS/AUTOMATION/LAST SCAN 三页签，用户手动建库）；T5 文档同步。
- 注：`server.py` 当前实际实现与早期摘要不同（POST/PUT 用 `_library_root_allowed`/`_new_library_id`/`_norm_rules` + `raise HTTPException`），改动已基于实际代码对齐。

## 第 17 期 T3：元数据只存服务端、不重写 EPUB（已完成）
- **需求升级（用户两次确认）**：① 所有「写回元数据」改为**只存服务器 DB、绝不改写 EPUB 文件**（手动编辑 / revert / 入库自动抓取 / 手动 apply 全部）；② **列表/卡片也要反映服务端元数据（全一致）**，即 `library.books()` 也要按 `override>online>opf` 合并。
- **架构结论**：详情/编辑页早已由 `core/metastore.py` 按 `override>online>opf` 合并（**详情页无需改**）；缺口在批量热路径 `library.books()` 只读 OPF。故本轮 = 去掉文件写盘 + 批量列表也合并 + 封面服务端化。
- **落地**：
  1. `db.py` 新增表 `meta_cover(book_id PK, data BLOB, media_type)`（CREATE IF NOT EXISTS 幂等，老库自动补）；新增 `set_cover/get_cover/cover_ids` 与批量 `get_effective_meta(bids)`（一次 `IN` 分片查询同时取 override+online，合并优先级并映射 `date→year`、`tags→list`；新增 `_ids_in`/`_parse_tags` 辅助，imports 加 `ast`/`re`）。
  2. `library._scan_once` 末尾**一次批量**合并 `db.get_effective_meta(bids)` + `db.cover_ids(bids)`：服务端值覆盖书对象字段；服务端封面并入 `has_cover` 并移除 `no-cover` issue。靠 5s 扫描缓存摊销，禁逐书查库。
  3. `metafetch.apply` 去掉 `rewrite_epub`/`patch_opf_meta`/`set_epub_cover`/自定义字段 OPF 写入，改为 `db.set_online` + `db.set_cover`（封面下载失败不阻断，`_download_cover` 仍可 mock）。
  4. `server.py`：`api_set_book_metadata` 去 `patch_epub_meta`（`written` 恒 `[]`，实际生效看 `changed`）；`api_revert_book_metadata` 改为只撤 `meta_override`（**不再外呼 online_candidate、不写文件**，`recovered` 改为「实际有覆盖的字段」）；`api_book_cover` EPUB 分支**先取 `db.get_cover` 服务端封面**，无则回退 EPUB 内嵌图（兼容原文件自带封面）。OPDS 封面复用同接口自动惠及。
  5. 每库开关：`lib_settings.ITEMS` 加 `metadata_fetch.auto_on_import`(bool)，`features.SETTING_CAPS` 登记 `→metadata`（漫画库不暴露）。watcher 判定逻辑不变，`config_for` 自动生效。
- **保留**：`fileops` 的 `patch_epub_meta/rewrite_epub/set_epub_cover/METADATA_FIELDS` 定义**不删**（结构性重排 `series_meta.renumber_apply`/`apply_rename` 仍写 EPUB，属另范畴，本轮未动，收尾待用户确认）。
- **测试**：新增 `tests/test_metadata_server_side.py`（10 用例：effective_meta 优先级/映射/空值；cover_ids；列表合并元数据+封面；手动编辑/revert 不改文件字节且 DB 有值；封面接口服务端优先+EPUB 回退；apply 只写 DB；每库 auto_on_import 独立于全局；漫画库不暴露）。全量 **223 passed**，2 warnings，exitCode 0；lint 干净。
- **文档同步**：`README.md`（元数据/抓取/单书详情三处改为「服务端存储、不改写 EPUB」+ 每库自动抓取开关）、`metafetch.py`/`metastore.py` 模块 docstring、`library.export_rows` 过时注释。
- **待续**：T4 前端建库三页签；T5 文档；以及是否把 `series_meta`/`apply_rename` 的结构性重排也去文件写（需用户确认）。
- 注意：`library.py` 实际**已** `import db`（旧注释「刻意不 import db」是错的，已修正）；`db` 顶层只 import `config`，无循环导入。

## 第 18 期：刮削出版（硬链接副本 + 转换日志内「刮削」子标签）—— 已完成
- **需求（用户原话要点）**：对扫描到的书补一道「扫描 → 刮元数据 → 在**新文件夹硬链接**出一个新文件」；在「转换日志」里加专门看刮削进展/结果/失败手动整理的功能。目的：① 不改原有书籍信息 ② 外部阅读器能看到整理完成的书 ③ 失败/未刮的可及时更正。
- **四项确认**：① 成品目录在**新增/编辑书库时手动选**，仅对该库生效；② **自动+手动**（默认扫描入库自动排队，可关）；③ 手动整理 = **写服务端 DB + 自动重建副本**；④ 命名 = 命名规则 + Komga 系列布局，**支持按库覆写**（复用第 13 期 `naming.pattern/scope`）。
- **补充需求**：「若删除硬连接，在日志中增加『硬连接文件已删除，是否删除原文件』的提示，由我空闲时确认」→ 副本被删**只标记待确认**，绝不自动删源、绝不自动重建；对称加了「源不在而副本在 → 孤本」。
- **数据层**：`libraries` 新增 `publish_path`（CREATE + 老库 ALTER + `_LIBRARY_COLS` + `create_library` 参数 + `_library_dto` + create/update 端点）；新表 **`scrape_items`**（book_id PK / library_id / source_rel / status / link_rel / link_mode / link_shared / src_size / src_mtime / embedded / has_cover / error / attempts / **force_fetch** / removed_at / removed_path / confirmed_at / updated_at），配 `scrape_set/get/list/counts/pending/reset_running/delete/delete_by_library/scrape_books_in`。状态机 `SCRAPE_STATUSES`：pending/running/ok/failed/skipped/removed/kept/orphan/**source_removed**，**只许降级**（回到 ok 必须用户显式动作）。
- **新增两个 core 模块（分工刻意拆开）**：
  - **`core/publish.py`**（只管文件）：`publish_dir/fill_pattern/relpath_for/link_or_copy/same_file/recycle/source_sig/embed_meta/publish`。硬链接优先、`os.link` 失败（EXDEV/EPERM/…）回退 `copy2` 并记 `link_mode`；写元数据走 `fileops.rewrite_epub` 的「临时文件 + replace」；旧落点/冲突副本一律 move 进 `CACHE_DIR/recycle`。
  - **`core/scrape.py`**（只管状态与调度）：`enqueue/enqueue_library/process/verify/resolve/run_once/start/stop/worker_state`。单线程 daemon worker（有活连续跑、空闲 5s 一查、按 `scrape.verify_interval` 周期校验），启动时 `scrape_reset_running()` 续跑；`resolve` 支持 delete_source/keep_source/rebuild/keep_copy/recycle_copy 五个显式动作。
- **两条命门设计（有测试兜底）**：① **硬链接副本禁止原地写** —— 共享 inode，`open(dst,'wb')` 会连源一起改坏，必须走原子替换；② **只写「与 OPF 原值不同」的字段**（tags 归一化后比较），一致的不写 → 无元数据可写的副本保持**纯硬链接、零额外占盘**；因此 `link_shared` 字段如实标注「内嵌过元数据的副本已变独立文件」。
- **配置/能力**：`config.DEFAULTS["scrape"] = {enabled: true, max_attempts: 2, verify_interval: 120}`；`lib_settings.ITEMS` 加 `scrape.enabled`（每库可覆写）；`features.SETTING_CAPS["scrape.enabled"] = "komga"`（电子书/漫画库可见，有声书库不出现）。`activity_log.ACTION_SCRAPE = "刮削"`（审计页 ACTIONS 也已加）。
- **接口**：`GET /api/scrape/state`（items+counts+worker+publish_configured）、`POST /api/scrape/run`、`POST /api/scrape/verify`、`POST /api/scrape/{bid}/resolve`（**字面量路径必须注册在 `{bid}` 之前**，已在注释与测试里钉死）；`/api/libraries/{lid}/scan` 扫描后按开关自动入队（返回 `scrape_queued`）；watcher 在转换/复制入库的三个分支调 `enqueue_scrape_async`（与 `auto_fetch_async` **刻意分开**：抓取看 metadata_fetch，出版看 scrape.enabled）。lifespan 仅在**有 pending 行**时启动 worker（测试环境不会冒出后台线程）。
- **前端**：`LogsView` 加子标签（日志 / 刮削）；`components/tools/ScrapePanel.vue`（概览计数+进度+筛选+表格/窄屏卡片流+待确认三按钮+批量保留/重建+删除原文件二次确认弹窗）；`components/tools/ScrapeFixDrawer.vue`（字段编辑+在线候选+「应用并重建」）；`LibrariesView` 建/改库弹窗加「成品目录」（含本地预检）+ 列表行状态徽标；`api.ts` 加类型与 4 个方法。
- **实测踩到并修掉的坑（都写进了代码注释）**：
  1. 建库时「成品目录放进**自己**库根」查不到（新库还没落库）→ `_publish_path_allowed(raw, extra_roots)` + `_own_roots()`，改库时按改后的 root/source 校验。
  2. `ScrapePanel` 是 **v-if 子组件**（比 KeepAlive 的 ToolsLayout 深两层），`onActivated` **首次不触发** → 面板停在 0 条；改 `onMounted` 首载 + `onActivated`（data 非空时）复用刷新。
  3. `worker.running` 是「线程活着」（起来后常驻），拿它当「正在刮削/是否轮询」会让轮询永不停止 → 新增 `worker.busy`（手里有书或队列有 pending），界面与轮询都用它。
  4. 面板默认继承侧栏当前库 → 切进来常是空的；改**默认「全部书库」**（与同页日志一致，属全局运维视图）。
  5. `source_removed` 的书若被重新放回源目录，会被台账永久挡住（该状态无按钮）→ 源存在即放行重入队。
  6. 源已回收的书不给「整理」入口（没有源可出版，进去白跑）。
  7. 模板里写 `**粗体**`（Vue 不渲染 markdown）→ 改 `<strong>`；窄屏七列表格改卡片流（`hidden md:block` + 独立 `<ul>`）。
- **验证**：新增 `tests/test_scrape_publish.py`（19 用例：纯硬链接同 inode+nlink=2、写元数据只改副本且源 inode/sha256 不变、幂等、未配成品目录不入队、跨设备回退复制、重试上限、副本被删只标 removed 且不自动重建、确认删源走回收目录、保留原文件不复活、rebuild 回 ok、孤本、源放回可重出版、库列可写、状态统计、端点顺序/鉴权/边界校验、扫描自动入队+按库开关）。**全量 242 passed**；type-check + build + deploy 通过。
- **端到端实测（8791 实例）**：源与副本 `st_ino` 相同且 `links=2`；删副本 → verify → `removed`；UI 勾选确认后原文件出现在 `cache/recycle/20260919-141934_三体.epub`（**移动非抹除**）；宽/窄屏截图正常，**0 console error**。
- **文档**：`README.md` 功能清单 + 新增「刮削出版：给外部阅读器的第二份真相」小节；`docs/roadmap-*` 是「上游缺口」文档，本项目独有扩展不写入。
- **仍未做**：第 17 期 T4（建库三页签对齐上游）/T5（文档）；`series_meta`/`apply_rename` 的结构性重排是否也去文件写（改前需用户确认）。

## 第 19 期：写文件的最后两处收敛 + T4 建库三页签（已完成）
- **用户指令**：「重排册号、批量改名只存服务端，继续做 T4」——把「结构性重排」也纳入「元数据只存服务端」的口径。
- **`series_meta.renumber_apply`**：写 OPF 改为写 `meta_override.series_index`（`orig=old_index`）。**新引入显式无值哨兵** `db.META_CLEAR = "-"` + `db._meta_out(field, value)`：覆盖值是**列**、存不了空串（空串在 `set_override` 里表示撤销覆盖），而「清空序号」是有意义的语义 → 写哨兵、读取端翻回空串；`_CLEARABLE = ("series_index",)` 限定作用域，别扩大到任意字段。翻译点三处：`db.get_effective_meta`（哨兵要**带着空值**并进 merged，否则 library 那边会保留文件旧值）、`metastore.effective`、`metastore.state`。返回新增 `cleared` 列表。
- **`fileops.apply_rename`**：删掉 `_rewrite_epub_meta`（唯一调用点就是它）；`meta_field/meta_value` 改为写 `db.set_override`，返回新增 `overrides`。**关键坑**：改名后 book_id 变了（basename 派生），覆盖必须落到**新 id**；而**不能**用 `_lib_of(名字)` 算库 id —— 改名刚做完时扫描缓存（TTL 5s）里没有新名字 → 反查失败 → id 退化成**旧的纯哈希形态** → 覆盖写进一个没人读的 id，表现为「改了没生效」且**不报错**。新增 `fileops._owning_library_id(path)`：按**真实路径包含关系**（取最深匹配）算库 id。测试用「扫描结果的 id」比对才抓得住。
- **模块头部新增第 4 条硬约束**（fileops）：本模块只动文件名、不动文件内容；`patch_epub_meta`/`rewrite_epub` 已**退出生产路径**（前者只剩测试造夹具用，后者只给 `publish.py` 写副本用），docstring 已标注，别再有新调用方。
- **测试**：新增 `tests/test_rename_server_side.py`（6 例：改名只动文件名+字节不变+覆盖落新 id、old==new 不碰文件、纯命名规则批改不写覆盖、冲突条目跳过、两个接口端到端）；`test_series_meta.py` 加严（`_sha_of` 断言库内文件字节不变、哨兵不泄漏、清空后参与回读比对）；`test_api_smoke.py` 的「仅支持改写 EPUB」断言随文案更新为「仅 EPUB」。
- **健壮性修复（真实教训）**：接口用例会把 **scrape daemon worker** 真叫起来，而测试 DB 是**用例级隔离**的 → 跨用例活着的线程拿旧连接查新库，制造「单独跑必过、全量跑随机挂」。修法：`scrape.stop(timeout=...)` 支持 join，`tests/conftest.py` 加 **autouse 夹具**每例收尾停 worker（与「测试不养 watcher 线程」同一纪律）。连跑三轮 248 passed 稳定。
- **T4 建库流程对齐上游**：`LibrariesView` 改为「顶部工具条（全部扫描 / 过滤 / 排序：默认 · 名称 · 书籍数 · 上次扫描）+ 每库**四栏卡片**（书库 · 内容 · 自动化 · 上次扫描）」；新建/编辑弹窗改**三页签**（内容 / 自动化 / 上次扫描）。自动化页签含监听开关、扫描间隔、cron、「刮削出版」开关 —— 后者是**每库覆盖项**，保存时按「与全局一致 → 恢复继承（DELETE settings）；不一致 → 写覆盖（PUT settings）」处理，避免把继承关系切断。`api.ts` 的 `LibraryEntity` 补 `watch/scan_interval/scan_cron`（DTO 一直在返回，类型里缺）。
- **实测（8791）**：工具条与四栏渲染正常；三个页签内容正确（含「上次扫描：24 分钟前（2 本）」）；取消「刮削出版」→ 保存 → 接口确认 `scrape.enabled` 由继承变覆盖（False），再勾回 → 保存 → 回到继承（覆写 False）；控制台无新增错误（只有既有的 `/api/prefs/devices/*` 404）。
- **仍待办**：第 17 期 T5（文档同步）；`publish.py` 是**唯一**还会写文件的地方（写副本，且经原子替换），这条口径已写进 README 与模块 docstring。

## 已推送 GitHub（4 个 commit）
- `da5b8ac feat(core)` 后端三合一（17 期收尾 + 18 期刮削出版 + 19 期写文件收敛）→ `0078b91 test` 用例 → `c8758fe feat(frontend)` 刮削页/成品目录/建库三页签 → `0c03128 docs` README 与记忆。
- **为什么按「层」而不是按「期」拆**：17/18/19 三期的改动在 `db.py` / `server.py` / `watcher.py` / `api.ts` / `LibrariesView.vue` 里**逐行交织**，按期拆必须做 hunk 级暂存（不可脚本化、易错）；按层拆每个 commit 都自洽（后端与测试同时落地）。三期的口径都写进了 commit 正文，历史可读性由正文兜住。
- `.vscode/settings.json`（仅 `python.languageServer: Default`）是用户本地编辑器偏好，**故意不提交**，保持未跟踪。
