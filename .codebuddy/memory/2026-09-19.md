# 2026-09-19

## 第 24 期 · 入库自动抓取放开到漫画/有声书（已提交推送）
- 根因：`watcher.auto_fetch_async` 的 `.epub` 早退（原「无 OPF 可写」理由第 22 期起失效），且有声书目录分支（watcher.py `p.is_dir()`）根本没调 `auto_fetch_async`/`enqueue_scrape_async`。
- 改动：
  - `auto_fetch_async(name, cfg, kind=None)`：guard 改 allow-list（epub/mobi/azw3/pdf/fb2/cbz/cbr，复用 `comics.COMIC_EXTS`）+ `kind="audiobook"` 显式放行；仍受 `metadata_fetch.enabled`+`auto_on_import` 双重门控。
  - 有声书目录分支补两行调用（`auto_fetch_async(rel, cfg, kind="audiobook")` + `enqueue_scrape_async(rel, lib, cfg)`）。
  - 更新过期 docstring（「只对 EPUB」→ 可抓格式 + 双重门控）。
- 测试：新增 `tests/test_watcher_auto_fetch.py`（5 个：allow-list+kind 单元、门控、漫画库扫描触发、有声书库扫描触发、关闭不触发），全部 mock 外呼、断言名字抵达 `metafetch.auto_fetch`；保持后台线程 `wait_pending` 收尾。
- 验收：全量 270 passed（基线 265 + 5）/ exit 0。
- 记忆：`MEMORY.md` 待办第 80 条移除（本期落实），并精简超限文件、基线计数更新为 270。

## 第 25 期 · Profile 补全（YOU→Profile：头像/显示名/时区/引导重放）
- 关键前提：原 `docs/bookorbit-settings-inventory.md` 已严重滞后于代码——用户据此选的「阅读器五页」实际早已 `ready`（PDF/Comics/Audiobook/Fonts/General 共享 localStorage 偏好），LIBRARY→Metadata 7 子页也早已共用 `MetadataPage.vue` 实现。真正剩余缺口只有 YOU→Profile 几项 + DEVICES 外部同步任务。后续规划应以 `frontend/src/data/settingsNav.ts`（ready/placeholder 权威）为准，不再信 inventory 文档的整体口径（仅逐项刷新）。
- 后端：
  - `novelforge/core/db.py`：`users` 表加 `display_name/timezone/avatar_path`（PRAGMA 守卫 ALTER 迁移）；新增 `get_user_profile/update_user_profile/set_user_avatar/clear_user_avatar`；`hour_histogram()` 按账号时区归一（ZoneInfo，失败回落服务器本地时）→ 接通 Early Bird/All-nighter 等时间类成就。
  - `novelforge/server.py`：新增 `GET/PUT /api/account/profile`、`POST/DELETE/GET /api/account/avatar`（复用 `_read_capped`，5MB、JPG/PNG/WEBP 白名单，落 `CACHE_DIR/user/avatar.<ext>`，零外链分发）；`GET /api/account/avatar` 加入 `_MEDIA_TOKEN_PATHS` 允许 `<img src>` 带 `?token=`。
- 前端：
  - `lib/api.ts`：加 `AccountProfile` 类型 + `getProfile/updateProfile/uploadAvatar/deleteAvatar/accountAvatarUrl`。
  - `stores/auth.ts`：扩 `displayName/timezone/avatarUrl` + getters `display` + `loadProfile/applyProfile`，init/login 时加载。
  - `views/settings/pages/ProfilePage.vue`：账号卡加头像上传/移除、显示名、时区（IANA 全量下拉）；新增「新手引导」卡接 `GuidedTourModal`；未支持口径收敛为 OIDC/Username/Email。
  - 新建 `components/settings/GuidedTourModal.vue`（3 步轻量浮层，book/settings/library 图标，Esc/backdrop 关闭）。
  - `components/UserMenu.vue`：顶栏优先显示头像图 + displayName。
  - `lib/icons.ts`：补 `x` 图标。
- 文档：`settingsNav.ts` account/profile note 刷新；`docs/bookorbit-settings-inventory.md` §2.1 口径表刷新（头像/显示名/时区/成就/引导 → ✅，Username/Email/OIDC → ➖ 单用户不适用）。
- 验收：后端 `py_compile` OK；临时 DATA_DIR 跑通 profile/avatar 助手与 hour_histogram；前端 `vue-tsc --build` 0 错误；eslint 0 诊断。HTTP 上传 e2e 未跑（需起服务，路由为已验证助手 + 既有 `_read_capped`/`FileResponse` 薄封装）。
- 注：本期未提交/推送（用户仅说「开始」，未要求 commit）。

## 文档刷新 · bookorbit-settings-inventory.md 全量对齐 settingsNav.ts
- 任务：用户在第 25 期候选梳理后选「刷新滞后文档」——全量刷新 `docs/bookorbit-settings-inventory.md` 的「本项目可行性」列，对齐 `frontend/src/data/settingsNav.ts`（ready/placeholder 权威）。
- 改动（纯文档，未碰代码）：
  - §0 图例 + §1.2 结构树补「权威真值源 = settingsNav.ts，本文为上游采集基线」说明。
  - YOU 组：Theme/Book Covers/Icons/Layout/Behavior/eBook/PDF/Comics/Audiobook/Fonts/General/Notifications 整表对齐（阅读器五页、Book Covers、Notifications、Audiobook 等原 ⬜/➖ 误标项改为 ✅；Display 三页保持占位对照；eBook 个别子项仍 ⬜）。
  - LIBRARY 组：Libraries（placeholder 对照）/ Metadata 7 页（Providers/Field Rules/Custom Fields/Score/Auto-fetch/Authors/Genre Blocklist 全 ✅）/ File Naming（🔵）/ Maintenance（✅ + IMPORT/RECOMMENDATIONS/UPDATES 只读）整表对齐。
  - DEVICES/ACCOUNTS/SERVER：KOReader（补 kosync 服务端落地 note）/ OPDS（✅）/ Hardcover/Readwise/StoryGraph（✅ 凭据+验证，同步未实现）/ Book Dock/Server Fonts/Audit Log（✅）/ Requests（🔵 指向网络与下载）。
  - 新增 §2.42 Komga 小节（settingsNav 有、文档原缺失）。
  - §5.2 映射表由旧「缺失/整体缺失/架构级差异」重写为已对齐的实际判定 + 注明 §5.3 为历史记录。
- 验收：grep 确认无残留「整体缺失/整体占位/架构级差异/无有声书/本项目无元数据抓取体系」等过时候选；残留 ⬜/➖ 均为 settingsNav 认定的真实未实现项（占位页/单用户无意义/个别子项）。
- 待办提醒：第 25 期(Profile)代码改动仍处未提交状态（git status 11 文件 + `frontend/src/components/settings/` 新目录），需在合适时机收口（提交+推送+起服务做头像上传 HTTP e2e）。

## 收口 · 第 25 期 + 文档刷新（e2e 验收 + 提交推送，已收口）
- 头像 HTTP e2e：起 `/tmp/nf-test` 实例（端口 8791，`AUTH_USER=admin`/`AUTH_PIN=test1234`，`AUTO_WATCH=false`）；脚本 `/tmp/avatar_e2e.sh` 7 项全过：上传 200+ok+avatar_url、`?token=` 分发 200（content-type image/png、字节一致、Cache-Control max-age=86400）、profile 含 avatar_url、移除 200、移除后分发 404、非图片 400、空文件 400。
- 提交推送：按能力拆 3 commit 到 origin/main（`ed5639c..4516149`）——后端 `b1567ec`（db.py+server.py）、前端 `efaf486`（api/icons/stores/UserMenu/ProfilePage/settingsNav + 新 `components/settings/GuidedTourModal.vue`）、文档 `4516149`（inventory.md + 本记忆）。
- `.vscode/settings.json` 仍 modified，非本任务改动（疑似环境/其他会话），提交时精确 add 排除、未纳入。
- 至此第 25 期 Profile 补全 + 文档刷新全部收口，工作区干净（仅余被排除的 `.vscode/settings.json`）。
