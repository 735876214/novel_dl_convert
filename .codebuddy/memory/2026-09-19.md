# 2026-09-19

## 第 23 期：对标差异清理（方向 D，纯前端/文档）
- 维护页其余分组：ACHIEVEMENTS 的 Backfill 后端已存在（`POST /api/achievements/backfill`），前端 `MaintenancePage.vue` 接入「重算成就」按钮；IMPORT / RECOMMENDATIONS / UPDATES 在容器口径下明确未支持（`SettingsUnsupportedCard`）。`api.ts` 的 `AchievementsOverview` 补 `backfilled?` 字段。
- 阅读器排版对齐：`ReaderEbookPage.vue` 实际已实现 13 项（阅读模式/13档主题/字体/字号/行高/内容宽度/段落间距/首行缩进/字距/词距/分栏/两端对齐/断词），但 `settingsNav` 的 `reader/ebook` note 仍写「仅字体/主题/字号/行高/内容宽度」——已修正为 13 项 + 4 未支持；未支持卡分组改为仅 `NEW BOOKS`/`ADVANCED`。
- 5 个 placeholder 页全部「留作上游对照」：`appearance/icons`/`appearance/layout`/`appearance/behavior`/`koreader-upstream` 补 note（`libraries` 原本已有 link+note）。未删任何导航项（无重复入口）。
- 文档同步：`docs/roadmap-verification.md` 未支持表补阅读器排版/维护页/外观占位；`docs/bookorbit-settings-inventory.md:763` 修正「均缺」断言；`docs/bookorbit-capability-gap.md` 两条 Achievements 标「第 22 期已实现」。
- 验证：前端 `type-check` + `build` + `deploy` 通过。本环境无 `.venv`、网络受限，未跑 pytest/playwright（改动纯前端 TS + 文档，未触路由注册/Python）。
- 顺手精简 `MEMORY.md` 待办：划掉 5 placeholder 项。
