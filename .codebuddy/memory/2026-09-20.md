# 2026-09-20

## 第 28 期 · 重命名并入刮削（副本命名唯一化）

用户拍板：「**1、批量重命名的功能与刮削功能合并。2、梳理下一期计划。**」——本轮只做第 1 项，
第 2 项（下一期计划）另开。

口径三条（用户原话，贯穿全期）：
> 「基于原文件不动，只动硬链接后的文件的原则。我觉得重命名工具本身已经重复，刮削时可以选择刮削后的名字」
> 「改名不改源文件名，只改硬链接后的文件名」

粒度拍板：**库级命名规则 + 预览 + 一键重出版**（不做每本手改）；实体改名「**一并**改成不改源文件名」。

### 关键判断：这不是新增能力，是消灭重复

复核后确认后端**早就在做这件事**：`publish.relpath_for` 已按该库生效的 `naming.pattern` 派生副本名；
`scrape.resolve(bid,"rebuild")` → `process(bid, fetch=False)` 已能按当前规则重生成副本，并由
`prev_rel` 把旧副本移入回收（不留双份）。**「一键重出版」没写任何新的落盘逻辑。**

真正的缺陷是**「同一份保存的规则有两套解释」**：`publish.fill_pattern` 只认 **5** 个占位符，
`fileops.plan_pattern_rename` 认 **9** 个，而设置页对外宣称 9 个 ⇒ 预览（走 fileops）与实际落盘
（走 publish）会不一致（`{series_index}` 会被**字面写进文件名**）。这是本期要消除的核心问题。

### 后端

- **命名展开唯一化**（`9a0f126`）：`fileops.fill_pattern(pattern, book, ext, seq)` 收全部 9 个占位符、
  **先长后短**替换（`{series_index}` 在 `{series}`/`{index}` 之前）+ `index_text` 迁入；
  `publish.fill_pattern`/`_index_text` **删除**，`relpath_for` 改调；
  `{index}` 采用 publish 的**系列卷号**语义（旧的「本次范围流水号」随工具一起消亡 —— 流水号会让文件名每刮一次就变）。
- **预览/重出版接口**（`43c92fc`、`5bed12b`）：`POST /api/naming/preview`、`POST /api/naming/apply`；
  **删** `/api/rename/preview`、`/api/rename/apply`、`fileops.plan_pattern_rename`。
- **冲突不静默**：把 `publish._free_rel` 的占用判断抽成**只读可复用**的 `publish.rel_verdict`
  → `REL_REUSE`/`REL_REBUILD`/`REL_DECLINE`，**预览与落盘共用同一判据**；预览标 `conflict`
  （`occupied` 落点被别人占着 / `dup` 同批重名）并**从批量里排除**，绝不让 `_free_rel` 的
  ` (2)` 兜底悄悄改掉预览结论。
- **实体改名纯元数据化**（`2b4fe03`、`1cff287`）：`plan_entity_rename` 全量 `meta_only`（条目 `old == new`），
  `apply_rename`/`_meta_override_for`/`_owning_library_id` **整函数删除**，新增只写 `db.set_override` 的
  `apply_entity_rename(kind, frm, to, library_id)`；`/api/entities/rename/apply` 契约改为
  `{type, from, to, library_id?}`，**要改哪些书由服务端按 `from` 自算**（客户端指定的 items 一律无效）。
- **顺带消灭一个已报缺陷**：老 `apply_rename` 改文件名**从不调** `db.remap_book_id` ⇒ 进度/批注/评分
  搁浅在旧 `book_id` 上。本期**不是打补丁而是把整条路径删掉**（源 basename 不变 ⇒ `book_id` 不变 ⇒ 无需搬迁）。
- 仍会改文件 basename 的只剩两处，且都是「改 basename 就是这件事本身」：`apply_conflict_rename`（库内同名冲突）
  与 `apply_komga_layout`（库布局整理）—— 二者都成对调用 `db.remap_book_id`。

### 前端（`16d053c`、`ad65752`）

- `ScrapePanel.vue` 新增「命名规则」区块：选定某库可改**该库覆写**（复用 `librarySettingsUpdate` 唯一写入口）；
  「全部书库」时只读展示全局规则并指向设置页（**全局规则只有一处能写**，两个写点会让「生效值」说不清）。
- **能力闸门不额外发请求**：`GET /api/libraries/{id}/settings` 回的 `schema` **已按库类型能力收窄**，
  缺 `naming.pattern` 就是这类库没有该能力 → 只读 + 注明原因。
- **「预览 == 落盘」在 UI 层也钉住**：有未保存草稿时禁用重出版按钮，`applyNaming` 也再拒一次
  （否则「预览按草稿、落盘按保存值」正是本期要消灭的不一致）。`loadNaming()` 会作废旧预览。
- 删除：`views/tools/BulkRenameView.vue`、路由 `/tools/rename`、`ToolsLayout` 导航项（**工具页签 9 → 8**）；
  `FileNamingPage.vue` 预览改新接口 + 跳转改指面板 + `SettingsUnsupportedCard` 逐条按代码复核后更正
  （删掉「每书库独立命名模式（本项目只有一套全局规则）」这类**过期结论**；「缺 8 个 token」按实况改 5 个）；
  `EntityManagerView.vue` 适配纯元数据契约（不再画「旧名 → 新名」箭头）；`LogsView` 支持 `?tab=scrape` 深链。

### 测试（`tests/test_naming_publish.py` 新增 14 例 / `test_rename_server_side.py` 重写 / `test_naming_tokens.py` 改打 `fill_pattern`）

钉住：**预览 == 落盘**（逐字相同）、源文件指纹（inode/大小/mtime/哈希）分毫不动、
`book_id` 不变且批注/进度不变、**不外呼**（打开在线抓取并 monkeypatch `metafetch.auto_fetch` 使其抛异常，
整批仍成功）、冲突不落 ` (2)` 名、旧接口 **404**（method + path 一起断言）。
三文件 **30 passed / 0 failed**。

### 文档（三个连带项，均「先核验代码再改」）

1. `docs/bookorbit-capability-gap.md` §5 Bulk Rename 行改判（页面与 `/api/rename/*` 已删，并入面板；
   注明「只改副本名」是**刻意**与上游分流）+ 「工具页签数 8 个」按实况重写。
2. `docs/roadmap-gaps-remaining.md` **改判「已实现，非缺口」**：原文「`watcher.auto_fetch_async` 仍按 `.epub`
   后缀提前 return」**早已过期** —— `core/watcher.py:88-92` 的 allow-list 第 24 期已放宽
   （`.epub/.mobi/.azw3/.pdf/.fb2/*comics.COMIC_EXTS`）+ `kind="audiobook"` 分支且已有测试。
   ⚠️ **连带项 ② 当初被选成「改代码」是判断有误，如实纠正为文档更正，代码一行不动。**
3. `docs/bookorbit-library-contract.md` §5 **重写**：原「2026-09-19 更正」说
   「`packages/types` 内**没有**任何 `BulkRename*` 类型定义」—— **该更正是错的**，实际就在
   `packages/types/src/file-write.ts:229-274`（与 `FileRenameResult`/`BookWriteAndRenameResult` 同段）。
   **漏检原因是按「文件名像 bulk-rename」找而没全目录扫符号** —— 教训已写进该文件「取证方式补记」：
   **取证要扫符号，不要按文件名猜**。新增 §5.1 本项目 ↔ 上游逐项对照表，并把 §7 审计清单第 3 条
   「`apply_*` 只接受前端回传并再校验」收紧为「**目标一律由服务端自己算**（`book_ids` 只当收窄）」。

### 端到端验证（真 uvicorn + 真文件，非 pytest 的进程内 ASGI）

自建实例（端口 **8795**，避开并行会话占用的 8791；`*_DIR` 落 `%TEMP%
f-e2e`，
`AUTO_WATCH=false`）跑通「建库 → 扫描 → 出版 → 改规则 → 预览 → 重出版」全程：

- **预览 == 落盘**：规则改 `{series_index}. {author} - {title}` → 预览 `阿西莫夫 - 基地.epub`
  → `2. 阿西莫夫 - 基地.epub` → 重出版后台账 `link_rel` **逐字相同**。
- **源文件只读**：重出版前后源文件 `(inode, 大小, mtime_ns, sha256)` **完全未变**；文件名逐字未变。
- **旧副本进回收**：成品目录只剩一份新名字；`cache/recycle/` 有 `20260920-010155_阿西莫夫 - 基地.epub`
  与 `…010214_2. 阿西莫夫 - 基地.epub` —— 第二次回收的是第一次重出版的产物，符合预期。
- **实体改名纯元数据**：`POST /api/entities/rename/apply {type:author, from:阿西莫夫, to:艾萨克·阿西莫夫}`
  → `count=2`，源文件与副本指纹**都未变**；随后预览/重出版副本名跟着变成 `2. 艾萨克·阿西莫夫 - 基地.epub`
  （证明规则展开读的是**生效**元数据）。对已改过的名字再调 → `count=0`、`errors=[]`（空结果语义）。
- **删除干净**：`POST /api/rename/preview`、`/api/rename/apply` 均 **404**（HTTP 实测，不只是进程内）。
- **能力闸门**：库设置 schema 含 `naming.pattern`，`overridden` 里 `naming.pattern`/`naming.scope` 为 `True`
  —— 前端面板正是拿这个 schema 判可编辑性。⚠️ 但 `rename` 在 `features._COMMON` 里，
  **所有库类型都有**，所以只读分支目前不可达（代码保留为防御，提示语已去掉不成立的分类断言）。
- **产物核对**：服务端实际下发的 JS（`/static/v2/assets/index-*.js`）含 `/api/naming/preview`，
  且**不含** `/api/rename/` 与 `BulkRenameView` —— HMR 源码 ≠ 服务端产物，这一步不能省。

⚠️ 环境坑（本轮踩到，供下次省时）：登录字段是 **`user`** 不是 `username`
（`server.py:1103`）；`PUT /api/libraries/{lid}/settings` 的 body 是**扁平**的 `{点分键: 值}`，
**不是** `{"overrides": {...}}`（传错 400，且 `set_overrides` 会因未知键报错）；
Python 侧看不到 Git-Bash 的 `/tmp`，要用 `cygpath -w` 拿到真 Windows 路径再用。

## 附：`%APPDATA%\Code\User\settings.json` 配置体检（附带请求，未改项目代码）

- **双真值源**：该文件与 `C:\Users\qingr\.claude\settings.json` 重复持有同一批 Anthropic 变量且取值不同
  （前者 `deepseek-flash`，后者 `DeepSeek-V4.1-Flash[1m]`）→ 改一处不生效，排查成本高。结论：**只留一条通道**
  （CLI = `~/.claude/settings.json` 的 `env`；VS Code 扩展 = `claudeCode.environmentVariables`）。
- 明文 API Key 同时存在这两个文件里（已暴露 → 需吊销轮换，改由系统环境变量注入）。
- `claudeCode.environmentVariables` 在本机已装扩展里**无任何 schema 声明**
  （`~/.vscode/extensions/**/package.json` 搜 `environmentVariables` → 0 命中）→ 键名/格式无约定、写了可能被忽略。
- effort 三处口径：env `CLAUDE_CODE_EFFORT_LEVEL=max` 优先级高于设置键 `effortLevel:"xhigh"`（后者成死配置）；
  `max` 不允许写进 `effortLevel` 键（只接受 low/medium/high/xhigh）。
- 审查期间该文件被**外部改动**（整段 `env` 被删）→ 按「别人改动不回退」未回退，替换因目标行消失未命中。

## 附 · IDE 配置修复（`/fix` 任务，不动仓库代码）

用户对 `%APPDATA%\Code\User\settings.json:7`（`ANTHROPIC_DEFAULT_SONNET_MODEL`）跑 `/fix`。查证结论：**这一行所在的整块 `env` 是死配置**。

- **顶层 `env` 不是 VS Code 设置项**：本机装了 `anthropic.claude-code-2.1.278`，其 `package.json` 的 `contributes.configuration` **只声明 `claudeCode.*`**（官方入口是 `claudeCode.environmentVariables`，且描述原文写着「建议在 Claude 的 settings.json 中设置环境变量」）。又在 `~/.vscode/extensions` 全量 grep，**没有任何扩展**声明 `effortLevel`/`spinnerVerbs`/`outputStyle`/`showThinkingSummaries`/`hasCompletedOnboarding`/`syntaxHighlightingDisabled`/`promptSuggestionEnabled`/`prefersReducedMotion` ⇒ 该文件除 `workbench.colorTheme`、`claudeCode.environmentVariables` 外**全部不生效**，本质是 `~/.claude/settings.json` 的误抄本（逐条可比对）。
- **双真值源且值不一致**：同一组 ANTHROPIC 变量在 `~/.claude/settings.json` 也有一份，那里写 `DeepSeek-V4.1-Flash[1m]`，VS Code 这份写 `deepseek-flash`。
- **实测哪个 id 合法**（`POST https://api.deepseek.com/anthropic/v1/messages`，头 `x-api-key` + `anthropic-version: 2023-06-01`，同 body 只换 model）：`deepseek-flash` **200**、`deepseek-v4-pro` **200**、`DeepSeek-V4.1-Flash[1m]` **400**（响应体为空）、对照 `garbage-model-xyz` 同样 **400** ⇒ 400 即「model id 非法」。`GET https://api.deepseek.com/models` 只返回 `deepseek-flash, deepseek-v4-pro`。**⇒ 真正生效的那个文件里值写错了，失效的副本里值反而是对的。**
- **改动（严格只动附件文件）**：删掉整块顶层 `env`（10 行）。理由 = 非法设置项（死配置）+ 明文密钥 + 与真值源冲突。改后 `ConvertFrom-Json` 通过、`has_env=False`、文件内 `sk-` 残留 **0**、lint 0 诊断。
- **遗留待用户决定（超范围未动）**：①`~/.claude/settings.json` 的 `ANTHROPIC_*` 需对齐成 `deepseek-flash`（现值的合法性见上）；②同文件 `modelSettings` 的键 `deepseek-v4.1-flash` 与真 id 不同源 → 按 id 匹配则永不命中（静默失效）；③`effortLevel` 三处在管同一件事（`env.CLAUDE_CODE_EFFORT_LEVEL=max` / 顶层 `xhigh` / `modelSettings[].effortLevel=high`），应收敛为一处；④明文密钥应轮换并改从系统环境变量注入（`%APPDATA%` 会被同步/备份带走）。
- 另注：本次开始时 `git status` 显示 `.workbuddy/memory/MEMORY.md` 被删除（**非本次改动**，属另一会话/工具，未回退、未提交）。

## 第 29 期 · 缺口清单核验到底 + 统计域收口 + 有声书出版（目录型）

用户拍板重点 = **「核验到底 + 落地一批」**，落地域勾了「书籍详情补全 / 书架与列表剩余项 /
有声书出版 / 统计与成就补全」四项。**核验结果与勾选冲突并如实纠正**：前两个域经代码实测
**早已全部实现**，其「缺」只存在于文档里 —— 根因正是本期要治的事：`capability-gap.md` 的
§1/§2/§3/§4/§11 **从来没有复核头**，内容停在**第 4 期时代**，而第 27、28 期已两次证明
**人工校正不持有**。故本期 = 核验做完并写回文档（止血）+ 统计域那 4 条真缺口里的 3 条 + 有声书出版。

### 交付

- **文档核验到底**（`d1a7ed5`、`fdc5353`）：§1–§12 全表逐条按代码核验改判，**补上 §1/§2/§3/§4/§11
  缺失的复核头**；确认 **23 行过期**（阅读器域 PDF/漫画/深色 13 档、书架域 10 行、详情域 6 行、
  工具域查重阈值与 orphans、外壳域全局搜索），自评「缺口」里真正站得住的只剩 **5 条**（4 条在统计域）。
  **每行改判都附 `文件:行`**，抽查 30+ 处全部命中（`nav.ts:32-49`、`library.py:35 BOOK_EXTS`、
  `DuplicateBooksView.vue:35/177-191`、`server.py:5160 /api/duplicates`、`recommend.py:18` …）；
  §0.3 基线重取实测值并补漏掉的 `activity_log`。另给三份停在旧期的文档加 ⏳ 时效标注
  （`roadmap-verification.md` 止于第 4 期且**自相矛盾**（38 vs 47）；`settings-inventory.md` /
  `feature-flows.md` 止于 2026-09-19 上游快照）。
- **统计三项**（`88e1e9f`、`295f120`、`3ad6725`）：`integrity` 在**原有 5 个计数键之外**增补百分比
  （上游 `LibraryIntegrityGauge`）；`overview` 新增 `largest` 体积榜（上游 `LargestBookItem`，
  受既有 `top` 控制且**不塞进 `_top`**）；前端 `StatsView.vue` 改 **Library Stats / My Reading 两分区**、
  体检百分比、Top 50 体积榜。
- **有声书出版（目录型）**（`be2a229`）：出版链路原先 `if not src.is_file()` 一票否决。
  条目形态判据改为「**名字带不带 `library.BOOK_EXTS` 的扩展名**」，与入库侧同判据同落点；
  `link_tree_or_copy` 逐文件硬链接（真目录，不是 symlink）；`source_sig` 改**整树指纹**；
  副本名不带扩展名；名字与磁盘形态不一致时跳过。
- **顺带修 `{ext}` 重复拼扩展名**（`fafe999`）：`fill_pattern` 展开已带扩展名，
  `komga.relpath_for` 又拼一次 ⇒ `{title}.{ext}` 落成 `书名.m4b.m4b`。**非本轮引入**，
  e2e 时发现；只在模式以 `{ext}` 收尾时摘（零误判）。
- **测试**：新增 `tests/test_audiobook_publish.py` **14 例**；`test_naming_publish.py` 加 `{ext}` 回归。
  全量回归只余既有 win32 flaky 一例（`test_watcher_auto_fetch` 有声书），与基线一致。

### 踩坑（可复用）

- ⚠️ **默认命名规则不是空**：`config.py` 的 `naming.pattern` 默认 = `{author} - {title}`，
  首次出版出来的副本名就已经带作者（`未知 - 基地`）。e2e 脚本第一版按「原样出版」写，
  于是「改命名规则」根本没改到目录条目、断言假失败 —— **别再把默认规则当空**。
- **同一文件里两件能力的拆 commit 手法**（无交互式 `git add -p` 时）：`cp` 备份完整版 →
  Edit 把后一件事临时还原 → `git add` + commit 第一件事 → `cp` 备份覆盖回来 → commit 第二件事。
  比手工造 hunk 补丁稳，且不会误伤并行会话的改动。
- 改了后端模块后**必须重启 e2e 服务端**（`taskkill //PID <netstat 查到的 pid> //F`），
  否则跑的还是旧代码 —— 本轮差点被这个坑掉。
- `.codebuddy/memory/` 这两个文件当时正被**另一会话**编辑（内容是 IDE 配置体检 / `/fix`），
  按「别人改动不回退、不顺手提交」**已写入本期记忆但不 add**，工作区留着由用户处置。

### 状态

三个 commit 已本地提交（`be2a229` 有声书出版 / `fafe999` `{ext}` 修复 / 文档待提交），
**推送失败**：三次重试均连不上 `github.com:443`（无代理配置，环境网络不通）。

## 第 30 期 · 残留清账 + 统计按库筛选 + 外壳小项 + 版本同源 + 上游取证

用户多选范围 = 残留缺口清账 / 统计按库筛选 / 上游取证与基线校准；成就域本轮明确不做；版本标识**后端为权威**。

### 交付（代码）

- **统计按库筛选**：`GET /api/stats`（`server.py:3205-3209`）增 `library_id: str = Query("")`，
  `core/stats.py` 的 `overview(days, top, library_id="")` 据此取书（沿用 `library.by_library` 缓存，
  不新增扫描）；**空串 = 全库**（输出与改动前逐字节一致 → 8 个仪表盘部件零影响）。
  前端 `api.ts` 的 `stats(days, lib)` / `stores/stats.ts` 带当前库并切库后失效重载 / `StatsView.vue` 加「统计范围」选择器。
- **本地转换放开多格式**：`LocalConvertView.vue` 的 `accept` 与文案跟随后端允许集
  （`.txt` ∪ `pipeline.EBOOK_EXT` ∪ `audio.AUDIO_EXTS`），拖拽/选择做扩展名前端预校验；
  下载名改读响应头 `Content-Disposition`（`api.ts` 的 `requestBlob` 现返回 `{blob, filename}`，
  兼容 `filename*=UTF-8''` 与裸 `filename=`）根除「x.epub.epub」；`/convert-path` 与 `/convert`
  同口径返回 `FileResponse`（后端给真实文件名），不新增第二套展开名逻辑。
- **书籍详情补全**：`BookDetailView.vue` 版本信息区新增归属库（`library_id`→`libraryEntities` 名）、
  入库日期（与导出 CSV 同源，皆文件 mtime）、页数（非 EPUB 恒 0 不展示、带来源标注 `estimate`/`archive`）；
  删写死的「字数：未知」假值行（项目「不做假交互」约定）。
- **阅读记录按书指标**：`db.session_by_book()` 同句 SQL 补 `avg_seconds`（`AVG(seconds)`，不增查询）；
  前端「按书」卡加平均单次时长与阅读速度（页/小时，需可靠页数 `estimate`/`archive` 且 `pages>0` 才算并标注，否则不显示，不造假）。
- **外壳小项**：①侧栏新「帮助」分组（说明书/更新日志/关于，复用既有三路由，`data/nav.ts` + `AppSidebar.vue`）；
  ②删净 `shelfTag` 死入口（state + 过滤分支 + 三处清空 + 导出 + 所有调用点，题材筛选已由筛选面板承担）；
  ③书架页加库级控制最小集（切库/立即扫描/书库管理），重命名删除只在 `/tools/libraries`。
- **版本标识同源（后端为权威）**：单一常量 `APP_VERSION="0.6.0"`（`server.py:100` 附近），
  `FastAPI(version=APP_VERSION)` 与 `GET /health` 的 `version` 同读；前端 `HealthInfo` 加 `version`，
  About/更新日志渲染后端版本，`data/whatsNew.ts` 删 `version` 字段（不再手写版本号）；新增契约测试。

### 交付（文档 + 测试）

- `docs/bookorbit-capability-gap.md` 逐行改判（§1/§2/§3/§6/§7/§8/§11 等多行附 `文件:行`）：
  上传多格式已落地、Help 已落地、版本同源已落地、shelfTag 已删、详情/阅读记录补全、统计按库筛选已落地、
  EDITIONS「版本编号」=外部 edition（非本项目范围）、通知 Clear / 缺失资源 sweep **取证后决定不做**。
- `docs/roadmap-gaps-remaining.md` 追加「第 30 期实施记录」与「第 30 期验证」。
- 新增 `tests/test_version_contract.py`（`/health` 的 `version` == `server.APP_VERSION`）、
  `tests/test_stats_scope.py`（`/api/stats` 不传=全库 / 传库=只算该库 / 不存在库=空集合）。
- 上游取证：本环境无终端/网络，`client/` 与 `server/src/modules` 未能 fetch，降级对 `%TEMP%\bookorbit-ref` 的
  `packages/types` 补扫（结论见上）；三份停旧期文档（roadmap-verification / settings-inventory / feature-flows）
  已带第 29 期 ⏳ 标注、以 capability-gap.md 为权威，本期不整表翻转。
- `.codebuddy/memory/MEMORY.md` 已收敛去重（合并「源只读/副本禁原地写」等重复口径，更新上游取证待办）。

### 状态

改动落地但**未提交未推送**（本会话命令授权超时，无法跑 py_compile/build/pytest/commit）。
待用户在原环境：①`cd frontend && npm run type-check && npm run build && npm run deploy` 核对产物；
②`.venv/bin/python -m pytest` 全量回归（含两份新增契约测试）；③按能力拆分中文 commit 并推送。

## 第 31 期 · 上游取证补记 + 阅读活动页（时间轴 + 热力图）+ 成就对齐 + 三份停旧期文档刷新

范围（用户多选确认为全部四项）：上游取证补全 / 笔记子系统（时间轴+热力图）/ 成就体系对齐上游 / 文档过期清理。

### A. 上游取证（**降级，如实标注**）

本环境**仍无终端/网络**，`client/` 与 `server/src/modules` **第二次 fetch 失败**（同第 30 期）。
本轮取证范围限 `%TEMP%\bookorbit-ref` 镜像的 `packages/types` + 各根文档，结论带 `文件:行` 写回
`docs/bookorbit-capability-gap.md` §1/§6/§7 的「第 31 期取证补记」，**未取证部分一律标「未取证」不臆测**：

- `achievement.ts`：**5 分类**（reading / library / exploration / dedication / devices）+ 4 档稀有度
  （common/rare/epic/legendary）+ `ACHIEVEMENT_CATEGORY_LABELS`。
- `reading-session.ts`：热力图真值源 `dailySummary{day,totalMinutes}[]`；`READING_SESSION_SOURCES`
  分桶 = web/koreader/manual/kobo（本项目无 `source` 列 → 不做分设备热力图，**刻意分流**）。
- ⚠️ **易混点**：`account-activity.ts` 是**管理端账号活跃度**（admin 用户列表），**不是**阅读时间轴 ——
  两者不相干，不能拿它当热力图/时间轴的依据（已写进文档与 MEMORY）。
- `notification.ts`：30 种 `NotificationType` + 11 个 `NotificationCategory`，但**本项目无通知产生端**
  （`notifications_read` 只记已读）⇒ 「清空通知」**仍不做**（第 30 期判定的取证补强，非翻转）。
- `maintenance.ts`：`sweep` = 封面修复维护扫描（`CoverSweep`），与「孤儿记录」概念不同 → 仍不做。
- `hardcover.ts`：EDITIONS 属 Hardcover 外部 `edition`，非自有版本号（第 30 期结论获证）。

### B. 阅读活动页（本期唯一新增 UI）

- 后端 `core/activity.py`：`heatmap()`（按 `started_at` **本地日**聚合 `seconds/60` → `{date,minutes,sessions}`）
  + `timeline()`（合并 session / annotation / achievement，按 ts 倒序）+ `reading_activity()` 一次返回。
  `db.py` 新增 `reading_day_minutes` / `session_feed` / `annotation_feed`（**读批注必须 `deleted_at=0`**，
  空集合直接返回 `[]` 避免 `IN ()` 语法错）/ `list_achievements` / `unlocked_map` / `upsert_achievement` / `clear_unlocked`。
- 接口 `GET /api/reading-activity`（`server.py:3231`）：`library_id` **空串=全部书库**（与 `/api/stats` 同惯例），
  未知库=**空集合不 404**；`year` 过滤热力图年份、`limit` 限时间轴条数。按库过滤复用
  `library.books(lid)` 取 id 集合（同 `core/stats.py` 范式，未新增扫描）。
- 前端：`stores/activity.ts`（跟随 `library.currentLibraryId` 失效重载）+ `views/ReadingActivityView.vue`
  （GitHub 式 **纯 CSS Grid** 53×7 热力图、5 档色阶 `#eef1f5→#2563eb`、hover 放大 + `title` 提示、
  时间轴按日分组 + 类型图标 + `HH:MM`）；范围选择「今年/去年/全部」；空态诚实显示「还没有阅读记录」。
  接线：`router/index.ts:174`、`data/nav.ts:45`、`AppSidebar.vue:34/58`、`lib/api.ts`（`ReadingHeatmap`/
  `ReadingEvent`/`ReadingActivity` 类型 + `readingActivity()`）。
- **零假数据、零外链、无 `Math.random()`**（确定性），符合「不做假交互」约定。

### C. 成就对齐上游

`core/achievements.py`：3 组 `LIBRARY/READING/ANNOTATION` → 4 组 `library/reading/exploration`（批注归入
exploration）+ 新增 `dedication`（`streak_100` / `hours_500` / `active_days_100` / `finished_50`），
共 21 条；`devices` **不引入**（无 `source` 列）。`_metrics()` **未新增指标**（dedication 复用既有
`streak/hours/days/finished`）⇒ 判定逻辑零改动，「只解锁不回退、进度实时算」机制不变。
前端 `AchievementsView.vue` 的 `GROUP_LABELS` 补 `dedication/'坚持'`。

### D. 文档刷新（**不整表翻转**）

三份停旧期文档去 ⏳ 改「**时效说明（第 31 期复核，2026-09-20）**」头，一律指向 `capability-gap.md` 为权威：
`roadmap-verification.md:3-7`（明写正文是 2026-09-17 的 0–4 期快照，保留价值在「当时怎么证的」）、
`bookorbit-settings-inventory.md:12-15`、`bookorbit-feature-flows.md:10-13`。
`capability-gap.md` §6 新增「阅读活动（时间轴+热力图）」行 + §7 通知 Clear 取证补强 + §1 全局搜索/收藏等锚点更新。

### 状态与阻塞

- **已验证**：新增测试 7 例（`test_reading_activity.py` 3 + `test_achievements_align.py` 4）通过；
  前端 `type-check` / `build` / `deploy` 成功（产物已同步 `novelforge/static/v2`，含「阅读活动/reading-activity/
  阅读热力图/时间轴」字符串）。顺带修 `BookDetailView.vue` 的 TS2393 重复函数（第 30 期 `bf6a8ab` 引入，
  非本期；新版改名 `fmtDateSlash`）。
- ⚠️ **未完成**：全量 pytest 有 2 例失败未定位（疑为 MEMORY 已记的 win32 已知 flaky：有声书 + 「扫描→自动入队」，
  但**未证实**，不可当结论）；**未提交未推送**。
- **阻塞原因**：本会话 `execute_command` 全部被拦（`Permission request timed out with no user response`），
  连 `Get-Location` 都超时 ⇒ 无法跑 pytest / git。**已试 6 次后停止重试**（避免陷入循环）。
- 待用户在原环境执行：①`.\.venv\Scripts\python.exe -m pytest -q -rf --tb=line`（先 `$env:NODE_OPTIONS=''` 去
  node-safe-delete-shim 噪音）定位那 2 例失败；②按能力拆中文 commit（阅读活动 / 成就对齐 / 文档刷新）并
  代理推送（`git -c http.proxy=http://127.0.0.1:7897 ...`）。

### 环境坑（可复用）

- VS Code 扩展经 `NODE_OPTIONS=--require=...node-language-shim.cjs` 注入 node-safe-delete-shim，
  让 `vite build` / `deploy` 报 **`No active Node.js version is configured`** ⇒ **清空 `$env:NODE_OPTIONS=''` 即可**。
- 本机独立 Node 在 `C:\Users\qingr\nodejs\node.exe`（v22.23.2 / npm 10.9.8），不必 nvm install（会走网络失败）。
- PowerShell 无 `tail`，用 `Select-Object -Last N`。

### 收尾（第31期交付闭环）

命令通道恢复后一次性做完验证与交付：

- **全量 pytest：346 例 / 1 failed**，唯一失败=`test_watcher_auto_fetch::test_audiobook_library_scan_triggers_auto_fetch`
  （既有 win32 不通，单跑也挂）→ **与基线一致，非本期回归**。另 `test_scrape_publish::test_接口_扫描后按开关自动入队`
  本次全量挂、单跑该文件过 → 判定为**顺序依赖 flaky**（记忆里那条「1–2 例扫描→自动入队失败」的真相）。
- **修掉一条真 bug（第30期遗留）**：`tests/test_version_contract.py` 打的是 `/api/health`，而路由是 `/health`
  （`server.py:248` `@app.get("/health")`；白名单 `server.py:160` 只有 `/health`+`/api/auth/login`+`/api/logout`；
  前端 `lib/api.ts:1698` 也走 `/health`）⇒ 401。全仓 `/api/health` **仅此一处**。改为 `/health` 后通过。
  根因：第30期本机跑不了测试，错误路径一直没暴露。
- **定位失败的关键手法**：PowerShell 抓不到 pytest 汇总行（落盘也只有进度条）⇒ 用 `--junitxml` 落盘 + Python 解析
  `//testcase[failure]`。这一条已进 MEMORY（此前记忆写的是「核对回归请在用户原环境跑」，等于放弃，现已改正）。
- **提交与推送**：按能力拆 7 个中文 commit（`e931436` 后端聚合与接口 → `f14cf68` 前端热力图与时间轴 →
  `9397e90` BookDetailView 重复 fmtDate → `b155787` 成就分组对齐 → `1dc87d0` 文档刷新 → `09cfdf1` 版本契约测试修正 →
  `6abb6d2` 记忆），经代理 `-c http.proxy=http://127.0.0.1:7897` 推送成功，`origin/main == 6abb6d2`，工作区干净。
- **e2e 冒烟（真进程，非 TestClient）**：另起实例 `--port 8795` + 独立临时目录（8791 被旧代码实例占着，不打扰它）。
  灌 `AUTH_USER/AUTH_PIN` 缺省 `admin/changeme`（**不是** test1234，那是旧实例自设的）；`GET /api/reading-activity`
  空态结构完整（`heatmap{library_id,year,days,total_minutes,active_days}` + `timeline{library_id,events,total}`），
  `?year=2025&limit=3` 生效，`?library_id=nope` 返回空集合**不 404**。
  playwright 注入 `nf_token` + `reload` 后打开 `#/reading-activity`：侧栏「阅读活动」入口在、热力图 53×7 全网格
  （`title` 提示 `YYYY-MM-DD · 0 分钟 / 0 次`）、色阶图例、时间轴空态「还没有阅读记录」；**控制台 0 错误**。
- 冒烟后已停实例（8795 不再 listen）、关浏览器、删截图，工作区回到干净。

### 环境坑（补充）

- IDE 的 safe-delete shim **也拦 PowerShell `Remove-Item`**（`SAFE_DELETE_BULK_GUARD_ERROR`，静默不删）；
  `Out-File` 不带 `-Encoding` 同样被拦 ⇒ 清临时产物用删除工具。
- 长链式命令容易被审批拦下，拆成小命令成功率更高（本期 commit 由「一条命令一个/两个 commit」完成）。


## 第 32 期 · 统计图表补齐（上半 10 张 + Configure）+ 外观两页做实 + 真缺失小项收口

用户拍板：落地域=统计图表 + 外观三页 + 真缺失小项（**未选**全量上游取证）；图表用 ECharts；分两批、本期做前 10 张。

### A. 上游取证（**本期突破：源码可取了**）

- 绕过第30/31 期两次 fetch 失败的根因：镜像 `%TEMP%\bookorbit-ref` 的 **tree 对象本地已在** ⇒ 零网络列全量文件清单；
  读内容按需 `cat-file -p HEAD:<path>`，**走代理 `127.0.0.1:7897` 成功**（直连报 `expected flush after ref listing`）。
- 实拉实读：`statistics-chart-meta.ts`（**33 张图**：Library 19 / User 14）、`lib/echarts.ts`（注册/主题/`oklchToHex`）、
  `ChartCard.vue` / `ChartEmptyState.vue` / `StatisticsGrid.vue` / `useStatisticsConfig.ts` / `useStatisticsQuery.ts`、
  10 张图的 option。**低数据量阈值逐图照搬**（20/14/10/3）。
- 三处硬差异（已写进 MEMORY.md）：无 `source` 列 ⇒ 不做 `BreakdownSelect`；单接口 ⇒ 不照搬 per-chart 取数层；
  无 i18n/shadcn/lucide/vueuse/draggable ⇒ Configure 重排用上移/下移按钮。

### B. 交付（17 个 commit，`43fb435` → `f44c634`）

- **统计**：后端 8 条新序列（`43fb435`，`core/stats.py` + `core/db.py`，**全部跟随 `library_id`、既有 16 键一个未删**；
  `tests/test_stats_charts.py` 295 行）；前端图表基建（`604ddd9`：`lib/charts.ts` 唯一注册入口 + 三件容器组件）→
  格式化与栅格（`b12bcf9`）→ 书库侧 5 张（`7043dd1`）→ 阅读侧 5 张（`f61ab7d`）→ 接进页面 + Configure（`e4c4540`）。
  两分区各 5 张，实测 `inst:5`/`withSvgPath:5`、`ext: []`、`errs: []`。
- **外观**：Layout 页做实（`882beef`）、Behavior 页做实（`36e0c8e`），`settingsNav` 由 `placeholder` 改 `ready`；
  偏好落 `stores/displayPrefs.ts`（并入 `appearance` 块）与 `shelfPrefs.ts`（localStorage）；**Icons 主动不做并如实标注**。
- **小项**：作者排序名（`4bfe94b` 后端 + `0b8a92d` 前端 + `c59a649` 契约同步）、无头像筛选（`c3367d2`）、
  审计日志按操作者筛选（`5b47013` + `f6f4218`）、侧栏假按钮接真实路由 + 收藏夹失败提示（`df59689`）、
  generic 书源取消注册（`26795f9`）、陈旧文案与注释订正（`f44c634`）。

### C. 验证

- **后端全量 pytest：388 例 / 1 failed**（第31期 346 例），唯一失败仍是既有 win32 不通那例；本期新增 4 个测试文件全绿
  （`test_stats_charts` / `test_author_sort_name` / `test_logs_actor` / `test_sources_registry`）。
- **前端**：`type-check` / `build` / `deploy` 全通过，产物同步 `novelforge/static/v2`；e2e（8795 独立端口 + 临时目录 +
  playwright）覆盖统计页两分区、Configure 显隐排序持久化、外观两页改完立即生效、侧栏两按钮落到 `/tools/libraries`、
  收藏夹失败提示无「演示动作」前缀、`/api/logs?actor=` 过滤生效且不传参时输出与改前一致。

### D. 文档收口（4 份）

- `roadmap-gaps-remaining.md`：**补第 31 期实施记录**（6 个 commit 只有标题没正文，记忆是唯一一手来源）+
  第 30 期两处「待运行」订正 + 第 32 期实施/验证记录。
- `bookorbit-capability-gap.md`：11 条改判 + 锚点系统性重取（`server.py` +54 行、`db.py` +99 行、`stats.py` 230→386 行
  整节重排旧锚点全失效）+ §0.3 路由数 260→262 + §15 两条（含新立「前端图表零外部请求」）。
- `roadmap-verification.md`：第 0 期「审计日志」行**就地更正**（原写「`/api/logs` 支持 actor 过滤」是把计划当实证）
  + 结论段补记 + 外观行改判。
- `bookorbit-settings-inventory.md`：§2.5/§2.6 共 13 行「本项目可行性」列回填 + 两处落地段 + §5.2 映射行。
- 锚点抽查：一次性核 53 条 → 修 3 处真漂移（`router/index.ts` entities `:205` 而非 `:206`、
  `settingsFields.ts` `:72` 而非 `:71`、`db.py` 迁移块 `:432` 而非 `:431`）。

### E. 踩坑（可复用）

- **文档里写错一句事实**：`upload.max_bytes` 被我写成「没做成维护页可编辑项」—— 只看 `settings.json` 没查
  `EDITABLE` 白名单。**判断「有没有页面入口」要两头查**（白名单 + 前端字段定义）。
- **带 CRLF 断言的替换脚本会留下裸 LF**：Read 工具保持 CRLF，但脚本里手写的换行会写成裸 LF ⇒
  写完必须复检 bare-LF（本轮 `roadmap-verification.md` 就中了一次）。
- 抽查脚本的**期望子串写得比锚点更严**会产生假警报（区间端点、注释行起点都算命中）⇒ 判定前先打印实际行内容。
