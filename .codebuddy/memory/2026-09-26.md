# 2026-09-26（第 54 期：语义向量 + 精确阅读位置）

## 背景
- 第 53 期（演播者）落地后，上游镜像主线已扫空（module-inventory「值得做」全落地、roadmap/capability-gap 未勾项 0）。用户拍板重开两项判「不做」的能力：`embedding` 语义向量 + `position-converter` 阅读位置换算。
- 计划确认时明确范围边界：kobo span / kepub DOM 不入本期；Kobo 同步仍不做。

## 落地（两能力均「彻底落地」闭环）
1. **语义向量**：`core/embed.py`（LSA 默认：TF-IDF+SVD 纯 numpy 离线；词表 df 封顶 4096 + 显式定序保证确定性；简介进语料按 0.25 降权）+ 可选本地 transformer（`CACHE_DIR/embedding-model`）；`book_embeddings` 表进 REMAP/ORPHAN；`POST /api/embeddings/recompute` + `/similar` 缺向量/扫描完成两条自愈钩子（单飞闸+600s 节流，`server.wait_embed_refresh` 进 conftest 收尾清单）；`recommend.similar_books(vectors=)` 逐对回落词袋、实质重合门不动。
2. **精确位置**：`core/epub_cfi.py`（CFI 生成/解析/兼容层，`xml.etree` 解析不引新依赖；**字符偏移=textContent 坐标系**，后端 ET text/tail 模拟 DOM childNodes）；`progress.cfi` 列 + 老库迁移；进度端点 `offset` 进/`cfi`+`offset` 出；`ReaderView.vue` 保存带 offset、恢复优先 CFI 反解（回落百分比反推）；`koreader.to_nf` 兼容识别 epubcfi 取章序号；**`from_nf` 刻意保持章首 XPointer**（kosync 只认 XPointer，相对原计划的保守取舍，已记入代码注释与文档）。
3. 依赖：`requirements.txt` 新增 `numpy>=1.26`（venv 已装 2.5.3，pip 走清华镜像）。

## 验证
- 后端全量 **820 passed / 0 failed**（基线 794 + 本期 26：test_embeddings 14 + test_epub_cfi 12）；新增测试曾抓出 3 个真问题：① 解析侧坐标空间 bug（下钻后局部重算 ⇒ 往返回不去，改为整章文本块表按全局 gstart 匹配）；② 隔离夹具不预建 OUTPUT_DIR ⇒ ebooklib 写失败只发 UserWarning、文件静默缺失（测试 helper 补 mkdir，教训进 MEMORY）；③ `test_reading_state.py` 进度归零整 dict 相等断言因响应加法演进（恒带 `cfi`）更新。
- 前端 type-check / test:unit 62 / build / deploy 全绿；`check_doc_anchors.py` 硬错 0（疑似漂移 37，按先例锚点披露）。

## 文档/记忆
- module-inventory：§2 第 27/46 行、§4.2 两行、§9 两行改判（embedding 已覆盖；position-converter 已覆盖·子集）+ 新增「十三、第 54 期更新」+ 锚点披露；roadmap 追加第 54 期记录；MEMORY.md 精简重写（53 期压缩、新增 54 期铁律、基线 721→820、补 Remove-Item 整批中止/夹具不预建目录两条新坑）。

## 未尽事项
- 无（冒烟与提交推送已完成，见下）。

## 浏览器运行时冒烟（playwright-cli + Edge，2026-09-26）
- 环境：临时 CONFIG_DIR/OUTPUT_DIR/LIBRARY_SOURCE_DIR + uvicorn 8123 + epub_builder 造 3 本真 EPUB；书库经 API 手建（铁律：书库用户手建，source_root 不会自动建库）。
- 结论全绿：① 登录页 29 条 401 属未登录预期，登录后详情页/阅读器**控制台 0 新增错误**；② 详情页「相似书」正确渲染语义候选（同作者门放行，语义向量参与打分）；③ **UI 端到端 CFI**：阅读器滚动事件 → 防抖保存 → 服务端生成 `epubcfi(/6/2!/4/1:0)` 落库 → GET 反解 offset 同值；reload 后恢复到正确章节。冒烟局限：单屏短章无溢出 ⇒ scrollTop 恒 0，章内滚动的视觉恢复无法在 UI 层区分（精度由契约测试钉住）。
- 注意：`epub_builder` **不写 tags（dc:subject）** ⇒ 造书验证「题材重合门」不可行，用同作者替代；「冒烟测试书」被门正确拦截（无实质重合不返回）。
- 提交：单笔 `02b3515`（53+54 期合提：db.py/server.py 跨期混合，hunk 级拆分风险大于收益）；`.vscode/settings.json` 的 `python.pythonPath` 机器路径**刻意未提交**；推送经代理 `127.0.0.1:7897` 一次成功（直连 github 超时）。

## 第 55 期（同日）：新增书库就地弹窗 + TXT 阅读

用户提五项新需求并拍板分期（55=②+⑤、56=③、57=①+④）；zlibrary 专用下载已说明不做（盗版源）。
- **② 就地弹窗**：新 `stores/libraryWizard.ts`（全局单实例；App.vue 挂一份 `<LibraryWizard>`；`show()` 现拉 `/api/libraries`、`created()` 刷全局实体 + 宿主回调），7 处入口改调 store（书架空态新增 `createLib`、提示条、引导 CTA `cta.to` 变可选、侧栏新增、本地转换/收书目录 0 库文案、探索 toast/desc 去设置页指向）；`?new=1` 经 store 仍可用。契约测试改判 + 新 spec 4 例。
- **⑤ TXT**：`core/detect.py` 确认是分章唯一真值源（补 7 例契约：含「首边界前内容被丢弃」与「正则非锚定、正文含『第 N 章』也算边界」两条现状口径，并把 docstring 订正）；新 `core/txtcache.py` 派生 EPUB 缓存（`CACHE_DIR/txt-epub/<id>/`，源指纹锁定形态、失败标记回落原生分章、原子落盘）；`epub_builder.build_epub(..., nav=False)` 新开关让派生 EPUB 索引 0 基与原生对齐；`library.book_detail` + `/chapter/{index}` 打通 TXT；`BookDetailView.canRead` 放行 TXT。**前端阅读器零改动**。
- 过程中两个真缺陷（测试驱动发现）：① 派生 EPUB 因 `epub_builder` 默认把 nav 放进 spine，导致首"章"是目录页且索引整体偏 1 ⇒ 加 `nav` 开关解决；② 我的测试正文里写了「第1章的正文内容」⇒ 被非锚定正则当边界，反过来暴露并钉住了这条口径。
- 验证：后端全量 **833 passed**（820 + 13）；前端 66 单测 + type-check + build + deploy 全绿。
- 另：本轮中途工具返回出现过一批**与仓库不符的失真输出**（自相矛盾的测试计数、虚构文件内容），已用 `git status` 权威核验并复跑确认真实状态；后续凡关键结论一律以 git/复跑为准。

## 第 56 期（同日）：多设备进度提示 + 偏好同步感知
- **进度**：后端 `GET/PUT /api/books/{bid}/progress` 补 `updated_at`（`db.set_progress` 改为返回写入时间戳；无行时不返回，避免造 0 基准）；`ReaderView` 记 `ownWriteAt` + 8s 轮询（hidden 不轮询）⇒ 「时间戳更新且位置不同」才显示提示条，**点「跳过去」才跳**并立刻写回，点「忽略」不动；提示条插在进度条与正文之间、不碰正文 DOM。
- **偏好**：用设备行 `last_seen` 当变更信号（零 schema 改动）；判定抽纯函数 `prefsSyncDecision`（noop/apply-remote/conflict，1s 容差）；`init()` 起 15s 轮询；conflict（本机有未推送改动）**只提示不覆盖** → 顶栏胶囊；apply-remote 时顶栏显示「已同步其他设备偏好」8 秒。
- 测试：后端 `test_progress_updated_at.py` 4 例；前端新增 `prefSync.spec.ts`（5 例纯函数）与 `ReaderView.remoteProgress.spec.ts`（4 例）—— ⚠️ 该 spec 只 fake `setInterval`/`clearInterval`/`Date`，保留真 `setTimeout`（否则 flushPromises 自挂）。
- 验证：后端全量 **837 passed**（833+4）；前端 **75** 单测 + type-check + build + deploy 全绿。

## 第 57 期（同日，A 段）：元数据提供商目录 + 设置页「提供商」1:1 重做
- 用户拍板三项（插件市场真在线 / 轮询提示 / TXT 双路线）与本期范围；**zlibrary 专用下载不做**（说明后以通用书源+投递替代）；出网口径修订为「默认零外部，仅插件市场与已启用元数据源显式出网」。
- 落地：`metasources.SOURCES` 扩为上游 14 家目录（四组 + `implemented/needs_config/key_field/config_hint`）；新增 `provider_catalog()` / `is_implemented()`；`GET /api/metadata/providers`（分组 + 已启用计数 + needs_setup）；probe 跳过未实现（零外呼）；`metafetch.plan`/`online_candidate` 只选已实现源；`MetadataPage.vue` providers 区块照截图重做（分组/徽标/过滤器/搜索/开关，未实现渲染 `—`）。
- 契约 `tests/test_metadata_providers.py` 8 项全过（含 `IMPLEMENTED == _FETCHERS.keys()` 一致性、探测零外呼 spy、配置混入未实现源 plan 不炸）。
- B 段（下一段）：手动书源表单化 + 声明式插件市场 + 系列级元数据补全。

## 第 57 期（同日，B 段）：14 家提供商全部接入 + 书源表单化；插件市场取消
- 用户拍板：14 家全写（免 Key 直用 / 填 Key 可用 / 抓取型标「易失效」）；页面仍叫「元数据来源」内部保留「提供商」分组；**插件市场取消**；书源做表单化 + 逐源自检。
- 落地：`metasources` 新增出网收口 `_get_json`/`_get_text`（契约测试唯一注入点）+ 12 家 fetcher（iTunes / AudNexus / RanobeDB / Hardcover / ComicVine / Aladin / Amazon / Goodreads / Kobo / Audible / Libro.fm / Lubimyczytac）；注册表 14 家全 `implemented` + `fragile`/`key_field`；`options_for` 统一密钥拼装（metafetch / series_meta 共用）；`_mask_metadata_fetch` 按注册表循环掩码；`/api/metadata/probe` 对缺密钥的家零外呼；页面加「易失效」徽标 + 密钥区按注册表渲染 + 清掉插件市场措辞；书源侧新增 `POST /api/sources/test`（不落盘试搜）+ `SourcesView` 三段式表单与逐源「测试」。
- 顺手修掉三个真实缺陷：全称语言未归一（"english"）、`_first_json` 吃不掉尾随分号、列表混入非 dict 元素会抛。
- 测试：`test_metadata_providers` 改写 + 新增 `test_metasources_parsers`(14)/`test_sources_test_endpoint`(6)；后端全量 **870 / 0 failed**；前端 75 单测 + type-check + build + deploy 全绿。
- 未做的确认项：声明式插件市场（用户取消）；6 家抓取型的真实可用性无法离线验证（已如实在页面/文档标注）。
- 浏览器冒烟（playwright-cli + 系统 Edge；`CONFIG_DIR/INPUT_DIR/OUTPUT_DIR` 指向 `.codebuddy/tmp-smoke` 隔离，admin/changeme 登录，结束已清理）：① 元数据来源页 `#/settings/metadata/providers` —— 14 行全给开关（`button[role=switch]` = 14）+ 14 个官网链接、四个分组标题、6 行「易失效」徽标、密钥区 4 个输入框（googlebooks/hardcover/comicvine/aladin）、「需要设置」过滤后只剩 3 行（三家需密钥）；② 工具 → 书源 `#/tools/sources` —— 表单 31 个控件、六段字段齐全，点「测试（不保存）」如实回「当前表单：未通过」+逐条校验错误，之后 `/api/sources` 仍只有 `gutenberg`、`SOURCES_DIR` 下 0 个 json ⇒ **不联网、不落盘**得到实证。
- 注：设置页真实路径是 `#/settings/<page.path>`（如 `#/settings/metadata/providers`），**不带分组段**（我曾误加 `library/` 段导致「未知路由」）。

## 修复（同日）：提供商目录在旧后端下静默变空
- 用户截图反馈「已启用：2/0 + 没有匹配的提供商」的空白列表。根因：其运行中的**后端进程仍是旧版本**（没有第 57 期新增的 `/api/metadata/providers` → 404），而前端 `loadProviders()` 的 `catch {}` 静默吞错 ⇒ 目录为空。
- 修复 `MetadataPage.vue`：失败时 ① 回落旧接口 `/api/metadata/sources` 保住列表可操作；② 页面上写明原因（含「重启后端进程」）+「重试」按钮；③ 总数改 `providerTotal` 兜底（不再 `N/0`）；④ 空态区分「加载中 / 无匹配 / 目录为空」。
- 验证（playwright + Edge + 隔离配置）：mock `**/api/metadata/providers` = 404 → 提示条出现、仍有 14 行可操作、徽标 2/14；撤 mock 重载 → 14 开关 / 4 密钥输入 / 4 分组 / 易失效 / 无提示条。前端四连全绿。
- 给用户的动作项：**重启后端**（源码运行重启 uvicorn；Docker 运行需重建/重启容器）后刷新页面。

## 第 57 期（同日，C 段）：系列级元数据四字段编辑入口
- 核查结论：后端本就齐备（`series_meta.FIELDS` 四字段 + `state()` + `POST /api/series/{name}/meta`），缺的只是前端入口（面板原先只能编辑简介）。
- 落地：`SeriesMetaPanel.vue` 重写（四字段一起保存；逐字段「本地」徽标 + 「↺ 恢复」；未覆盖时标来源「成员书聚合 / 在线」；空值也留恢复口子；首发年 4 位格式闸门）；`SeriesDetailView` 透传 `meta_state`；新增 `SeriesMetaPanel.spec.ts` 7 项（盯 payload：四字段同发 / 单字段恢复只发单字段 / 非法年份不发）。
- ⚠️ 新 spec 当场抓到真回归：重写时漏掉「简介 → 恢复在线」入口（已补回）。
- 另做后端离线往返：四字段覆盖 → 单字段清除（其余保留）→ 题材顿号解析，均符合 spec 假设。
- 验证：前端 82 单测（75+7）+ type-check + build + deploy 全绿；后端未改。

## 第 57 期 C 段补验（同日）：系列页编辑面板真浏览器端到端
- 本轮**只做验证、不改代码**。做法：隔离环境（`CONFIG_DIR`/`INPUT_DIR`/`OUTPUT_DIR`/`LIBRARY_SOURCE_DIR` 全指向 `.codebuddy/tmp-e2e`）+ 一次性播种脚本（`epub_builder` 造 2 本 EPUB → `POST /api/libraries` → `/scan` → 用 `POST /api/books/{bid}/metadata` 打 `series`/`series_index`），把系列页跑起来。
- 踩坑：`POST /api/libraries` 的 `source_dirs` **必须在来源根之内**（`LIBRARY_SOURCE_DIR`），放到根外直接 400；`epub_builder` **不写 series**（`komga.py:17`），所以系列只能用元数据接口打，不能指望造书器。
- 实测结论（playwright + Edge，四条路径全过）：① 编辑四字段保存 → 落库 `publisher=新星出版社 / first_year=1951 / tags=['科幻','太空歌剧']`（顿号串解析 ✓）、`overridden` 四字段全 true；② 点出版社「↺ 恢复」→ `overridden.publisher=false` 且 **首发年/题材/简介的覆盖原样保留**、恢复按钮 3→2、界面该行消失；③ 点简介「↺ 恢复在线」→ 只清 description、界面回到「未找到可信的在线系列简介」空态、其它字段不受影响；④ 空态文案如实（不编造简介）。
- 结论：C 段 UI 的 payload 级 spec 与真实交互行为一致，上一轮申报的「未做浏览器验证」缺口已关闭。

## 第 57 期（同日，D 段）：凭据搬到提供商行内（用户反馈）
- 用户要求：密钥位置**列在对应的提供商旁边**（对齐上游：每行「配置 ▾」→ 行内密钥输入 + 「测试」），不要再集中堆在页面底部。
- 落地：注册表补 `key_label`/`key_placeholder`（googlebooks/hardcover/comicvine/aladin）；`/api/metadata/probe` 支持 `keys`（按源 id 的**临时凭据覆盖，不落盘**）；`MetadataPage` 每行加「配置 ▾」+ 行内面板（警告条/密钥输入/测试/保存/清除），删除底部密钥区；`api.ts` probe 签名扩 `keys`。
- 真浏览器实测：4 个配置按钮、Hardcover 面板齐全；**填草稿点「测试」真的出网**（Hardcover 401 如实回报）且配置未写入；保存 → 落库掩码回显 + 输入框清空 + 显示「已设置」+ 出现「清除」；清除 → 退回未设置。
- 教训：页面上有多个同名「保存」按钮时，`getByRole(...).first()` 可能点到别处 —— 必须用面板内 scoped 定位（我因此误判过一次「保存无效」）。
- 测试：后端 `test_metadata_providers` 13 项全过（新增「行内测试传 keys 用 keys / 未传沿用已保存 / 都不落盘」+ 文案字段齐备）；前端 82 单测 + type-check + build + deploy 全绿。
