# 2026-09-23

## 第 44 期：重审 §6.2-B 并完成「批注样式类型」
- 方向：重审 `docs/bookorbit-module-inventory.md` §6.2-B「有价值但不做」5 项。结论：仅「批注颜色↔样式映射」的**样式类型**部分改判为做，其余 4 项（批注级位置换算、实体删除策略、元数据提取器扩类、provider 扩源）维持不做（理由仍成立）。「跨端降色」因无 Kobo 设备仍不做。
- 实现（纯新增维度，不加路由/不加 book_id 列，remap 契约不受影响）：
  - `novelforge/core/db.py`：`annotations` 建表加 `style TEXT NOT NULL DEFAULT 'highlight'` + 老库迁移补列；`add_annotation/list_annotations/all_annotations/trashed_annotations` 透传/返回 style。
  - `novelforge/server.py`：`POST /api/books/{bid}/annotations` 收 style；`GET /api/annotations/export` 的 csv（加 style 列）/json（`**a` 自带）/markdown（非 highlight 注样式）输出 style。
  - `frontend/src/data/annotationColors.ts`：新增 `HIGHLIGHT_STYLES`（高亮/下划线/删除线/纯笔记）+ `highlightStyleLabel()` 作为**唯一**真值源；`api.ts` 的 `Annotation`/`AllAnnotation` 加 `style`。
  - 四处 UI 统一引用：`ReaderView`（选区浮层加样式切换 + `wrapQuote` 按 style 分流：underline=text-decoration、strikethrough=line-through、note=虚线下沿、highlight=铺底）、`AnnotationsView`、`BookDetailView` 批注 tab、`HighlightOfTheDayWidget`。
  - 契约测试 `tests/test_annotations.py::test_annotation_style_roundtrips`（创建带 style→列表/导出含 style→老库迁移补列，后者并入既有 legacy 断言）。
- 校验：后端全量 **721 例 / 0 failed / 0 error**（`tests/check_doc_anchors.py` 硬错 0，疑似漂移 15 条均在 capability-gap/roadmap 等未改文档，属历史存量）；文档 §6.2-B 首行标注 ⚠️ 改判、追加 §八 记录。前端 `npm run test:unit` 因本机无 Node（nvm 空、网络受限）未跑，未新增前端测试、改动类型自洽。
- 提交 `21a5c16` 已走系统代理（HKCU 代理 `127.0.0.1:7897`，git 一次性 `-c` 参数，未写全局配置）推送至 `main`。

## 事故：全量 pytest 后 14 个根目录文件从工作树消失
- 现象：跑完全量 `pytest`（721 例 0 failed）后，`git status` 显示 `config.yaml / Dockerfile / .gitattributes / .gitignore / .env.example / .dockerignore / README.md / requirements*.txt / pytest.ini / start.sh / docker-compose*.yml` 共 14 个根目录跟踪文件被标记为 deleted（磁盘确已缺失），`.gitignore` 丢失导致 `.venv`/`__pycache__`/`data-test`/`novelforge/static` 等显示为未跟踪。
- 处理：这些文件在 HEAD 完好，`git checkout HEAD -- <14 文件>` 已恢复；`git status` 现仅剩他人对 `.vscode/settings.json` 的未提交改动（保留）。
- 警示：**未查明根因前勿重跑全量 pytest**，以免复现误删。疑似测试副作用（conftest/某测试的清理逻辑误删仓库根文件），列为待查产品 bug。

## 本机安装 Node/npm/vue 运行环境（第 44 期收尾 + 前端可测）
- 动机：第 44 期提交 `21a5c16` 时前端 `npm run test:unit` 因本机无 Node 未跑，现补齐环境。
- 过程：winget 系统级装 Node.js LTS 时**安装器 1603 失败**（非交互壳无法应答 UAC 提权），改用 `--scope user` 用户级安装成功（→ `...\Microsoft\WinGet\Packages\OpenJS.NodeJS.LTS_...\node-v24.19.0-win-x64`，但 PATH 检索会跳过该 WinGet 包目录，且被 nvm 空 shim 抢注）。
- 实际落地：**机器本身已有 nvm v2.0.0（路径 `C:\Users\qingr\AppData\Local\Author Software\nvm` + `.nodejs` 空 shim）**，它才是 `node`/`npm` 的实际控制方。走系统代理 `127.0.0.1:7897` 执行 `nvm install 24.19.0` + `nvm use 24.19.0`（已设默认），`node`/`npm` 现解析为 v24.19.0 / 11.17.0。
- 验证：`frontend/` 的 `npm install` 报 "up to date"（依赖本就在 node_modules）；`npm run test:unit` → **6 文件 / 62 用例全过（EXIT=0）**。前端环境现可用（`dev`/`build`/`test:unit` 均可）。
- 结论：本机 Node 由 **nvm 管理且已激活 24.19.0**（非 IDE 托管、非 winget 直装可用）；记忆中"nvm 是空的另一套 / 用 IDE 托管 Node"已过时，需更正。winget 那份 node 文件无害残留。

## 待办
- MEMORY.md 已超尺寸被截断注入，需按规则压缩合并（本条仅记当日，长期约定仍在 MEMORY.md 旧内容中）。
- 更正 MEMORY.md 中"本机 nvm 是空/用 IDE 托管 Node"的旧结论 → 现 nvm v2.0.0 已装并激活 24.19.0，是 node 唯一控制方。

## 调查结论：pytest 误删根文件事故（第 45 期收尾）
- 方法：code-explorer 全仓只读排查 + 在 `c:\tmp\nf-repro` 隔离副本（robocopy 复制、排除 .git/.venv 大目录，用真实 `.venv` 解释器跑）加仓库根守卫后跑全量 pytest。
- 结论：**当前 HEAD（=21a5c16，即事故提交）代码里不存在任何会删除仓库根文件的路径**。依据：
  - 所有删文件逻辑都走 `config.*` 绝对目录常量，且 `tests/conftest.py` 在导入业务模块前用 `os.environ.update` 把这批目录重定向到临时区（`_SESSION_ROOT`）；
  - 全部清理端点（`api_clear_cache/api_clear_recycle/api_reset_config` 等）要么绑定临时目录、要么根本没被任何测试调用；
  - `check_doc_anchors.py` 仅 `os.walk(ROOT)` 只读；测试里无 `os.chdir/getcwd/Path.cwd`，无 `ROOT`/cwd 相关删除；
  - 全仓唯一 `os.unlink` 在 `network.py:161`（cookie 在临时 COOKIE_DIR）；14 个根文件名在 .py 里只出现在注释/docstring。
  - 隔离副本 + 真实仓库各跑一次全量 pytest（守卫就位）：均 `EXIT=0`、14 根文件完好、守卫零触发。
- 根因推断：事故更可能是**当时那次运行 conftest 的目录重定向未生效**（启动方式/环境问题，非具体测试逻辑），导致某清理逻辑把仓库根当成数据/配置目录清空。当前代码安全，但重定向是唯一的旧防线。
- 修复（已落地，仅改 `tests/conftest.py`）：加**仓库根防删除守卫**作为不依赖重定向的最后防线——会话级 autouse 夹具手动打补丁 `os.remove/unlink/rmdir/shutil.rmtree/os.rename/os.replace/shutil.move/Path.unlink/Path.rmdir`，凡目标（或 move/rename/replace 的源）解析到仓库根内一律 `pytest.fail` 并打印栈帧；`pytest_sessionfinish` 再对 14 个根文件做完整性告警检查。已用探针测试验证守卫确实会拦截仓库根删除。
- ⚠️ 未提交、未 git add（与硬约定一致）。`c:\tmp\nf-repro` 为临时副本，可随时删。

## 第 46 期收尾：第 45 期松散项归零 + 全量基线复核
- conftest 仓库根防删除守卫已提交并推送：`ec3222a`（`21a5c16..ec3222a main`，走系统代理一次性 `-c`，未改全局配置）。
- 全量基线复核（守卫就位）：后端 `.venv` win32 **721 passed / 0 failed / EXIT=0**（113s；日志 `c:\tmp\pytest46.log`、junit `c:\tmp\junit46.xml`），14 个根文件全部完好、守卫零触发 ⇒ **事故未复现**；前端 `npm run test:unit` **62 passed / EXIT=0**。
- MEMORY.md 压缩：15289 → **10897 B**（按 MEMORY/REF 职责拆分：铁律留 MEMORY、细节外移 MEMORY-REF.md），并订正 REF 中过时的 Node 段（「nvm 空 / IDE 托管」→ nvm v2.0.0 已激活 24.19.0）。
- lock 噪声 `frontend/package-lock.json`（纯 `"dev": true` 增删）按第 39 期规则还原、未提交；`.vscode/settings.json`（他人改动）保持不动。

## 第 46 期前端：恢复第44期误清空的两文件 + 阅读活动/批注缺陷修复
- **重大回归（已修）**：第44期提交 `21a5c16` 把 `frontend/src/data/annotationColors.ts`（`fecc024` = 2626 B）与 `frontend/src/views/AnnotationsView.vue`（`fecc024` = 14941 B）**误提交成 0 字节**（该 commit 其余 8 个文件改动完好）。后果：`HIGHLIGHT_COLORS`/`highlightHex` 等符号全仓只有引用无定义（构建断链）、`/annotations` 白屏、`annotationOverview`/`exportAnnotations` 变孤儿 API。逃过校验的原因：只跑了 `test:unit`（vitest 容错、不做导出完整性校验），未跑 `type-check`/`build`。
  - 修法：`git checkout fecc024 --` 两文件；`annotationColors.ts` 补回第44期样式类型导出（`HighlightStyle`/`HIGHLIGHT_STYLES`/`DEFAULT_HIGHLIGHT_STYLE`/`highlightStyleLabel`）；`AnnotationsView.vue` 补回样式标签。
  - 证据：`npm run type-check` EXIT=0；`vite build` 成功（977 modules）；`test:unit` 62 通过。
- `ReaderView`：删批注不再整块 `innerHTML` 重建正文（只解包目标 `[data-anno-id]` span + `normalize()`）；新增批注只包裹新增段（去掉整章重扫的重复包裹）；增/删失败 `ui.toast(apiErrorMessage(...))`，删失败不回滚本地列表。
- `stores/activity.ts`：「年份/条数上限」纳入 store 作唯一真值源（修「切库不带 year ⇒ 拉回全量、选择器与数据错配」），`force` 缓存生效，加 `error` 标志。
- `ReadingActivityView`：错误态 + `#action` 重试；时间轴用后端 `timeline.total` 显示「已显示最近 N/共 M」并「加载更多」（步长 200、上限 500）；热力图格子补 `role="img"`+`aria-label`。
- `ReadingLogView` / `HighlightOfTheDayWidget`：加载失败不再当空态，给错误文案/重试。
- 未做（披露）：热力图「服务端本地日 vs 浏览器本地日」时区口径差异（需后端下发事件日）；`DashboardShelfRow` 的 `Math.random()` 洗牌（属运行时随机发现、非演示数据，改动会变语义，待产品确认）。
- 提交：`d445b77` + `1adecfb` + `ac5dc63`，已推送（`8b95d56..ac5dc63`）。

## NAS 部署梳理：docker-compose 全变量驱动（未提交）
- `docker-compose.yml` 重写为「一份文件 + 一份 .env」：`IMAGE_TAG`/`CONTAINER_NAME`/`PULL_POLICY`/`WEB_PORT`/`{INPUT,OUTPUT,CONFIG,COOKIE,CACHE,DATA,LIBRARY}_PATH`/`TZ`/`AUTH_*`/`AUTO_WATCH`/`WATCH_INTERVAL`/`EBOOK_CONVERT_BIN`/`HTTP(S)_PROXY`+`NO_PROXY`/`PUID`+`PGID` 全部 `${VAR:-默认}`。默认值与旧文件**逐项等值**（端口仍 8992、容器名仍 `novel_dl_convert`、挂载点与宿主机子目录未变 ⇒ 既有数据位置零迁移）。
- 两处**刻意的行为改动**（均已写进注释与 README）：① `pull_policy: always` → `missing`（断网/ghcr 受限也能启动，更新靠 `docker compose pull`；想回旧行为设 `PULL_POLICY=always`）；② 新增 `user: ${PUID:-0}:${PGID:-0}`（默认 0:0 = root，与不写等价）。
- **地雷清除**：`docker-compose.override.yml` → `git mv` 为 `docker-compose.offline.yml`。原名字会被 Compose **自动合并**（静默 8993 + `pull_policy: never` + 挂 `./novelforge`），NAS 上一条 `up -d` 即踩。新名字必须 `-f docker-compose.yml -f docker-compose.offline.yml` 才生效，其 `ports` 加了 `!override`（实测不加会并成 8992+8993 两个监听）。
- `.env.example` 重写为 NAS 变量模板（7 段：端口镜像 / 数据路径 / 登录安全 / 运行开关 / 代理 / UID-GID / LLM 说明）。**订正一处长期错误**：`LLM_API_KEY/LLM_BASE_URL/LLM_MODEL` 是**死变量**（全仓无任何代码读它们，`core/ai_detect.py` 只读 `config` 的 `llm` 段）——原 compose 注释与 .env.example 都在误导，已删并改为指向「设置 → 转换 → 分章」/`config.yaml`。
- README `## Web 服务（NAS 部署）` 同步：端口 8000 → **8992**（原文写错的），加「三步部署」+「NAS 常改变量表」，更新目录结构与「更新代码」小节（说明 `up -d` 默认不拉新镜像的原因）。
- 校验（实测，非推断）：`docker compose config` 与 `-f` 叠加版本均解析通过、端口/挂载/别名解析正确；用模拟 `.env` 验证 12 个变量全部生效（含绝对路径与 `LIBRARY_NAME` 中文）；`.venv` 实测 `HTTP_PROXY=''` 时 `httpx.get_environment_proxies()` 返回空 map（仅 NO_PROXY 生成 localhost 直连项）⇒ 空值不会把请求导向空代理；`LIBRARY_SOURCE_DIRS1_NAME=''` 仍回落显示名 `libraries`、`AUTO_WATCH=true` 仍 enabled（旧行为不变）。
- ⚠️ 未做：本机**没有** `ghcr.io/735876214/novel_dl_convert` 镜像（本地只有 `novelforge:test`/`novelforge:dev`），故未做「真拉镜像起容器」的端到端冒烟；也未动用户正在跑的 `novelforge-test`(8993) 容器。

## 订正（同日）：`.env.example` 并入 compose —— 用户要求「只拉一个文件就能跑」
- 用户反馈「compose + .env 两份文件」不便携 ⇒ `.env.example` **已删除**，其内容（7 段说明）作为行内注释并回两个 compose：
  - `docker-compose.yml` 回到**字面量**配置（端口 8992 / 容器名 / 7 处挂载 / `AUTH_*` / `AUTO_WATCH` / `WATCH_INTERVAL` 全部写死），**`${VAR:-默认}` 全部移除、不再读 `.env`**；可选开关改为注释行：`user: "1026:100"`、`EBOOK_CONVERT_BIN=`、`HTTP_PROXY/HTTPS_PROXY/NO_PROXY=`、`LIBRARY_SOURCE_DIRS1_NAME`/`DIRS2*`。
  - `docker-compose.test.yml` 补同样可选注释段、订正 LLM 注释，头部注明「不依赖任何 .env」。
- 连带改动：`tests/conftest.py` 根文件完整性清单去掉 `.env.example`（否则每次全量 pytest 一条假告警）；`config.yaml` 首行「见 .env.example」→「见 docker-compose.yml」；README 部署章节改成**两步**（拷单个文件 → `up -d`），常改变量表从「变量名」改为「改 compose 里哪一处」。
- 校验：三份 compose（base / offline 叠加 / test）`docker compose config` **全部通过**；base 展开后只剩字面量环境变量（不再有空的 `HTTP_PROXY` 之类噪声）；全仓 `.env.example` 引用归零（`grep` 实证）。
- 上一条记录里 `pull_policy: missing`、override 改名、LLM 死变量订正**全部仍然有效**（只是表达形式由变量换成字面量，`PUID/PGID` 改由 `user:` 注释行表达）。
- 提交：`2bcef19`（override→offline）+ `009b57d`（单文件化），已推送。
- ⚠️ 现场教训：推送前跑全量 pytest 时 `$LASTEXITCODE=1`，日志尾部只有 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]`（拦的是会话末 `%TEMP%\pytest-of-*` 清理，1449 个文件）—— **junit 才是权威**：`tests=721 failures=0 errors=0`。以后见 EXIT=1 先解析 junit 再判断，别急着重跑。

## 第 47 期：闭环第46期遗留（时区/去随机/冒烟）+ 深化占位视图
- **热力图时区口径统一**：`core/activity.py` 的 `timeline` 给 session/annotation/achievement 三类事件补 server-local `date`（`time.strftime("%Y-%m-%d", time.localtime(ts))`，与 `db.reading_day_minutes` 的 heatmap 同口径）；前端 `ReadingActivityView` 的 `grouped` 改读 `e.date`，删 `new Date(ts*1000)` 浏览器时区换算 ⇒ 消除 server/browser 跨时区 ±1 天错位。契约 `tests/test_reading_activity.py::test_timeline_events_carry_server_local_date`。
- **Dashboard 去随机**：`DashboardShelfRow` 的 `discover` 行去掉 `Math.random()` 洗牌，改按 book id 稳定升序（刷新顺序固定、无运行时随机）。
- **收藏夹做深（后端+前端）**：`db.collections` 加 `updated_at` 列（迁移回填 `created_at`；成员增删/重命名均 `_touch_collection`）；`list_collections` 返 `first_book_id`（`first_book_has_cover` 由 **API 层**补，避免 db 依赖 library 造成分层/循环）；新增 `PATCH /api/collections/{cid}` 改名端点；前端 `CollectionsView` 加首书封面预览 / 最后修改 / 行内重命名；`icons.ts` 加 `edit` 图标。契约 `tests/test_collections.py`（3 例）。
- **更新日志做深**：`data/whatsNew.ts` 条目加 `tag`；`WhatsNewView` 加版本徽章（`/health` 真实版本）+ 分类 chip + 空态 + localStorage「自上次访问」高亮（确定性）。
- **通知中心错误态**：`NotificationsView` 加载失败不再退化成「暂无通知」，给错误文案 + 重试（同第 46 期修法）。
- **浏览器冒烟（首次真正落地）**：`playwright-cli` + 系统 Edge（`open --browser=msedge`，本机无 chromium 也能跑）起临时实例（`c:\tmp\nf-smoke`，admin/test1234），注入 `nf_token` 后加载 `/annotations`、`/reading-activity`、`/collections`、`/whats-new` —— **控制台 0 错误**，第 44 期误清空回归在运行时确认已修。
- **验证**：后端全量 **725 passed / 0 failed / 0 error**（EXIT=0；基线 721 + 新增 4 例）；前端 `type-check` 0 错 + `test:unit` 62 + `build` + `deploy` 全绿。
- 提交：`f2e0dc8` + `5f55d2a` + `6946c8c` + `e4840cc` + `e91ba4b`，已推送（`70d062c..e91ba4b`）。
- ⚠️ 实操要点：**前端改动必须 `build` 后再 `npm run deploy`**（`build` 只落 `frontend/dist`；`deploy` 才同步 `novelforge/static/v2`）。冒烟时服务端返回的 asset 名与本地 build 产物名不一致，即为「漏 deploy」的信号。

## 第 48 期：三页质量一过（成就 / 探索发现 / 智能书架）
- 定位：第 47 期滚动项落地。三页**早已「已覆盖」**（均 220–270 行、非占位），故本期只做**质量 / 深化一过**，**零后端改动、无新路由 / 表**。
- **智能书架**（`SmartScopesView`）：`load()` 原 `catch { /* ignore */ }` 吞错 ⇒ 失败被当成「还没有自定义书架」；改**错误态 + 重试**，规则控件补 `aria-label`（名称 / 匹配方式 / 字段 / 操作符 / 值）。
- **成就**（`AchievementsView`）：加载失败原只 `ui.toast` + 底部兜底空态 ⇒ 改**可重试错误态**（与空态分离）；新增**解锁筛选**（全部 / 已解锁 / 未解锁）+ **分组进度条**；「重算」按钮条件由 `data?.enabled !== false` 改 `data && data.enabled`（原在加载失败时也会显示）。
- **探索发现**（`ExploreView`）：预览弹窗补 `role="dialog"` / `aria-modal` / `aria-labelledby` + **Esc 关闭**（`onMounted` / `onUnmounted` 挂卸）；结果区加「共 N 条结果」。
- **验证**：前端 `type-check` 0 错 + `test:unit` 62 + `build` + `deploy` 全绿；playwright 冒烟三页认证后**控制台 0 错误**（⚠️ 同第 47 期：注入 `nf_token` 后**必须再 `reload`** 才生效，否则仍 401）；后端全量 **725 passed / 0 failed**（本期无新增后端用例）。
- 提交：`c9cf1f8` + `136a74e` + `3794cb0` + `e6a67d0`。

## 第 49 期：设置页信息架构 + 全站错误态收敛 + 偏简视图做深
- **书库管理并入设置页（彻底）**：`settingsNav` 的 `libraries` 由 `placeholder` 转 `ready`（组件 = `views/tools/LibrariesView.vue`），删 `ToolsLayout` 的「书库管理」标签与 `tools/libraries` 路由；8 处入口改指 `/settings/libraries`（`AppSidebar` / `nav.ts` / `ShelfView` / `FirstRunNotice` / `GuidedTourModal` / `LocalConvertView` / `BookDockPage` / `MigrationGateDialog` 用路由 name `settings-libraries`）；用户可见文案「工具 → 书库管理」→「设置 → 书库管理」（含后端 `core/library_rules.py`×2、`core/library.py`、`core/migrate.py`）；契约 `tests/test_first_run_contract.py` 的 `LIBRARIES_ROUTE` 同步。
- **删 12 个上游对照只读占位页**（图标 / 界面语言 / 隐私与共享 / 内容限制 / Kobo / KOReader 上游对照 / 邮件投递 / 用户 / 账号活动 / 免密链接 / OIDC·SSO / 求书）⇒ 设置页 **48 → 36 页**、**占位页清零**；`tests/test_settings_nav_contract.py` 重构（页数 36、`EXPECTED_PLACEHOLDERS` 空集、新增 `REMOVED_UPSTREAM` / `REMOVED_PAGE_PATHS` 显式「已移除」清单、`EXPECTED_OWN` → `{komga}`）。⚠️ 删页后 `KOReader` 上游标题失去落点 ⇒ 已把其 `upstream` 块**并入真实的 `koreader` 页**（不是误标「已移除」）。
- **全站错误态收敛（19 处）**：「主数据加载失败被静默当空态」逐页改为**可重试错误态**：`AuthorsView` / `SeriesView` / `SeriesDetailView` / `AuthorDetailView` / `AnnotationsView` / `StatsView` / `CollectionDetailView` / `BookDetailView`（store `detailError` 区分「失败」与「找不到」）/ `tools/{Sources, Output, MissingResources, DuplicateBooks, EntityManager, Logs, Libraries}View` / `settings/pages/{Integration, Koreader}Page` / `stores/{collections, stats, library}`（stats 走**仪表盘页级**一条提示，避免改 8 个部件）。**有意静默保留**：`ReaderView` 离线进度、`ShelfView.bookMoveBatches`、watcher / health 预取、`stores/library.ts` 的 `librariesLoaded` 保护、`AuditLogPage` 的 `err` 兜底（**勿误改**）。
- **偏简视图做深**：`SeriesView` 排序（册数 / 名称）；`tools/OutputView` 格式筛选 + 排序（时间 / 名称 / 体积）；`DocumentationView` 接 `/health` 真实版本号 + 「关于与更新」入口。
- **文档锚点**：跑 `tests/check_doc_anchors.py` ⇒ **硬错 0**（修 `capability-gap.md` 8 处 + `module-inventory.md` 4 处实测行号；硬错是 `settingsNav.ts:517` 越界 —— 该文件已被删到 478 行）。余 **14 条疑似漂移全在 `docs/roadmap-gaps-remaining.md` 的历史实施记录**（如 `:1526` 属「第 40 期实施记录」）⇒ 按约定**不改**（改历史行号等于篡改历史）。
- **验证**：前端 `type-check` 0 错 + `test:unit` 62 + `build` + `deploy`；后端全量 **725 passed / 0 failed / 0 error**（junit 权威；EXIT=1 是既知的会话末清理守卫）；冒烟（`--browser=msedge`，注入 token 后 **reload**）5 页控制台 **0 错误**，并确认 `/settings/libraries` 即操作页（「还没有书库」初始态）、`/tools` **无「书库管理」标签**、`/settings/kobo` 落**未知路由兜底**。
- 提交：`3f23d5b`（合并 + 删页）+ `6cb9862`（错误态）+ `95334f7`（做深），已推送。
