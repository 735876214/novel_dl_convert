# 2026-09-23（第 42 期：上游重新取证 → 判定刷新 + 部分缺口）

## 本期定位
用户拍板「重新取证上游找新缺口」。**纯取证 / 文档期，零运行时改动**。

## 关键发现
- **上游 `735876214/bookorbit` 的 `refs/heads/main` 仍为 `c292d6cc`**（与第 33 期取证同一 commit，无新提交、无新 tag）
  ⇒ 「找新版本的新模块」前提**不成立**。`git ls-tree` 对比 `c292d6cc..FETCH_HEAD` = **0 提交 / 0 文件**；`ls-remote` 权威确认。
- 镜像 `%TEMP%\bookorbit-ref` 是 `blob:none` 部分克隆、**工作树未检出**（`list_dir` 只看得到 `packages/`），
  但 `git ls-tree` / `cat-file` 正常 ⇒ 取证别依赖工作树。

## 交付（3 个文档 commit + 1 记忆 commit）
1. `docs/bookorbit-module-inventory.md`：头部加「第 42 期更新」；**§2 判定列刷新 10 处**
   （7 项「漏项候选」→「已覆盖」——第 34–36 期其实已落地、判定列没回填；另订正 3 处**文档错误**）；
   新增**第六节**「已覆盖模块的部分缺口」14 项（三分类、逐项带锚点）。
2. `docs/bookorbit-capability-gap.md`：§0.3 基线重取（路由 **284 → 285**，第 41 期 +`GET /api/libraries/source-dirs`；
   表仍 31；`APP_VERSION="0.6.0"`）；修 **10 处活跃锚点漂移**。
3. `docs/roadmap-gaps-remaining.md`：补第 42 期实施记录。

## 三处文档错误（本期订正）
- `module-inventory` §2 第 32 行 `health`：原记「本项目有 `/api/health`」→ 真路由是 **`GET /health`**（`server.py:262`），
  **无 `/api/health`**。第 31 期早已修代码侧（并修了打错路径的契约测试），此表漏改。
- §2 第 33 行 `kobo`：原记「已实现可用子集」→ **未实现**（`README.md:113`、`capability-gap.md:435`、
  `settingsNav.ts:344-363` 占位；全仓无 `kobo*.py`）。
- §2 第 40 行 `metadata`：原记「`core/metadata.py` 对等（6 类）」→ **高估**，仅 EPUB 全解析（+ 漫画/音频结构），
  FB2 完全不支持、MOBI/AWZ3/PDF 无内容解析。

## 方法学收获（重要）
**§2 判定列是第 33 期一次性写就，第 34–36 期的落地只写进各行 ⚠️ 备注与 §4.2，没回填判定列**
⇒ 直接读表会得出「这么多还没做」的错误印象。**判定列必须与落地记录同批回写**。

## 验证
- 后端全量 pytest：**690 例 / 0 failed**（与第 41 期基线一致，纯文档无回归）。
- 文档锚点核验：**硬错 0**；疑似漂移 **26 → 11**（修掉 15 处活跃锚点），**剩余 11 条全在逐期历史实施记录内**
  （`roadmap` `:859`/`:879`/`:1077`/`:1416`/`:1526`/`:1563-1565`/`:1617`/`:1721`/`:1730`），按「历史记录不动」约定不修。

## 部分缺口候选（详见 module-inventory 第六节）
- **值得做 6**：系列缺册 / 作者排序键回填 / 书架首字母跳转 / 阅读尝试(重读) / 批注导出增强 / 系列折叠偏好上云。
- **有价值但不做 5**、**与定位或既有决策不容 6**（批注跨端同步 / 按设备重建位置 / Kobo 状态投影 / 通知清理 job / 通知 SSE / 孤儿封面清扫）。

## 顺带修正的记忆过期项
- `MEMORY.md` 第 14 条「每库来源=`LIBRARY_SOURCE_DIR/<source_subdir>`」→ 改为第 41 期的 `source_dirs` 多绝对路径。
- `MEMORY-REF.md` 运行手册「建库接口要 `root_path`」→ 改 `source_dirs`；§16「§4.2 真·不做 5 项」→ **4 项**。

---

# 第 43 期：部分缺口清账（§6.2-A「值得做」6 项落地）

## 范围
用户拍板做第 42 期取证产出的**全部 6 项**「值得做」部分缺口。本期是**能力落地期**，
后端 + 前端 + 契约测试 + 文档一并交付。

## 六项与关键取舍
1. **系列缺册**：唯一实现在 `library.series_gaps`（按 `series_index` 数字集合求 `[1..max]` 补集），
   注入 `GET /api/series/{name}` 的 `gaps`。⚠️ **无序号 / 非数字序号各自单列、不并入缺册**。
2. **作者排序键回填**：⚠️ **只写派生态列 `sort_name`，绝不动 `sort_name_local`**（写它＝冒充用户改过）。
   派生规则保守，**CJK 原样 ⇒ 跳过**。
3. **书架首字母跳转**：判据唯一源 `lib/shelfBuckets.ts`；**不做拼音**（要新增依赖且多音字会算错）⇒ CJK 归 `#`；
   桶序 A–Z 升序、`#` 垫底；桶数 <3 不显示跳转条。
4. **阅读尝试 / 重读**：新表 `reading_attempts`（一轮＝开始读→读完）。自动维护挂 `db.set_status`
   （进 reading 开轮 / finished 收尾；**搁置·弃读不动**）；`reset_reading_state` 扩为**四清**（+attempts）；
   表进了 `ORPHAN_TABLES`/`REMAP_TABLES`；补录 `backfill_attempts` 只补没轮次的书。
5. **批注导出增强**：改走后端 `GET /api/annotations/export`（markdown/json/csv + 按书/库收窄，只导活跃）；
   前端由「纯前端生成 Markdown」改为 blob 下载。
6. **系列折叠偏好上云**：新增**第 7 个偏好块 `shelf`**，⚠️ 只搬 `collapseSeries`，其余书架字段仍只存本机。

## 验证
- 后端全量 pytest：**720 例 / 0 failed**（第 42 期基线 690 + 本期 30）。
- 前端：`npm run test:unit` **62 例**（6 spec，含新增 `shelfBuckets.spec.ts` 4 例）；`vue-tsc --build` exit 0。
- 基线刷新：路由 285 → **291**（+6）、表 31 → **32**（+`reading_attempts`）。

## 环境坑（本期实测）
- ⚠️ **本机 `nvm list` 是空的另一套**；可用的 Node 是 IDE 管理的
  `C:\Users\qingr\.workbuddy\binaries\node\versions\22.22.2-3`（`versions\current` 文件记着版本号）。
  跑 npm 前须把它加进 PATH（用 `npm.cmd`）；`install_binary`(node) 在本机会 EPERM 装不上，别反复试。
- 既有契约变更：`reset_reading_state` 返回值从三键扩为四键（+`attempts`），
  `tests/test_reading_state.py` 的两处精确断言同步更新（**预期内的契约变更**，非回归）。
- ⚠️ **跑 `npm run build`/`deploy` 前必须 `$env:NODE_OPTIONS=''`**：IDE 注入的 safe-delete shim 会拦
  Vite 清 outDir 与 `deploy.mjs` 的删除（报 `checkBulkDeleteGuard` / 「No active Node.js version」）。

## 冒烟（真进程 + 真浏览器，端口 8797、独立临时目录）
另起实例（`admin/changeme`）、建库 `smoke`、造 4 本真 EPUB（系列 #1/#3、`Dune`/`John Doe`、`Apple`/`Ann Lee`）后逐项核验：
- **后端六项接口全通**：`series.gaps.missing=[2]`；作者回填 `filled=1`（`Doe, John`）、CJK 跳过；
  阅读尝试 start→finish→list（`current=null`）；批注导出三格式 200（content-type/Disposition 正确、含内容）、未知格式 400；
  `POST /api/prefs/profiles` 收 `shelf` 块成功；`reset-reading-state` 返回**四键**。
- **浏览器**（playwright + `?nc=` 强刷）：书架跳转条渲染 `A(1) / D(1) / #(2)` 三桶、行带 `data-bucket`；
  系列页显示「缺少 1 册（2 号）· 查看」；「我的记录 → 阅读尝试 / 重读」显示「第 1 轮 2026-09-23 → 2026-09-23 已完成」+「再来一遍」；
  批注页导出控件（格式下拉 Markdown/JSON/CSV + 「仅当前书库」，在「全部书库」下**禁用并给出原因**）。**各页控制台 0 错误**。
- **冒烟抓到并修掉一处文案缺陷**：阅读尝试块里误把 Markdown 的 `**自动**` 写进纯文本段落 ⇒ 页面原样显示星号
  （与「设置页 note 是纯文本插值」同一类坑）⇒ 改为「会自动维护轮次」，并重建部署。
