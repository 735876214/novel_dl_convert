# 2026-09-23（第 42 期：上游重新取证 → 判定刷新 + 部分缺口）

## 本期定位
用户拍板「重新取证上游找新缺口」。**纯取证 / 文档期，零运行时改动**。

## 关键发现
- **上游 `735876214/bookorbit` 的 `refs/heads/main` 仍为 `c292d6cc`**（与第 33 期取证同一 commit，无新提交、无新 tag）
  ⇒ 「找新版本的新模块」前提**不成立**。`git ls-tree` 对比 `c292d6cc..FETCH_HEAD` = **0 提交 / 0 文件**；`ls-remote` 权威确认。
- 镜像 `%TEMP%\bookorbit-ref` 是 `blob:none` 部分克隆、**工作树未检出**（`list_dir` 只看得到 `packages/`），
  但 `git ls-tree` / `cat-file` 正常 ⇒ 取证别依赖工作树。

## 交付（3 个文档 commit + 1 记忆 commit）
1. `docs/bookorbit-module-inventory.md`：头部加「第 42 期更新」；**§2 判定列刷新 10 处**
   （7 项「漏项候选」→「已覆盖」——第 34–36 期其实已落地、判定列没回填；另订正 3 处**文档错误**）；
   新增**第六节**「已覆盖模块的部分缺口」14 项（三分类、逐项带锚点）。
2. `docs/bookorbit-capability-gap.md`：§0.3 基线重取（路由 **284 → 285**，第 41 期 +`GET /api/libraries/source-dirs`；
   表仍 31；`APP_VERSION="0.6.0"`）；修 **10 处活跃锚点漂移**。
3. `docs/roadmap-gaps-remaining.md`：补第 42 期实施记录。

## 三处文档错误（本期订正）
- `module-inventory` §2 第 32 行 `health`：原记「本项目有 `/api/health`」→ 真路由是 **`GET /health`**（`server.py:262`），
  **无 `/api/health`**。第 31 期早已修代码侧（并修了打错路径的契约测试），此表漏改。
- §2 第 33 行 `kobo`：原记「已实现可用子集」→ **未实现**（`README.md:113`、`capability-gap.md:435`、
  `settingsNav.ts:344-363` 占位；全仓无 `kobo*.py`）。
- §2 第 40 行 `metadata`：原记「`core/metadata.py` 对等（6 类）」→ **高估**，仅 EPUB 全解析（+ 漫画/音频结构），
  FB2 完全不支持、MOBI/AWZ3/PDF 无内容解析。

## 方法学收获（重要）
**§2 判定列是第 33 期一次性写就，第 34–36 期的落地只写进各行 ⚠️ 备注与 §4.2，没回填判定列**
⇒ 直接读表会得出「这么多还没做」的错误印象。**判定列必须与落地记录同批回写**。

## 验证
- 后端全量 pytest：**690 例 / 0 failed**（与第 41 期基线一致，纯文档无回归）。
- 文档锚点核验：**硬错 0**；疑似漂移 **26 → 11**（修掉 15 处活跃锚点），**剩余 11 条全在逐期历史实施记录内**
  （`roadmap` `:859`/`:879`/`:1077`/`:1416`/`:1526`/`:1563-1565`/`:1617`/`:1721`/`:1730`），按「历史记录不动」约定不修。

## 部分缺口候选（详见 module-inventory 第六节）
- **值得做 6**：系列缺册 / 作者排序键回填 / 书架首字母跳转 / 阅读尝试(重读) / 批注导出增强 / 系列折叠偏好上云。
- **有价值但不做 5**、**与定位或既有决策不容 6**（批注跨端同步 / 按设备重建位置 / Kobo 状态投影 / 通知清理 job / 通知 SSE / 孤儿封面清扫）。

## 顺带修正的记忆过期项
- `MEMORY.md` 第 14 条「每库来源=`LIBRARY_SOURCE_DIR/<source_subdir>`」→ 改为第 41 期的 `source_dirs` 多绝对路径。
- `MEMORY-REF.md` 运行手册「建库接口要 `root_path`」→ 改 `source_dirs`；§16「§4.2 真·不做 5 项」→ **4 项**。
