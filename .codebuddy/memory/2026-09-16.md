## 书架多选 + 批量接口（第 1 期遗留项）完成

- 后端 `POST /api/books/batch`（server.py）：动作 `set_status`/`set_rating`（0=清除）/`add_to_collection`，逐本执行、单本失败不影响其余，返回 `{succeeded, failed[{id,error}]}`
- **关键坑（复用 /export 的教训）**：批量端点必须注册在 `/api/books/{bid}` 之前，否则 "batch" 被当 book_id 吞掉
- 前端 ShelfView：多选开关、三视图勾选框（系列行不参与选择——没有勾选框，选代表本只会困惑）、点卡片=切换选中不跳转、批量动作条（状态/评分/收藏夹）、全选按当前视图可见书
- 后端验证 15/15；浏览器 e2e：勾选/切换/全选/批量落库（17 本 reading 确认）/退出全过
- **并行编辑同一文件的教训（第三次）**：一次发 8 个 replace_in_file 到同一文件时，部分编辑会静默丢失（工具报错但另一部分"成功"的实际是别的编辑写入了）——同文件的多个编辑必须串行，且每次 replace 前重读确认。本次脚本块丢了两次才写进去
- 路线图剩余：统计增强 / 自定义智能书架 / 查重阈值（各自独立、量小）

## 统计增强完成（第 2 期最后一项）

- `/api/stats?days=7..365&top=1..50`（FastAPI Query 校验，越界 422；stats.py 内还有函数级收敛兜底）
- 新增字段：publishers/genres（Top 榜）、years（按十年聚合，known+unknown=total）、avg_progress、integrity（缺作者/缺语言/无封面/0字节/解析失败计数）、window
- **历史字段名 added_28d/reading_28d 保留**（dashboard 契约不变），长度跟随 days，window 是权威口径
- StatsView 不走 stats store（store 是 dashboard 的 28 天缓存，粒度切换写回会让 dashboard 标注失真），本地按 days 取数
- 踩坑：PageHead 无插槽，粒度按钮放里面不渲染——移到页头下方
- 踩坑：api_stats 初版漏接 top 参数（写了 stats.overview(days, top) 的能力但路由没暴露），验证 21 项抓到
- 浏览器 e2e 全过；17 本小库 Top ≤8 无展开按钮是正确行为
- 路线图剩：自定义智能书架 / 查重阈值
