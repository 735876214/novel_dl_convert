# 2026-09-24

## 第 52 期收尾（设置页「外部账号」+「服务端」剩余能力补齐）

状态：全部完成并已逐笔提交推送（中文 commit，按能力拆分）。

### 已落地能力（默认全关 / 失败不阻塞 / 前端零外呼）
- **审计日志留存**（`core/activity_log.py`）：按大小轮转 + 保留 N 份 + 可选 gzip；`_read_tail` 跨归档合并，`recent/actors/clear/download` 兼容；`GET /api/logs` 回传 `storage`；保存配置后清 5 秒缓存（`invalidate_retention_cache`）。前端 `AuditLogPage.vue` 新增留存策略卡，`settingsFields.SECTION_KEYS.logs` 单开分区只提交 `logging`（避免连带提交 network/download 草稿）。`tests/test_activity_log_retention.py`（22 项）。
- **字体变体**（`core/fonts.py`）：子样式串 → 字重/斜体 + OS/2 `usWeightClass`/head `macStyle` 交叉校验；`list_fonts/save_font` 补 `weight/italic/family_key`（解析不出→留空不猜）。`stores/fonts.ts` `@font-face` 按族归组注入 `font-weight/font-style`；`ReaderView` 阅读器选择器按族分组；`ReaderFontsPage` 按族展示变体。`tests/test_font_variants.py`（7 项，最小 TTF 构造器）。
- **收书目录 AUTO-FINALIZE**（`core/metafetch.py` + `BookDockPage.vue`）：开关→`metadata_fetch.auto_on_import`（开时一并开 `enabled`）、阈值界面 0–100↔内部 0–1、合并预设（overwrite/fill_only/safe_merge→fill_only/embedded_only→skip）映射 `fields` 策略（`preset_to_fields`）。`tests/test_metafetch_presets.py`（5 项）。
- **集成同步**（先前会话已完成，本次只收尾）：`core/sync.py`（ISBN→书名+作者→跳过匹配；Hardcover 推状态/书评；Readwise 推书摘；StoryGraph Cookie 校验）+ 接口 preview/sync/verify + `integration` 能力键 + `IntegrationPage` 控件。

### 验证
- 后端全量 pytest：**762 通过 / 0 失败**（基线 725 + 本期 34 项新测试）。
- 前端：`type-check` 0 错 + `build` + `deploy`（静态产物被 gitignore，不入库）。
- `tests/check_doc_anchors.py` 硬错 0。
- Playwright 冒烟（msedge）：注入 `nf_token` 后，六页（hardcover/readwise/storygraph/book-dock/server-fonts/audit-log）控制台 **0 错误**；未登录首屏的 29 条 401 属预期（已确认）。

### 提交（已推送 origin/main）
- `ad51dee` 审计日志留存 · `5ed287b` 字体变体 · `f99cc70` 自动定稿 · `747ac31` settingsNav 六条 note + 三份文档口径同步。

### 注意点（后续若触碰）
- `core/fonts.py` `_metrics` 里 OS/2 `usWeightClass` 偏移是 `+2`（不是 +4）。
- 字体偏好值：有 `family_key` 时为 `custom:<family_key>`，否则 `custom:<id>`；`readerFontStack` 去前缀后拼 `NF-` 与 `@font-face` family 对齐。
- `playwright-cli` 已装；冒烟用 `AUTH_USER/AUTH_PIN` 起服务 + `/api/auth/login` 取 token + `localstorage-set nf_token` + reload。

## 第 53 期 · 演播者实体补全（已完成，待提交）

状态：代码 + 测试 + 文档 + 记忆全部落地；后端全量 **794 / 0 failed / 0 error**，前端 `type-check`/`build`/`deploy` 全绿（已同步 `novelforge/static/v2`）。尚未 `git commit`/`push`（等用户确认）。

### 落地内容
- **零依赖音频标签解析**：`core/audio_meta.py`（m4b/mp3/m4a/opus/ogg/flac 提取演播者），`audio.first_audio_file` 公共助手；扫描期 `library._scan_once` 落 `books.narrators`。
- **演播者实体**：`core/narrators.py`（镜像 `authors.py`）+ db `narrators` 表（name PK + sort_name/sort_name_local 两列分列，无头像/软删对齐作者现状）；`library.narrators_list()` / `narrator_books()` 聚合。
- **元数据链路**：`db._CLEARABLE`/`_META_FIELDS`/`_meta_out`/`metastore._opf_value` 纳 `narrators`（与 tags 同构）；`fileops.METADATA_FIELDS` 纳 `narrators`。
- **命名 token `{narrators}`**：`fileops.PATTERN_FIELDS` + 前端 `RENAME_TOKENS`（钉死一致）；`FileNamingPage` 缺口「缺 5→缺 4」。
- **按书替换**：`MetadataEditor.vue` 编辑元数据表单加「演播者」字段（镜像 tags 的 tagText/parseTags）。
- **展示**：`BookDetailView` 信息栅格 + `AudioPlayerView` 作者下方展示演播者（零外部请求）。
- **接口**：`/api/narrators`、`/api/narrators/{name}`、`/api/narrators/{name}/sort-name`、`/api/narrators/sort-name/backfill`。

### 测试
- 新增 `tests/test_audio_narrators.py`（解析器 6 格式 + 扫描落盘）、`tests/test_narrator_entity.py`（实体/排序名/软删/接口三态）；`test_naming_tokens.py` 断言 9→10 + 增 `{narrators}` 展开测试。

### 文档
- `docs/bookorbit-module-inventory.md` / `settings-inventory.md`：narrator 改判「已覆盖」（计数 已覆盖 46→47、不做 16→15）。
- `docs/roadmap-gaps-remaining.md`：追加第 53 期记录。
- `MEMORY.md`：追加「演播者实体（第 53 期）」长期约定。

### 提交拆笔建议
按能力拆：`audio_meta` 解析 + 扫描落盘 / `narrators` 实体 + db 表 / 命名 token + MetadataEditor / 展示 + 接口 / 测试 + 文档。中文 commit。
