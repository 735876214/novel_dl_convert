# 2026-09-24

## 第 51 期：设置页外观与阅读器七页照上游补齐并深化

**范围（用户澄清后）**：封面样式 / 布局 / 电子书 / 漫画 / 有声书 补齐上游差额；**浏览行为、PDF 不新增功能**；布局折叠形态与漫画 5 项都按「全做」执行。

- **封面样式**：详情页取色加 `off/one/two` 三档（`stores/coverPrefs.ts` 的 `tint`；单色档**不写第二套 CSS 变量**，靠 `cover-effects.css:69-70` 既有的 `--cover-tint-hue-2: var(--cover-tint-hue)` 收成一片单色，`off` 档连取色都跳过）；叠加层补 `series_index`（`BookCover.vue` 右下角，共 6 项）。`CoverPage.vue` 的 `IMPLEMENTED` 白名单**保持**含 `Book details cover tint`（现在是真实现了）。
- **布局**：`displayPrefs` 加 `primaryLabel`/`secondaryLabel`/`collapsedCover`；`ShelfView.vue` 抽 `labelOf/primaryLabelOf/secondaryLabelOf`（取不到值回退书名/作者、**不留空行**）；折叠行代表本按偏好取（`first` 首本 = 改造前 / `latest` 末册 / `first_unread` 首本未读），`stack`/`mosaic` 用纯 CSS 多封面组合复用 `BookCover`（不额外请求）。原「折叠 computed 只换 members 不换 book」的行为对 `first` 档原样保留。
- **漫画**：`comicPrefs` 加 `mode` 第三档 `infinite_nogap` + `spreadAlign`/`widePage`/`forceTwoPage`/`autoNext`；`ComicReader.vue` 补 `boxW`（ResizeObserver）与 `pageSizes`（图片自然尺寸，宽高比 > 1.15 判宽页，**取不到尺寸不判定**）；自动翻下一本用既有 `api.seriesDetail` + `sortBySeriesIndex`，**零新增后端**。
- **电子书**：`readerPrefs.fixedLayoutWidth`（`book`/`single`/`columns`），只在 `ReaderView.vue` 的 `fixedLayout` 分支给容器尺寸，**仍不注入任何重排设置**。
- **有声书**：`AUDIO_SPEEDS` 补回 `1.75`。
- **PDF**：仅移除错挂的「页与页之间的自定义间距」（那是漫画页条目）+ 删其 import，页尾改为口径说明。**浏览行为未动**。

### ⚠️ 运行时发现并修掉的真缺陷（冒烟的价值）
- `ComicReader.vue` 的 `onKeydown` **直接调 `go()`**，而我把「越界即自动翻下一本」的钩子挂在 `next()` 上 ⇒ **键盘 ArrowRight / PageDown（最常用的翻页方式）会静默失效**（clamp 成同一页、无任何提示）。已改挂到 `go()` 这个**唯一汇聚点**（键盘/点击/工具栏都走它），并在代码注释里写明原因。修复后实测：键盘前进越界会弹出「这本没有系列信息，无法自动翻下一本」（该实例的书无系列），控制台 0 错误。

### 冒烟（`--browser=msedge` + 临时实例）
- 6 个设置页 + 书架：控制台**全 0 错误**；新控件按 `\uXXXX` 转义断言全部命中（封面样式 `11` / 布局 `11` / 电子书 `11` / 漫画 `1111` / 有声书 `11`）。
- **造数做漫画阅读器端到端**（本轮新方法，值得复用）：`Compress-Archive` **只支持 `.zip`** ⇒ 先压成 zip 再 `Move-Item` 改成 `.cbz`；然后 `POST /api/libraries {name,type:'comic',source_dirs:[…]}`（`exts_effective` 自动为 `.cbz/.cbr`）→ `POST /api/libraries/{lid}/scan` → `GET /api/books` 取 id → 打开 `/#/read/{id}`。实测默认档页码 `1 / 2`、工具栏出现第三档「纵向连续（无间隙）」。
- ⚠️ **偏好注入会被覆盖**：直接 `playwright-cli localstorage-set comic-prefs '{…}'` 之后打开阅读器，值会被**偏好同步层**拉回默认（实测落盘变回 `pageView:single, forceTwoPage:true, autoNext:false`）。**改偏好必须走 UI 点击**（设置页控件）才可靠 —— 实测用点击后 `comic-prefs` 正确写成 `double/shifted/auto/false/true`，并因此观察到「`forceTwoPage=false` + 窄容器 ⇒ 自动回落单页」的真实行为。

### 验证与闸门
- 前端 `type-check` 0 错 + `test:unit` 62 + `build` + `deploy`（服务端 asset 切到 `index-D7IxMATc.js`）。
- 后端全量 **725 passed / 0 failed / 0 error**（junit 权威）。⚠️ **中途踩了一次契约**：首跑 `724 passed / 1 failed` —— `tests/test_settings_nav_contract.py` 钉住「设置页 `note` 是纯文本插值」，我在 PDF note 里写了 `**漫画页**` 被拦（改用「」后通过）。
- `tests/check_doc_anchors.py`：**硬错 0 / 疑似漂移 14 / 需人工看 735**，与第 50 期**逐条一致**。⚠️ 但本期在 `ShelfView.vue`（+约 120 行）与 `ComicReader.vue` 上加了行 ⇒ `capability-gap.md` §2 里指向 `ShelfView.vue` 的若干「实测行号」实际已后移（落在「需人工看」桶内、工具不报）。已在 `module-inventory.md` 第 51 期记录里**显式披露**，并写明「下次触碰书架域应整域重测（对齐第 33 期做法）」。
- 提交（已推送 `85bc569..ef55747`）：`cd9e63f`（封面样式）+ `3ebb529`（布局）+ `b88d271`（漫画）+ `7a7d133`（电子书）+ `d2ca0f4`（有声书/PDF）+ `2379628`（漫画 go() 修复）+ `901d8cd`（settingsNav note）+ `ef55747`（文档）。工作树仅剩 `.vscode/settings.json`（他人改动）。
