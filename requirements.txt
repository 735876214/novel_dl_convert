ebooklib>=0.18
httpx>=0.27
pyyaml>=6.0
opencc-python-reimplemented>=0.1.7
Pillow>=10.0
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
beautifulsoup4>=4.12
# PDF 页面渲染：Komga 兼容服务端的页面流要把 PDF 逐页转成图片
# （pypdfium2 自带 PDFium 二进制，pip 装即可，不需要系统库）
pypdfium2>=4.30
# CBR（RAR 格式漫画）读取：rarfile 是纯 Python，但**必须有外部解压器**
# （bsdtar / unrar）。容器里由 libarchive-tools 提供 bsdtar（见 Dockerfile）；
# macOS 本机自带 bsdtar，无需额外安装。
rarfile>=4.0
# 逐库定时扫描（第 17 期 T2）：轻量纯 Py，解析 5 段 cron 表达式，仅在后端用。
croniter>=2.0
