# 临时 override：仅在「无外网、测试版镜像缺失」时用来离线启动真实版镜像，
# 并复用 host 的 novelforge/static/v2（含本次设置页改动）。
# 正常联网环境下请勿保留此文件（它把端口从 8992 改到 8993 并禁用了镜像拉取）。
services:
  novel_dl_convert:
    pull_policy: never
    ports:
      - "8993:8000"
    volumes:
      - ./novelforge:/app/novelforge
