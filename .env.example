# 复制为 .env 后填写；真实密钥不要提交（.env 已在 .gitignore 中）

# 容器内目录（与 docker-compose 的挂载对应）
INPUT_DIR=/app/input
OUTPUT_DIR=/app/output
CONFIG_DIR=/app/config

# 时区
TZ=Asia/Shanghai

# 可选：AI 分章兜底（hybrid 模式下疑难章节才调用，不填则不联网）
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
